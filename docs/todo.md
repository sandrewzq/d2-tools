# 当前待办

> 更新时间：2026-07-27
> 这里只保留当前健康度、未完成任务和未解决 bug。已完成 Bug 与阶段过程使用 Git 历史追溯；详细目标、切片和验收标准在 `docs/work/backlog/`。

## 健康度

| 检查项 | 状态 | 备注 |
|---|---|---|
| GitHub CI | ⏳ push 后异步执行 | 执行 frozen install、`pnpm test`、共享 Shell 原型计算样式契约和 `pnpm typecheck`，并上传 Shell 截图；agent 不等待普通 push 的 CI |
| Release 门禁 | ⏳ 发版时执行 | `tools\git-auto-release.cmd` 必须等待本地门禁和 GitHub Release 全部成功 |
| Agent 自动验证 | ⛔ 默认禁用 | 普通开发不自动运行；用户主动本地测试或打包时照常运行现有测试和类型检查 |

构建提醒：Prototype / Web 生产构建的 chunk 大小提示和 Vite CJS Node API 弃用提示当前不阻断验证。

## 当前任务

| 编号 | 优先级 | 状态 | 任务 | Backlog | 下一步 |
|---|---|---|---|---|---|
| T1 | P1 | 🟠 进行中 | 配装方案与攻略导入 | [配装方案与攻略导入](work/backlog/T1-loadout-plans-and-guide-import.md) | 已完成原型和页面字段 / action 对照；正在重建配装页面结构，随后接入新本地方案模型、批量应用和 DIM 配装分享链接导入 |
| T2 | P1 | 🟡 待推进 | 仓库推荐与清理工作台 | [仓库推荐与清理工作台](work/backlog/T2-vault-recommendation-and-cleanup-workbench.md) | 统一 DIM wishlist、本地目标、同名对比和清理清单 |
| T3 | P1 | 🟡 待验收 | 商人结构、覆盖与官方获取来源 | [商人结构、覆盖与官方获取来源](work/backlog/T3-vendors-and-drop-sources.md) | 已接入 Vendor Group / 目的地两级目录、持久缓存、启动预热和周期刷新；仄已按周六到、周三走规则过滤离场缓存；下一步验收真实地点覆盖、跨重置边界、Offer 时效、去重和机灵模组 A/B |
| T4 | P3 | 🟡 待推进 | 活动复盘增强 | [活动复盘增强](work/backlog/T4-activity-review-enhancement.md) | 接 PGCR、完成时间推算和副本级趋势 |
| T7 | P1 | 🟡 待推进 | 社区推荐边界收口 | [社区推荐边界收口](work/backlog/T7-architecture-boundary-hardening.md) | Core、Services、App、UI 与平台壳的主边界已完成；下一步将本地社区表、个人知识及 AI/light.gg 缓存和外部调用迁入 Services，Core 只保留规则、DTO、解析和匹配。 |
| T8 | P1 | 🟠 正在迁入共享 UI | 全应用 UI 还原 | [全应用 UI 还原](work/backlog/T8-full-application-ui-functional-convergence.md) | 共享 Shell 与设置菜单已完成第二轮原型对齐，当前均待视觉验收。Shell 已收口桌面与断点几何、页头和侧栏摘要、`Tone + Priority`、分层边框、一级导航 current、品牌与工具区、375px 状态入口，以及 AI 宽屏停靠/窄屏 Drawer、抽屉头部、双页签、对话区、输入区、遮罩、焦点与后台任务；设置页已按冻结合同重建唯一内容视图，补齐目录宽度、控件字号、按钮层级、Bungie 表单和重复页头操作清理，并保留八个分区的真实状态、操作与错误恢复。下一步先验收 Shell 与设置页，再迁移统一武器/护甲详情和其余菜单。 |

原型视觉复查已收口：桌面顶部状态栏保持完整显示并恢复为连续直角状态组，375px 顶部状态改为可展开的连续状态列表，首页刷新信息消除横向裁切，AI 窄屏抽屉接管完整工作区宽度，折叠菜单保留无障碍名称；三份原型默认显示完整状态控制器，仅在追加 `?controls=0` 时用于无控件截图；护甲定义无固定属性时改为紧凑提示，获取来源占满阅读宽度。共享 Shell 代码已按该冻结基准迁入，等待规定视口和主题下的视觉验收。

设置页共享 UI 代码已按八个分区完成迁移：目录使用 `240px / 210px / 190px / 单列` 连续导航，并保持透明以承接页面内容底色，不复用一级主侧栏背景；冻结 HTML 的动态目录也已删除旧副说明，恢复八项单行导航，避免副说明、分隔线和下一项标题挤压。状态摘要使用方形 `status-matrix`，设置项使用 `282px / 260px / 单列` 右侧控制轨，版本记录与操作日志使用连续行分隔。冻结 HTML 与共享 UI 均已区分 `fact / status`：日期、版本、数量等事实值不再跟随容器状态染色，只有明确状态词使用语义色；设置命令统一通过 `SettingsButton` 输出 `34px` 高、`144px` 宽，Primary / Secondary / Danger 只改变颜色。设置页字段只覆盖 `background-color`，Select 保留全局下拉箭头和 `32px` 右侧箭头空间；账号、资料库、应用更新和操作日志时间统一为本地时区 `YYYY/MM/DD HH:mm:ss`。Bungie API Key 已遮罩，帮助框恢复普通对象边框，Desktop / Web / Prototype 不再向设置页头注入重复的“复制脱敏诊断”。当前仍待 Desktop 实窗视觉验收，不能标记为完成。

全局时间合同已补齐：绝对时间统一保存 UTC ISO，显示时跟随当前系统时区；日期版本不做时区转换；完整、普通、Shell 紧凑和周期边界时间分别使用固定精度。全应用冻结原型已移除 `Asia/Shanghai`、中国时区标签、原始 ISO 直出、商人预格式化刷新文案和静态倒计时，首页、Shell、账号、资料库、商人和设置共用本地时间规则；武器详情中的 Vendor UTC 周期也改为当地日期、星期和时间。共享 UI 已新增统一时间格式模块，并接入设置后台任务、首页重置周期、商人刷新周期和设置精确时间；商人 workspace 改为传递原始 `resetAt / updatedAt / nextResetAt`，旧 Label 仅作兼容回退。资料库当前只有日期版本字段，继续按日期合同显示；护甲详情当前没有具体时间字段，不需要额外分支。账号与装备详情剩余局部时间格式后续随对应菜单迁移收口。

圆角合同已冻结：Shell、工作区、列表、摘要矩阵、摘要框与状态框使用直角；顶部 Shell 状态项组成连续边框，仅整体首尾为 4px；独立对象卡为 6px；控件与缩略图为 4px；只有短标签、计数与进度使用胶囊。原型 CSS 的历史 `3/5/9/99px` 圆角已收口到语义 token；下一步进行视觉验收并按同一合同迁入共享 UI。

AI 辅助栏的共享产品实现已重建为对话/任务双视图：宽屏作为停靠辅助栏，窄屏作为完整 Drawer，并补齐遮罩、背景隔离、焦点限制、`Escape` 和焦点恢复；抽屉头部已统一为 `64px`，设置与关闭使用 quiet 图标按钮，默认透明且仅在 hover / focus 时显示控件反馈，页签、工具栏、消息列表和底部输入区已按冻结原型改为连续结构。任务页已纠正为 AI 任务、攻略解析、账号对照与配装草稿，不再注入应用后台任务或使用后台任务数量作为页签角标；应用后台任务继续在设置菜单的诊断与操作日志分区管理。顶部和左侧 AI 入口均按整个抽屉的开关状态切换，无论当前处于对话或任务视图，再次点击均关闭；冻结 HTML 的窄屏背景隔离不再禁用 topbar。对话工具栏已恢复“新会话 / 会话记录 + 上下文状态入口”，空会话恢复两条紧凑消息，真实快捷问题收进折叠入口；底部输入区恢复为文本框与 `76px` 发送按钮的直接两列结构。下一步对深浅主题和 `980px` 临界宽度做视觉验收。

窗口关闭按钮保持 quiet 默认态，仅在 hover / focus-visible 时使用红色关闭反馈；Danger 按钮视觉只用于页面内明确的破坏性命令，不能污染窗口控制。

## 验证入口

- 普通 push：GitHub CI 异步执行，不在本地重复运行。
- Release：运行 `tools\git-auto-release.cmd` 并等待全部步骤成功。
- 专项排查：仅在用户明确指定时运行对应命令。
