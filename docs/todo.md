# 当前待办

> 更新时间：2026-09-25
> 本文件只保留**尚未关闭的任务**：一行说清「还要做什么、等谁」。
> 范围、验收标准与自验记录一律在对应 backlog；已完成的 Bug 与任务直接从本表删除、不留归档行，追溯用 Git 历史与 backlog。

## 当前任务

| 编号 | 优先级 | 状态 | 任务 | Backlog | 下一步 |
|---|---|---|---|---|---|
| Bug #103 | P1 | 🔨 已修，待实窗验收 | 穿戴配装时插槽一个都改不了 | [任务说明](work/backlog/Bug103-loadout-wear-missing-socket-data.md) | 穿戴路径读的是账号快照，而快照显式 `Omit` 掉 `sockets` 和 `armor_energy`，所以 `hasVerifiedPlug` 只对已经装着的 plug 成立、护甲核对必产 gap 并清空执行步骤。修法照已走通的武器 Perk 写入路径：要写哪几件就按需拉那几件的完整详情，不整账号转 full。已实现（新增 `shared/loadouts/hydratePlanAccount.ts` + `useLocalLoadoutPlans` 接线 + 回归测试），待真实账号实窗验收 |
| Bug #109 | P2 | 🔨 已定位，未定改法 | 英文推荐模板的栏位名过不了导入校验 | [任务说明](work/backlog/Bug109-english-template-slot-names.md) | 照英文模板填好导不回来，六个栏位列（Barrel or Sight / Magazine / Masterwork / Perk 1 / Perk 2 / Origin Trait）每格都报「无法在这把武器任一已列版本的对应官方栏位中精确确认。」；实测把 Perk 值换成中文官方名就通过。栏位名按资料库**当前语言**的定义核对（`projectCommunityMatchDefinition` 只取 `displayProperties.name`，资料库语言默认 `zh-chs`），英文定义池（`community.ts:397`）只有武器名桩定义、没有插件。中文模板不受影响，界面语言与资料库语言都是英文时才导得回来。可选改法：英文模板的栏位示例值与说明改用资料库语言的官方名（最小）、下载时按资料库语言判断给不给英文模板、或给插件也补一份英文定义池。未运行本地自动化验证 |
| Bug #110 | P1 | 🔨 已修，待重导验收 | DIM 导入后来源行计数比管理卡片少几十件 | — | 主进程失效按规则的**覆盖集**（`item_hashes`，导入期家族全展开的产物）逐 hash 删匹配行，渲染层随后的重扫与内存合并却只按**声明集**（`item_hash`，`wishlistStore.ts:164` 保证它是 `item_hashes[0]`）。展开出来的同族其它版本因此「匹配行被删、又不在重扫范围里」，来源行计数长期偏少，直到一次整账号重扫（重启）才补回来。本机只读副本实测（2026-09-24）：Aegis 心愿单导入后仓库行 467、管理卡片 509；重启全量重扫后 467 → 509、Aegis 推荐表 467 → 507，与管理卡片的 509 / 507 逐字一致，缺口正好是那 44 件从未被重算的实例（心愿单 +42、推荐表 +40）。修法：`packages/desktop/src/renderer/features/vault/VaultPage.tsx:284` 的 `collectWishlistWeaponHashes` 改取覆盖集，与 `packages/desktop/src/main/ipc/wishlist.ts:205` 的 `dimWishlistItemHashes` 对齐（同一套影响范围，不再各算一份）；`packages/desktop/src/renderer/api/vaultApi.ts:15` 的 `DimWishlistRule` 补 `item_hashes` 字段——此前渲染层类型里没有这一列，覆盖集根本拿不到。T56 家族全展开落地后的一处漂移。未运行本地自动化验证 |
| Bug #111 | P1 | 🔨 已修，数据已落地，待实窗验收 | 本件 Roll 的 Perk 不显示说明 | — | 「本件 Roll」浮层的说明来自快照里的 `weapon_roll`，而快照构建查定义时走 `account-snapshot` 投影，`projectInventoryItemSummary`（`gameDataWorker.ts:363`）的 `displayProperties` 只留了 `name` 和 `icon`，说明被砍掉，`summarizeWeaponRoll.addPlug`（`summary.ts:2221`）读的 `definition.displayProperties.description` 因此恒为空——本机只读副本里落盘快照的 13804 条插件条目，0 条带说明，同一批插件在 `world.sqlite` 里都有说明，完整详情缓存里的武器插件也大多带说明。修法是在这个投影里补回 `description`；完整详情路径不受影响（走无投影的全量定义）。代价：按 hash 去重后 IPC 载荷 +约 84 KB（842 个插件 × 平均 102 字节），快照 JSON 因每条实例各存一份会 +约 1.4 MB（7.8 → 约 9.2 MB）。**生效要两步**：主进程得跑改后的产物（`dev:desktop` 启动时增量编译 worker），再刷新一次账号重建快照；旧快照不会自动重建，另外已打开的详情不会自己重新取数，要重开。本机实测（2026-09-25 00:43 那份快照）：13804 条插件条目 13804 条带说明；用 `createSelectedItemPreview` 复算前 5 件武器 91/91 条带说明，「小试牛刀」读到的是「每次最后一击都能提高充能/拉弓速度。」。未运行本地自动化验证 |
| Bug #112 | P1 | 🔨 已修，待实窗验收 | Perk 说明浮层被右侧实例栏裁掉 | — | 层级约束全压在一个 `z-index` 上：`.weapon-detail-perk-entry` 停在 `z-index: 0`（`packages/ui/src/styles/components/09-weapon-detail.css:453`），自己就是一个层叠上下文，浮层那条 `--layer-popover`(20) 只在它内部生效——浮层能压过什么，全看条目这一层站在哪。上一版把条目停在 `calc(var(--layer-sticky) - 1)`(9)：赢过了兄弟与来源卡栏头(8)，却低于右侧实例栏和写面板(10)，浮层从卡片中心向两侧展开，最右一列卡片的右半截伸进右侧栏那一列，就被不透明的右栏从右边裁掉（实窗看到的说明文字被「目标角色 / 取出装备 / 复制转移计划」面板盖住）。修法是把页面层级重排成 章节页签 12 > 悬停卡片与说明浮层 11 > 右侧实例栏 / 写面板 10 > 来源卡栏头 8 > 内容 0（`--layer-sticky` 是 10）：条目从 9 抬到 `calc(var(--layer-sticky) + 1)`，相邻的章节页签跟着从 `+1` 抬到 `+2`，保住「页签是窗口构件，谁都不许盖」。只改 `09-weapon-detail.css` 的两个层级值和四处注释，没有 JS 改动；`architecture-maintenance.test.ts` 那条层级守卫跟着换期望值，并补一条把右侧实例栏的层级也钉住——此前只钉了页签与栏头，条目与右侧栏的相对次序没人守，这次正是从这儿漂开的。层级顺序同步写进武器详情合同（`docs/work/references/ui-specs/equipment-details.md`）。窄屏（≤1360px）右侧栏本来就是抽屉(`--layer-drawer` 40)，不受影响。未运行本地自动化验证 |
| T1 | P1 | 🔨 仓库、装备详情两个切片已完成，剩配装、AI 助手与两个孤儿组件 | 全应用英文国际化补齐 | [任务说明](work/backlog/T1-english-internationalization.md) | 已迁完两块：仓库切片（`VaultCopy` 骨架 + 槽位键 `VaultSlotKey`）与装备详情切片（`ItemDetailCopy` 骨架，插槽名、属性名、来源措辞从 app 侧中文字符串改成 token，成句由 `packages/ui/src/item-detail/itemDetailLabels.ts` 按界面语言现算）。下一步按 T1 既定顺序做配装页（消费已有 `loadoutsCopy`；T58 第 2 批已把子职业术语接上 copy，页面其余文案仍是写死中文）与 AI 助手面板，然后收 S10 两个孤儿组件（`VaultDuplicateGroups` / `VaultTargetRulesPanel`，按用户指示不接线、不删除，只决定去留），最后是阶段 D 的预览 fixture 与静态门禁。刻意保留中文的写入数据、诊断留痕，以及 `packages/core` 那批来源与显示名词表，清单见 backlog 实施记录。2026-09-23 `pnpm ci:local` 全绿，顺带修回一处槽位顺序回归：没有 bucket hash 的条目只能按显示名认槽位，`slotRank` 当时只按 key 查表，这批条目会被拼音序打散 |
| T56 | P1 | 🔨 主体已落地，待重导与实窗验收 | 统一推荐模型与多来源导入（重开：导入解析层四个残留问题 + 同名匹配口径） | [任务说明](work/backlog/T56-unified-recommendation-model.md) | 原 Bug #105–#108 并入本文件，编号冻结作追溯。四条同一个根：导入期「一个名字、一条 hash 代表哪个身份」的解析口径散在多处，各处自己判一次。名字查找与校验两套口径（#105）：已收敛成 `packages/services/src/gameData/lookupText.ts` 一份实现并补折叠名兜底，待 build 后重导；CSV 判重只认一个中文名（#106）：改在数据源脚本 `攻略/脚本/extract-starside-pve-farming.mjs`（图标 / 版本序收窄 + `故我在` 合并），待重导；来源名有文档标题与文件声明两套来路（#107）：收敛到 `recommendationSourceNames`、声明名降为副标题 `declared_label`、缓存版本 24 → 25，待实窗验收。**同名匹配口径 2026-09-24 已定：推荐跟着武器走，不跟着版本走**——导入期按武器家族展开到全部版本、取消发布组归约，CSV 与愿望单文本同一套（赛季版本不同、同发布组孪生都一样；武器与非武器同名不处理）；④ 名字带（专家）/（失时）/（痛苦）/幻彩后缀这一档复算为 140 个发布组、109 个基础名，原始表 0 行把后缀写进武器名 → 一起给 → 全匹配，现状已经在做（Aegis CSV 185 个「基础版 ↔ 后缀版」配对，实测 134 装得上、6 装不上，都卡在起源栏位，归池子自检剔除）；展开只落在本来源没写过规则的版本上（声明版本优先，避免同一版本上叠出两条规则、把「符合 n 套」抬起来）；展开后逐 hash 用这一版自己的插件池自检——**自检只看「这版有没有规则里写下的东西」，不看栏位**（2026-09-24 改判，见本行末尾第六批），这就是 #108 的修法；只展开不自检就是 613 / 12698 个永久红叉。本机只读副本实测（改判前按栏位判的那次复算记的是 CSV 覆盖 990 → 1080、愿望单文本 938 → 1081，已作废），改判后 Aegis 两条链的覆盖集都是 1077 个 hash、逐条相同。**已有数据先失效**：现存的导入都是旧口径写下的，落地后按「需要重新导入」处理、不参与匹配，导入文件也要用新脚本重新生成（`recommendation_documents` 加一列记导入口径版本，管理面标「按旧口径导入」——已落地，见本行末尾）。③ 撞译 2026-09-24 也定了：全库 1318 个武器名里同名跨武器类型的只有 2 组（隐士 The Recluse / The Eremite、龙息 Dragon's Breath / Breath of the Dragon），用「英文名称」列消歧——**有英文名就只按英文名匹配**（2026-09-24 改判，原定「中英文名都写就都要命中」）；英文名写了却匹配不到，按「找不到可核对的武器」报错，不退回中文名；只写中文名而这个名字横跨两个以上家族则拒导，要求补「英文名称」；同名、类型相同只差弹药槽的 4 组（亚琛-LR2、恼人麻烦 Mk.28、荒芜之地 Mk.24、真实预言）2026-09-24 改判为不做「取最新」——按名称匹配到两代都算，由逐 hash 池子自检分开，只写「武器ID」的行仍以声明为准；`极高反射` 同名同类型同槽、中英文名都一样，只有弹药类型不同（主 / 特殊）——**算第六种：CSV 单独加一列「弹药类型」，非必填**，填了就要命中（与英文名同一口径），① 的家族展开不跨弹药类型（今天全库 1212 个家族里只有它命中）；来源表靠「分类」/图标槽位分（白弹 `shopping-primary` / 绿弹 `shopping-special`）。模板第 3、4 列已是「英文名称」「弹药类型」，数据源脚本的「英文名称」留空、「弹药类型」按页面分区填（`攻略/脚本/extract-starside-pve-farming.mjs:40` 的 `PAGE_AMMO_TYPES`，混弹药的页面留空），撞译行仍靠「武器ID」分开。TXT 侧 2026-09-24 查清了原来标「未查」的那条：愿望单文本只写 hash、没有栏位，落库 `slot` 空串是格式约定，读取期按这把枪自己的定义解析——两个 DIM 源各 348 条加速突击要求（共 48 把枪）实测 348/348 归到 `origin` 栏，不是缺陷；TXT 侧要补的家族全展开 + 逐 hash 自检 + 声明版本优先 + 旧口径重导已落地，两处接线（身份推导按 `family_key` 取同族版本、自检前把同族版本的定义补进导入期定义池）也一并接上。按同一份账号快照复算，落地后三份来源的仓库命中数预计从 636 / 452 / 498 收敛到 509 / 509 / 498（推荐表失去 137 件死配对虚高、新增 10 件；心愿单新增 57 件；dim 不动）。**2026-09-24 主体已落地**：身份推导（家族全展开 + 声明版本优先 + 逐 hash 池子自检）在 CSV 与愿望单文本两条导入链共用 `expandWeaponFamilyHashes`（`weaponRecommendationKnowledge.ts:1405`），发布组归约删除，家族关系的两个实现连接键都换成 `family_key`，`DimWishlistRule` 新增 `item_hashes` 覆盖集、读取期仍只做 `includes`；旧口径失效：`recommendation_documents` 加 `import_pipeline_version` 列（schema 11 → 12）、读取侧四路统一挡住、管理面标 `needs_reimport` 与「按旧口径导入，需要重新导入」、`matchAlgorithmVersion` 25 → 26、状态 API 改由参与匹配的来源派生；迁移用例新增 `markImportsCurrent`，推翻原来「DIM 侧不重导」的口径并各加一条「翻转之前读不到」的断言。**2026-09-24 第二批已落地**：③ 的英文名判据（`weaponRecommendationKnowledge.ts:1404` 的 `resolveOfficialWeaponDefinitions` 写了英文名就只按英文名匹配、`:701` 的 `weaponIdentityAmbiguityIssue` 判撞译）、⑥ 的「弹药类型」列与校验（`:729` 的 `narrowItemHashesByAmmoType`）、两条判据都只在没写「武器ID」时生效（`:786` 的 `hasDeclaredWeaponIds`）、英文定义池（desktop `community.ts:397` 的 `loadEnglishItemDefinitions`）、表头加两列（中文模板 14 列 / 英文模板 13 列，加列前的两版表头照旧认得）；perk 名解析不到本来就进预览异常清单（`resolveRuleRequirements`，早已实现，不是本轮新增）；`importWeaponRecommendationCsv` 里那次「可导入行数」复算也补上了同一份身份上下文（从前只有主进程预览带家族关系，靠展开才装得上的行会被算成 0 条、导不进）。导出侧拿不到 hash → 英文名，导出的「英文名称」列只能是空值，撞译行回导时会被拦下要求人工补。**2026-09-24 第三批（模板可读性）已落地**：导出模板的表头自带填写说明（`用途分类` / `枪管瞄具` 去掉列名里的斜杠，说明写在括号里、导入时由 `canonicalHeader` 去掉），表头下面跟一行示例数据，`isTemplateExampleRow` 按第一列的标记认出并在解析层丢掉——预览、校验、落库、定义池收集读的是同一份行，示例行不会进库；多值分隔符统一成不带空格的 `/`，带空格照旧认；导出成功文案与仓库导入说明同步，顺带修掉「当前模板 12 列」这个过期数字。**2026-09-24 第四批（只收武器 + 统一模板加「武器ID」列）已落地**：用户三项决定——推荐功能目前只匹配武器，身份推导就只收 `itemType === 3`（`isWeaponDefinition` 收口在 `resolveOfficialWeaponDefinitions` 与 `buildWeaponNameEntries` **两处**；只改名字展开那一处不够，`enrichWeaponRecommendationRow` 会把同名非武器的 hash 补进「武器ID」，随后被当成声明集）；③ 撞译继续拦住、`field` 改报「武器ID」、`value` 读作者自己写下的 `__declared_weapon_ids` 而不是补出来的那串；统一模板加「武器ID」列（中文 15 列 / 英文 14 列，武器ID 第 3 列、英文名称与弹药类型顺移到第 4 / 5 列，加列前的 14 / 13 列带说明表头另立常量继续认）。起因是 ⑥ 把同名徽标 / 载具 / 护甲的 0 弹药类型当成第二种弹药类型，报出「（ / 主弹药）」这种空标签的拦截；修复后三份文件拦截行数 9 / 8 / 8 → 1 / 2 / 3（Aegis:142；LGpig:132、:249；YXCRALLXY:170、:390、:414，留下的都是真需要消歧的行）。四份文件各自的改法都用产品代码实测过：真实预言只能靠 `武器ID=960948343`（两代英文名与弹药类型都一样）、极高反射填 `武器ID=2662459958` 或换统一模板表头后填 `弹药类型=特殊弹药`、龙息填 `英文名称=Dragon's Breath`、YXCRALLXY 那三行还要把表头从 11 列换成统一模板、`Aegis心愿单.txt` 不用改（只写 hash，展开链不经过名字）。模板示例行的中文三格换成插件池里真有的官方名，复测 0 异常；英文模板的栏位名过不了校验是另一回事，登记 **Bug #109**。**2026-09-24 第五批（数据源脚本加「弹药类型」列 + 三份导入文件重新生成）已落地**：脚本玩家模板表头 13 → 14 列、新增「弹药类型」（按页面分区填，混弹药的页面留空），单行同名武器新增 `assignSingleWeaponId`——只在名字横跨两个以上家族、页面图标恰好钉到一版时才写「武器ID」（实跑只写 `龙息 → 17096506`、`真实预言 → 960948343` 两处）；`攻略/愿望单与导入模板/导入模板/` 三份文件按新脚本重新生成，`YXCRALLXY推荐表_导入模板.csv` 从旧 11 列换成带说明的 15 列统一模板、另按插件池反查填三行消歧取值（`:170 极高反射` 填 `弹药类型=特殊弹药`、`:390 隐士` 填 `武器ID=3257283337`、`:414 真实预言` 填 `武器ID=960948343`）；只读副本实测 748 / 340 / 455 行全部可导入、0 条异常，第四批留下的 1 / 2 / 3 行人工消歧清空，旧文件备份在 `攻略/.local-data/tmp/`。别名复核：`LGPIG_PERK_ALIASES` 22 个目标名都在，「`失衡弹药` 错名修正」不成立，无需修正。**仍未落地**：在应用里把三份来源与迁移出来的导入各重导一次（旧口径一律不参与匹配）。原「235 个死配对」复测不成立，实测 68 个。第五批顺带修掉一处挡下桌面端启动的类型不匹配（`buildSlotPerkIndex` 的 `plugSetDefinitions` 改成可选，与 DIM 侧本来就可选的定义池对齐）。**2026-09-24 本地 CI 已全绿**（`pnpm ci:local`：docs:check / encoding / 7 包 build / 行为测试 151 文件 685 用例 / 测试质量 / 架构测试 / 视觉契约 / 7 包 typecheck），顺带把 3 个文件 7 条陈旧断言改齐（迁移用例的 `user_version` 11 → 12、投影对拍改按 `declared_label` 数具名来源、v10 夹具翻回旧口径）。实窗验收仍待用户。**2026-09-24 第六批（两条链的覆盖口径对齐）已落地**：起因是同一份 Aegis 数据生成的两个导入文件在应用里读出的覆盖数不同（推荐表 1072 把、心愿单 1077 把）。自检不再回答「这版能不能整套装出来」，只回答「这版有没有这条规则提到的东西」——推荐表每格都带栏位、愿望单文本只有一串 perk，按栏位判必然算出两个覆盖集。落点：`filterItemHashesByOwnPerkPool`（`weaponRecommendationKnowledge.ts:1948`）的入参只收 `candidates`、`VersionPerkPool` 只留 `everyHash` / `everyName`（`weaponRecommendationKnowledge.ts:1974`），CSV 侧同判据调用（`weaponRecommendationKnowledge.ts:859`），DIM 侧去掉调用点里的 `slot: ""`（`dimWishlistValidation.ts:134`）——两条链此前正是靠这个空串一个按「任意栏位」判、一个按真实栏位判。改后两侧覆盖集逐条相同（1077），此前只有心愿单覆盖的 5 个版本（新玩意 v610 / v710、环形逻辑 v700、蒙恩 v730、力拔头筹 v700）被覆盖，它们「两个 perk 抢同一个特性槽」在原始 manifest 里已取证。「这版装不出整套」改由匹配期表达，不再从覆盖集里抹掉整条规则——否则用户看到的就是漏武器。**未做**：「这版配不出整套」的界面标注（要新增匹配状态词与存储标记）；生效需要在应用里重导两条来源。**2026-09-24 第七批（两条链的「Perk 命中」分档对齐）已落地**：覆盖集对齐之后，同一份 Aegis 数据在两个导入文件上的分档仍差 2 个实例（推荐表 `0/2` 对心愿单 `0/1`，形态同实窗截图里的 `1/1` / `0/1`）。根因在读取期：CSV 的栏位是作者写死的，愿望单文本的栏位是读取期按插件池反推的，而反推取的是**手上这一件的版本**——家族展开到换过插槽池的版本以后，这版装不进标准栏位的 perk 归成 `special` / `unknown` 被丢掉，同一条作者意图在愿望单里只剩 1 条要求。改法：`diagnoseDimWishlistRules`（`dimWishlistDiagnostics.ts`）改按**规则声明的那一版**（`rule.item_hash`）建目录，声明版本不在定义池里才退回实例版本，读取期与导入期校验从此是同一份输入；`matchAlgorithmVersion` 26 → 27，只改读取期、不需要重导。只读副本复算：两条来源的分档修后逐实例相同，202 条记录逐栏对拍只差「同栏两条要求被后写者胜合并」这一处预期差异。**2026-09-25 第八批（逐 hash 自检的判据放宽成存在性）已落地**：起因是实窗反馈「starside 有暗夜魅影，导入进来没有了」——仓库 13 件暗夜魅影里 12 件（hash `34731066`）卡片上是「暂无推荐来源」。只读副本复算：这版的枪管 / 弹匣 / 大师杰作 / perk1 / perk2 全部命中，只因起源特性不是规则要的 `加速突击`，被导入期自检的 `requirements.every(...)` 整版剔除——判据是存在性（`some`）才与〈覆盖口径改判〉写下的口径一致（规模：推荐表 748 条规则里 275 条、心愿单 6009 条里 1945 条有版本被这样整版剔除）。修法：`filterItemHashesByOwnPerkPool`（`weaponRecommendationKnowledge.ts:1967`）改成 `judgeable.some(...)`，候选为空的要求不参与判定。新判据下覆盖集几乎是整个家族（1659 个展开版本 → 保留 1600），第一次复算两条链差 5 个版本，逐条查全是同一个原因——这 5 个版本跟规则的交集**只有大师杰作**，而 CSV 模板有「大师」列、DIM 文本没有这一栏，于是 CSV 侧把它排除在判定之外（要求本身照旧落库、照旧展示与参与匹配）；排除后两条链都是 **1600 个 hash、差集 0 / 0**。导入口径 `recommendationImportPipelineVersion` 2 → 3，四条来源都要重导一次；旧判据下算出的「509 / 509 / 498」预期要重新量。未运行本地自动化验证 |
| T58 | P1 | 🔨 第 1、2、4 批已落地，待实窗验收 | 新建配装工作流与子职业保真 | [任务说明](work/backlog/T58-ingame-loadout-subclass-fidelity.md) | 一套草稿从入口到存进游戏内槽位的完整路径（**原 T92 已并入**）。已确认：两个来源分开计数（游戏内每角色 10 个封顶、应用不限量）并靠标记互相引用；草稿进编辑器自动建、保存由玩家点；护甲改设置后不自动重算，只打「需要重新计算」并禁用接受，脏标记要点名是哪一类设置变了（护甲要求 / 模组调整 / 碎片读数）——模组类执行前还有一次核对，另外两类没有别的校验，禁用接受就是它们唯一的闸门；子职业不写回游戏，用「读当前配置 + 校验闸门」代替。完整流程原型按玩家的**四种入法**（存下现在这一身 / 抄一套 / 凑一套 / 改一套）× **主干七步**排开，29 屏放在 `docs/work/backlog/T58-loadout-flow-prototype/`，2026-09-20 定稿；子职业分层原型（`docs/work/backlog/T58-subclass-prototype/`）待确认。两处原型 2026-09-23 从 `.local-data/tmp/` 搬进 backlog 留档——那边被 git 忽略，放着随时会丢。第 1 批（护甲脏状态 + 截断警告）、第 2 批（子职业分类收敛、身份保真、星象碎片属性进求解器、展示、复制入口只收三武器五护甲——口径同 DIM 游戏内配装十格，装饰槽与赛季神器都不进目标）与第 4 批（改一套的另存为出口 + 跨来源标记）已落地，未运行本地自动化验证；2026-09-22 又修两处：跨来源比对的槽位标签对不上（子职业标签是「职业分支」，比对表只认「子职业」），导致「内容一致」这一档一次都没出过；DIM 链接 payload 里没有槽位字段，导入的装备会整批落进「其他装备目标」，改为按 item hash 反查账号实例补 Bucket。下一步跑一次实窗（验 Bug #103、抓子职业真实读数、顺带验第 1、2、4 批），再做第 3 批 |
| T93 | P1 | 🔨 已做热点等价改写（1.88–2.03x）、账号刷新时限与离线计算，全自动仍会截断 | 护甲规划器求解耗时 | [任务说明](work/backlog/T93-armor-planner-performance.md) | 槽位规则全设为「自动」时一次搜索 3651 万次内层迭代，8.5 秒且撞上 `state_limit` 截断——「慢」和「不确定」是同一个问题。分叉全在属性模组：`auto` 把每个部位的可选装法乘成 `调谐数 × (1+6+6)`，钉死属性后迭代次数掉到 120 万、0.27 秒且完整搜完。已做三处严格等价改写（`stateKey` 手工拼接替代 `join`、主循环属性累加绕开 rest 参数数组、`addArmorStatValues` 展开成固定属性），同一份输入下计时 1.88–2.03x，`states_examined` 与候选摘要逐行相同。2026-09-23 先修「卡住」：账号刷新段（`armorWorkspaceRuntime` 的强制 `refresh`）每一步虽有 30 秒超时但整段无上限，已压到 20 秒、超时退回上次同步的账号并把降级写进结果 `warnings`，点计算一定回结果；又做「离线计算」：护甲规划器的 profile 组件组之前比账号同步多一个 309，导致账号同步刚拉回的 profile 一次都命中不了，已对齐成同一组；新增 `packages/services/src/account/armorPiecesStore.ts`，把一次联网得到的归一化护甲数组落盘（10 分钟有效，账号一重新同步就作废），命中就跳过账号摘要，点「计算」不再联网（首次计算或缓存作废后仍要一次全量 GetProfile）。截断警告的文案也补上了绕法。下一步做 DP 前的按部位候选 Pareto 剪枝与下界剪枝，这是把「自动」的分叉压到「钉死属性」同级的唯一手段；`state_limit` 目前恒 2000 且界面不传，也需要档位或自适应。账号刷新两行的实测值、以及缓存命中时不联网这条，都仍未测。未运行本地自动化验证 |
| T94 | P2 | 🔨 已实现，待实窗验收 | 仓库推荐筛选新增「无任何匹配」按钮 | — | 只筛「卡片上没有任何推荐来源」的装备，判据与卡片那句“暂无推荐来源”同源：`packages/ui/src/vault/VaultPageContentView.tsx` 的 `isVaultItemWithoutRecommendation` 直接看实例来源摘要表，并沿用来源筛选的守卫——来源扫描没跑完、这件还没算到的不算无匹配，否则会先冒出一批随后又消失的装备。与来源勾选互斥：打开它清空来源勾选，勾任一来源则关掉它。按钮排在来源列表上方单独一行，沿用来源行的外框、选中态、勾选框与数量徽标；数量按「其他已选条件 + 左侧筛选」计算，为 0 且未打开时禁用；作为一条已生效条件进入筛选摘要，随重置按钮与切换出武器组一起清空；左侧分面计数一并按它收窄（`buildVaultRecommendationAllowedItemKeys` 加同一份入参）。「无任何匹配」与两条说明已补 en-US 词条，合同写进 `docs/work/references/ui-specs/application-workspaces.md` 的仓库推荐筛选段。未运行本地自动化验证 |

## 说明

- `T数字` 与 `Bug #数字` 创建后保持全局唯一且不再改号；本表按任务编号升序排列，优先级和状态以对应列为准，不通过表格位置表达。
- 已完成的 Bug 与任务直接从本表删除，不保留归档行；追溯使用 Git 历史与对应 backlog。
- T41「邮政官安全处理」已取消，编号冻结且不再复用。
- T23「D2Checkpoint 进度机器人集成」已于 2026-09-21 取消，不再做（2026-09-08 发出授权申请后一直未推进）；工具导航里的 D2Checkpoint 官网入口保留，仍不读取其实时状态。编号冻结保留作追溯，不单独占行。
- T55 / T57 / T59 / T60 / T61 与 Bug #80–#87 已并入 T56；T56 于 2026-09-19 验收关闭，上述编号冻结保留作追溯，不单独占行。
- Bug #105 / #106 / #107 / #108 已于 2026-09-24 并入 **T56**（2026-09-24 重开），四份单独的说明已删除，编号冻结保留作追溯，不单独占行；T56 的 2026-09-19 关闭记录原样留在 backlog 里。
- T76 的亲和性机制保留并生效，但它对「换 Perk 后读回旧配置」这个 bug 的结论已被证伪（2026-09-18 的留痕推翻了它）；该 bug 的修复由 **T77** 承接，已于 2026-09-19 验收关闭，T76 编号冻结保留作追溯，不单独占行。
- **T78 是 T77 的实窗复验修复轮**（受理状态跨弹框存活、补上第四份视图、面板去断言），说明并入 T77 与同一份 backlog，不单独占行；编号冻结保留作追溯（代码注释里引的就是它）。T77 / T80 已于 2026-09-19 一并验收关闭。
- T79「插槽状态收敛成一份派生结果」已于 2026-09-19 验收关闭，编号冻结保留作追溯（代码注释里引的就是它），不单独占行。
- T90「工具导航」菜单已于 2026-09-20 实窗验收关闭，编号冻结保留作追溯，不单独占行。
- T82「首页简报的定义读取失败会抛穿」已于 2026-09-19 验收关闭，编号冻结保留作追溯（代码注释里引的就是它），不单独占行。
- T46 只**消费**推荐结果，不并入 T56。
- T43「任务待处理中心」、T44「光等提升路线」、T45「本周刷取清单」已于 2026-09-20 并入 **T91「待办清单」**；T91 于 2026-09-21 判定在实际账号数据下不可用，整体取消并删除（界面、装配与「向 Bungie 读取任务 / 赏金」的链路一并移除），四个编号冻结保留作追溯，不单独占行。
- T81「首页掉落池条目能跳进待办里对应的行动行」随 T91 于 2026-09-21 一并取消，编号冻结保留作追溯，不单独占行。
- T92「新建配装完整工作流」已于 2026-09-20 并入 **T58**（含原型、已确认结论、未定项和切分表），编号冻结保留作追溯，不单独占行。
- 子职业口径（2026-09-20 定）：**不把星象、碎片、超能、技能写回游戏。** 玩家在游戏里选好子职业，应用只读取当前配置再参与配装，并用「配装假定的配置 = 角色当前配置」这道校验闸门代替写回。依据：DIM 能写，但 issue #10344 / #8750 记录无谓改动会让玩家丢掉全部超能/技能能量；参考项目 d2-armor-solver 也只写五个护甲槽和护甲模组、绝不碰子职业插槽，并要求碎片属性与角色当前配置精确一致才允许直装。写回如果将来要做，单独立项，不并入 T58。
- 推荐来源口径（T56 定，2026-09-16）：**愿望单文本只是数据来源的一种格式，不是面向用户的概念**；判定、标签、文案一律从数据来源取，禁止硬编码。
- UI 只维护 `packages/ui` 的共享产品实现；Web 用于快速预览，Desktop 用于真实功能验收。
- 自动验证策略见 `AGENTS.md`：默认不跑本地测试 / 构建 / 打包，只有用户明确要求时才执行。
