# 当前待办

> 更新时间：2026-09-21
> 本文件只保留**尚未关闭的任务**：一行说清「还要做什么、等谁」。
> 范围、验收标准与自验记录一律在对应 backlog；已完成的 Bug 与任务直接从本表删除、不留归档行，追溯用 Git 历史与 backlog。

## 当前任务

| 编号 | 优先级 | 状态 | 任务 | Backlog | 下一步 |
|---|---|---|---|---|---|
| Bug #103 | P1 | 🔨 已修，待实窗验收 | 穿戴配装时插槽一个都改不了 | [任务说明](work/backlog/Bug103-loadout-wear-missing-socket-data.md) | 穿戴路径读的是账号快照，而快照显式 `Omit` 掉 `sockets` 和 `armor_energy`，所以 `hasVerifiedPlug` 只对已经装着的 plug 成立、护甲核对必产 gap 并清空执行步骤。修法照已走通的武器 Perk 写入路径：要写哪几件就按需拉那几件的完整详情，不整账号转 full。已实现（新增 `shared/loadouts/hydratePlanAccount.ts` + `useLocalLoadoutPlans` 接线 + 回归测试），待真实账号实窗验收 |
| T1 | P1 | 📝 候选方案，待排期 | 全应用英文国际化补齐 | [任务说明](work/backlog/T1-english-internationalization.md) | 先页面标题与共享弹窗，再仓库、装备详情、配装、AI 助手；仓库切片已于 2026-09-20 勘察完，切片顺序、必须先行改掉的 9 处中文逻辑和会撞的测试都记在 backlog 里 |
| T58 | P1 | 🔨 第 1、4 批已落地，待实窗验收 | 新建配装工作流与子职业保真 | [任务说明](work/backlog/T58-ingame-loadout-subclass-fidelity.md) | 一套草稿从入口到存进游戏内槽位的完整路径（**原 T92 已并入**）。已确认：两个来源分开计数（游戏内每角色 10 个封顶、应用不限量）并靠标记互相引用；草稿进编辑器自动建、保存由玩家点；护甲改设置后不自动重算，只打「需要重新计算」并禁用接受，脏标记要点名是哪一类设置变了（护甲要求 / 模组调整 / 碎片读数）——模组类执行前还有一次核对，另外两类没有别的校验，禁用接受就是它们唯一的闸门；子职业不写回游戏，用「读当前配置 + 校验闸门」代替。完整流程原型按玩家的**四种入法**（存下现在这一身 / 抄一套 / 凑一套 / 改一套）× **主干七步**排开，29 屏放在 `.local-data/tmp/loadout-flow/`，2026-09-20 定稿；子职业分层原型（`.local-data/tmp/t58-subclass/`）待确认。第 1 批（护甲脏状态 + 截断警告）与第 4 批（改一套的另存为出口 + 跨来源标记）已于 2026-09-20 落地，未运行本地自动化验证；下一步跑一次实窗（验 Bug #103、抓子职业真实读数、顺带验第 1、4 批），再做第 2 批 |

## 说明

- `T数字` 与 `Bug #数字` 创建后保持全局唯一且不再改号；本表按任务编号升序排列，优先级和状态以对应列为准，不通过表格位置表达。
- 已完成的 Bug 与任务直接从本表删除，不保留归档行；追溯使用 Git 历史与对应 backlog。
- T41「邮政官安全处理」已取消，编号冻结且不再复用。
- T23「D2Checkpoint 进度机器人集成」已于 2026-09-21 取消，不再做（2026-09-08 发出授权申请后一直未推进）；工具导航里的 D2Checkpoint 官网入口保留，仍不读取其实时状态。编号冻结保留作追溯，不单独占行。
- T55 / T57 / T59 / T60 / T61 与 Bug #80–#87 已并入 T56；T56 于 2026-09-19 验收关闭，上述编号冻结保留作追溯，不单独占行。
- T76 的亲和性机制保留并生效，但它对「换 Perk 后读回旧配置」这个 bug 的结论已被证伪（2026-09-18 的留痕推翻了它）；该 bug 的修复由 **T77** 承接，已于 2026-09-19 验收关闭，T76 编号冻结保留作追溯，不单独占行。
- **T78 是 T77 的实窗复验修复轮**（受理状态跨弹框存活、补上第四份视图、面板去断言），说明并入 T77 与同一份 backlog，不单独占行；编号冻结保留作追溯（代码注释里引的就是它）。T77 / T80 已于 2026-09-19 一并验收关闭。
- T79「插槽状态收敛成一份派生结果」已于 2026-09-19 验收关闭，编号冻结保留作追溯（代码注释里引的就是它），不单独占行。
- T90「工具导航」菜单已于 2026-09-20 实窗验收关闭，编号冻结保留作追溯，不单独占行。
- T82「首页简报的定义读取失败会抛穿」已于 2026-09-19 验收关闭，编号冻结保留作追溯（代码注释里引的就是它），不单独占行。
- T46 只**消费**推荐结果，不并入 T56。
- T43「任务待处理中心」、T44「光等提升路线」、T45「本周刷取清单」已于 2026-09-20 并入 **T91「待办清单」**；T91 于 2026-09-21 判定在实际账号数据下不可用，整体取消并删除（界面、装配与「向 Bungie 读取任务 / 赏金」的链路一并移除），四个编号冻结保留作追溯，不单独占行。
- T81「首页掉落池条目能跳进待办里对应的行动行」随 T91 于 2026-09-21 一并取消，编号冻结保留作追溯，不单独占行。
- T92「新建配装完整工作流」已于 2026-09-20 并入 **T58**（含原型、已确认结论、未定项和切分表），编号冻结保留作追溯，不单独占行。
- 子职业口径（2026-09-20 定）：**不把星象、碎片、超能、技能写回游戏。** 玩家在游戏里选好子职业，应用只读取当前配置再参与配装，并用「配装假定的配置 = 角色当前配置」这道校验闸门代替写回。依据：DIM 能写，但 issue #10344 / #8750 记录无谓改动会让玩家丢掉全部超能/技能能量；参考项目 d2-armor-solver 也只写五个护甲槽和护甲模组、绝不碰子职业插槽，并要求碎片属性与角色当前配置精确一致才允许直装。写回如果将来要做，单独立项，不并入 T58。
- 推荐来源口径（T56 定，2026-09-16）：**愿望单文本只是数据来源的一种格式，不是面向用户的概念**；判定、标签、文案一律从数据来源取，禁止硬编码。
- UI 只维护 `packages/ui` 的共享产品实现；Web 用于快速预览，Desktop 用于真实功能验收。
- 自动验证策略见 `AGENTS.md`：默认不跑本地测试 / 构建 / 打包，只有用户明确要求时才执行。
