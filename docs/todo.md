# 当前待办

> 更新时间：2026-09-18
> 本文件只保留**尚未关闭的任务**：一行说清「还要做什么、等谁」。
> 范围、验收标准与自验记录一律在对应 backlog；已完成的 Bug 与任务直接从本表删除、不留归档行，追溯用 Git 历史与 backlog。

## 当前任务

| 编号 | 优先级 | 状态 | 任务 | Backlog | 下一步 |
|---|---|---|---|---|---|
| T1 | P1 | 📝 候选方案，待排期 | 全应用英文国际化补齐 | [任务说明](work/backlog/T1-english-internationalization.md) | 先页面标题与共享弹窗，再仓库、装备详情、配装、AI 助手 |
| T23 | P2 | ⏸️ 等待官方书面授权 | D2Checkpoint 进度机器人集成 | [执行 backlog](work/backlog/T23-d2checkpoint-integration.md) | 2026-09-08 已发授权申请；收到回复前不进入开发 |
| T43 | P1 | 🟠 待真实账号验收 | 任务待处理中心 | [任务说明](work/backlog/T43-pursuit-attention-center.md) | 三角色任务、赏金、角色目标与赛季挑战的数量、进度、追踪、到期状态 |
| T44 | P1 | 🟠 待真实账号验收 | 光等提升路线 | [任务说明](work/backlog/T44-character-progression-center.md) | 周挑战奖励按 `displayRewards` 读取；核对三角色「本周可做 / 无可领取」 |
| T45 | P1 | 🟠 待真实账号验收 | 本周刷取清单 | [任务说明](work/backlog/T45-weekly-farming-checklist.md) | 核对轮换活动、装备来源、T20 推荐与四类图样状态 |
| T56 | P1 | 🟢 代码已完成、两组判据全绿；剩余 Bug #80–#87 人工复验 | 仓库推荐：统一模型与来源去分叉 | [任务说明](work/backlog/T56-unified-recommendation-model.md) | ① **人工推荐表格要重新导入一次**（DIM 文本不用）：切换后旧表示已不再读写，不重导就看不到那份来源的内容——这是既定代价，不是缺陷；② 重启桌面端点一遍七个子项：排序入口、位置筛选、筛分区顺序、框架下拉、槽位图标、来源页混排与对齐、验收报告归入诊断（其中「验收报告」当时标记为「有实际需要时再排期」，属主动押后、未做完） |
| T58 | P1 | 🟠 待真实账号验收 | 游戏内配装完整子职业配置 | [任务说明](work/backlog/T58-ingame-loadout-subclass-fidelity.md) | 按超能/技能、星象、碎片展示并保留原始 socket 映射 |
| T62 | P2 | 📝 待拍板（A / B / C 三选一） | 护甲详情目标来源去分叉 | [任务说明](work/backlog/T62-armor-detail-target-source-defork.md) | T56 同构残留：三页签里 `配装与攻略` / `社区来源` 恒空，来源名 `"我的推荐"` 是硬编码且与 personal knowledge 撞名。处置待定：**A** 纯去分叉（建议）/ **B** 对齐武器侧再拆区块 / **C** 只删死页签 |
| T68 | P1 | 🟢 代码已改并自验通过，待用户复验 | 本地文件导入的预览与起名搬进弹框 | [任务说明](work/backlog/T68-file-import-naming-in-dialog.md) | 四条导入路的确认卡一律在弹框内；「导入文本文件」先开框、框里再选文件 |
| T69 | P1 | 🟢 代码已改并自验通过，待用户复验 | Perk 条目统一样式（本件 Roll / 固有能力 / 完整掉落池 对齐推荐区那套） | [任务说明](work/backlog/T69-perk-cell-style-unification.md) | 一个条目组件两处共用（`WeaponPerkEntry`）；换 Perk 交互未动（之后单独重构）；`unresolved_in_definition_pool` 仍无落点，单列待办 |
| T70 | P1 | 🟢 代码已改并自验通过，待用户复验 | Perk 卡片三处收尾（首屏固有能力、占位几何、当前已选的辨识度） | [任务说明](work/backlog/T70-perk-cell-followups.md) | 首屏固有能力改为按需读定义（先骨架、读完没有才空态），不再出现终态的「未返回固有能力」；骨架/空态复用同一条目盒子（几何一处定义，实测 50 高、顶边对齐）；本件 Roll 的「当前已选」恢复蓝色选中语义（选中底 + 选中边框 + 左缘 3px 条 + 蓝点），推荐对照区维持蓝点 + 文字。实窗复验时报的「打开白屏 / 一直在闪」已修：补定义改走后台 `loadSelectedItemDefinition`（不读完整 Roll、不置详情加载态、失败不重试），完整 Roll 仍只在点「查看完整掉落池」时读。**复验只需在 dev 窗口里刷新**（改动全在 `packages/ui/src` 与 `packages/desktop/src/renderer`，Vite 别名直读源码、热更；`packages/ui/dist` 只在测试与打包时被消费；2026-09-18 更正原来的「复验前需重新构建」）。实窗 `light / dark × 1280 / 980 / 760` 待用户复验 |
| T71 | P1 | 🟢 代码已改并自验通过，待用户复验 | 武器详情三区重排（推荐 Roll → 本件 Roll → 完整掉落池） | [任务说明](work/backlog/T71-weapon-detail-region-order.md) | 章节顺序改为 推荐 Roll → 当前配置 → 完整掉落池 → 属性与获取 → 升级与锻造；完整掉落池从配置章节里拆出来，独立成第三个区域（自己的章节盒子与分隔线），三区共用同一条章节盒模型；首屏定位与懒挂载集合跟着第一节走，导航顺序同步。**这一条改掉了 `equipment-details.md` 原有的「当前配置在前」与首屏挂载口径**，也把推荐证据的读取时机从「滚到附近」提前到「打开详情」。完整 Roll 仍只在点「查看完整掉落池」时读。**复验只需在 dev 窗口里刷新**（改动全在 `packages/ui/src` 与 `packages/desktop/src/renderer`，Vite 别名直读源码、热更；`packages/ui/dist` 只在测试与打包时被消费；2026-09-18 更正原来的「复验前需重新构建」） |
| T72 | P1 | ✅ 代码已改完，待用户验收（2026-09-17 拍板 A+B+C） | 推荐对照区排布：卡片宽度与两列对齐 | [分析说明](work/backlog/T72-perk-comparison-layout.md) | 实测（真组件 + 无头 Chromium）：卡片宽度曾是「这一行有几张」的函数，同窗口并存 156.7 / 220 两档；1512 下同一行两侧首张卡最高差 28.5px。**已改完**：等宽列网格 + 两侧顶对齐、栏头合并成来源卡顶部一条并吸附、状态词统一；守卫 `architecture-maintenance.test.ts:330-342` 改成钉列模板 + 条目不设宽度。**改完后的自验**（`.local-data/tmp/t72-perk-layout/verify-product.mjs`，读仓库当前样式表，8 档宽度 430 项全过）：同半卡片等宽、两半列距相等且列线 = 0+k×列距、首张卡 Δy=0.0、栏头与槽位行 Δx=0.0、栏头吸附 Δ=0.0 且出路让位、≤760 两半堆叠。**未跑**仓库的测试 / 类型检查 / 构建（按约定留给用户） |
| T73 | P1 | ✅ 代码已改完（含 09-18「无法切换」修复），待用户验收（2026-09-17 拍板 m2 + 换 Perk 进推荐区） | Perk「当前启用」辨识度 + 换 Perk 入口 | [分析说明](work/backlog/T73-perk-active-signal-and-swap-entry.md) | 实测「未命中 + 当前启用」那张卡：旧标记可见部分只有 **6px**、对卡片底 **3.2:1**，绿勾 16px / **7.8:1**。**已改完**：m2（16px 空心蓝环 + 「当前」文字改蓝加粗，**实测 8.92:1**，作用域只在推荐对照区，T70 的本件 Roll 不动）；推荐区本件拥有条目在浮层里「选择 / 取消选择」，与本件 Roll 同一批 `pendingPerks`、同一处写面板；写面板上提到详情正文级并吸附在正文底部。实施时发现并修掉一个真 bug：浮层整体 `pointer-events: none`，新加的按钮点不着（守卫已钉）；顺带把配置列的「当前已选」并入同一套词表的「当前启用」。**2026-09-18 实窗复验报「无法切换」已修**：打开详情时 `sockets` 是 `buildPreviewSocketsFromWeaponRoll` 用快照搭的占位值（`is_enabled`/`can_insert` 一律 `false`，语义是「快照不知道」），而 `can_apply` 把它当插槽事实读，于是完整 Roll 读到之前本件 Roll 的格子、推荐区浮层里的「选择」、写面板三处一起哑掉；现在按 `detail_loaded.instance` 分开判定（核实过才用游戏返回的硬条件，未核实按快照判「拥有且不是当前这项」），并把「已经装着的那一项」从可切换项里摘出去——判据只在模型里写一次；UI 侧那个「能不能远程换 Perk」的判定原来在三处各写了一遍（其中推荐区那一遍少了「非固定配置」这条），收成一个函数。判据固化在 `weapon-detail-view.test.ts` 两条用例。**复验只需在 dev 窗口里刷新**（改动全在 `packages/ui/src` 与 `packages/desktop/src/renderer`，Vite 别名直读源码、热更；2026-09-18 更正原来的「复验前需重新构建」） |
| T74 | P1 | 📝 待拍板（A / B 二选一） | 首页轮换活动「掉落池」的覆盖与数据来源 | [分析说明](work/backlog/T74-rotation-loot-pool-coverage.md) | 数据集只录了 2026-09-10 当周轮换的 4 个活动（门徒誓约 / 救赎的边缘 / 贪婪之握 / 战争领主的废墟），9-16 重置换走后四张卡全空——接口只返回「突袭装备 / 强力装备」占位，UI 的武器过滤再把占位滤掉。已实测 Manifest Collectible `sourceString → sourceHash` 可推导全部约 30 个突袭 / 地牢的武器清单（国王的陨落 7、最后一愿 8、二象性 7、晚星之主 4+破冰者），并复现了现有 4 条 sourceHash 全中。**A**：生成脚本把数据集扩到全量（约 300–400 行，不碰索引 / worker / T45 契约，建议先行）；**B**：运行时构建索引（约 600–900 行、10–12 个文件，且 `pattern_record_hash` 推不出、双语匹配要另测）。名称歧义（`利维坦` 5 源、`预言` 2 源）决定两者都仍需一层人工覆盖表 |
| T75 | P1 | 📝 诊断已完成，候选修法待拍板（A / B / C） | 启动黑屏：窗口先显示 + 页面底色在 shell 之外仍是深色 | [分析说明](work/backlog/T75-startup-black-flash.md) | 机制三段：`main.ts:54` 窗口构造即显示 → `:59` 不透明铺底 + `titleBarStyle: hidden`（无标题栏，整窗一块纯色）→ `:69/:74` 才开始加载渲染进程。**黑是页面画的，不是 `backgroundColor`**：`02-elements.css:3` 的 `body` 底 `--page` 在 `:root` 是 `#070b10`，而亮色配色只挂 `.app-shell[data-color-mode="light"]`，所以亮色主题下启动画面也是黑的；`html[data-theme="light"]`（`00-tokens.css:210` / `03-surface-contract.css:24`）是**死选择器**，全仓无人设置。实测（无头 Chromium 打运行中的 Vite，逐帧采样）：`89ms → 418ms` ≈ 330ms 纯黑，`App.tsx` 的「正在启动 / 加载中」也打在黑底上。`main.ts:96` 的 `activate` 使 macOS 关窗后重开每次都闪。**A**：`show:false` + `ready-to-show`；**B**：真的写 `data-theme`（或让回退屏落在主题宿主内）；**C**：`body` 底色按 `color_mode` 兜底。真实会话时长未测（测它要占用正在使用的数据目录） |
| T77 | P0 | 🟠 代码已改完（含 09-18 实窗复验后的 T78、T80 两轮修复）；未跑本地自动化，待重新构建后实窗复验 | 换 Perk：写入受理即确认，写后读回降级为后台静默校对 | [分析说明](work/backlog/T77-perk-write-acceptance.md) | T76 的留痕把「猜」变成了「看」：6 次读回读到的是**同一个**旧值、每条都带 `affinity_cookie: true`；12:15:09 探测到新值、12:15:22 两端点逐槽一致。真因是**服务端传播延迟**（实测 26 秒仍读到旧值、2 分 45 秒读到新值，**收敛上界没测出来**），而当时是阻塞式门闸，拿 14.75 秒去判它，把正常传播定性成了失败。**已改完**：core 新增 `applyAcceptedSocketPlugs`，同源更新插槽**四份**视图（`sockets[].selected_plug` / 各槽 `reusable_plugs[].selected` / 派生的 `socket_plugs` / `weapon_roll` 当前项与指纹）；`runItemWriteAction` 加 `acceptedSocketChanges`，受理即落地并立即返回（照 DIM 的 `refreshItemAfterAWA`，与仓库既有的 `expectedAccountPatch` 同一条路子）；读回改成 fire-and-forget 的 probe 模式（不合并、不置加载态、不回退），台阶 `[0, 5, 15, 30, 60, 90]` 秒；删掉 `refresh-error` 面板态。**T78（实窗复验后报的两个 bug）**：① 受理状态原来只活在弹框里，关掉再打开就看见旧 Perk（用户原话「不知道到底切换成功没有」）→ 落点改成模块级 store（`shared/stores/acceptedSocketPlugs.ts`，5 分钟窗口，读取出口叠加、探针走 `serverTruth` 不叠加、窗口耗尽后的账号同步认账）；② 漏改 `reusable_plugs[].selected`，界面上「两个都变启用了」→ 补齐第四份视图；③ 面板不再替服务器宣布结果（删 `onSettled` 与「已与服务器确认」，`success` 拆成 `submitted` / `reloaded`）。顺带修掉布局上下跳（每轮读回翻 `detail_loading.instance`，`weapon-detail-config-loading-note` 反复挂载/卸载）与两处文案 / 状态 bug。**未跑**本地测试 / 类型检查 / 构建。复验步骤见 backlog §5.2 |
| T79 | P1 | 📝 待排期 | 插槽状态收敛成一份派生结果（现在是四份并行表示） | [任务说明](work/backlog/T79-socket-state-single-source.md) | 换 Perk 一次要同时改 `sockets[].selected_plug`、各槽 `reusable_plugs[].selected`、派生的 `socket_plugs[]`、`weapon_roll` 的当前项/`owned_plugs[].selected`/指纹 —— T78 的「两个都变启用了」就是漏掉其中一份造成的。写路径靠手工逐份改、读方各挑一份读，且**没有一处能判出谁对**。目标是当前配置只留一处真源（倾向 `sockets`），`socket_plugs` 与 `weapon_roll` 的当前项都改成派生，让「漏一份」在结构上不可能，而不是再加一条守卫去钉。属重构、不改行为，需先清点读侧消费点并准备行为等价判据；排在 T77 实窗复验通过、行为确认稳定之后 |
| T80 | P0 | 🟠 代码已改完；未跑本地自动化，待重启 dev 脚本后实窗复验 | 换 Perk 的**执行路径**：主进程退出裁判席、写响应体当旁证、一次只飞一个写 | [分析说明](work/backlog/T80-perk-write-execution-path.md) | 09-18 实窗复验又报了一次「武器配置未更新 / 需要处理」，根因在 T77 **没修到的另一条路**：换 Perk 有两条写入路径，报错的是 `desktop/src/main/ipc/actions.ts` 里那个自带写后读回裁判的实现（`hasAppliedSocketPlug`，预算 750ms + 2000ms = 2.75 秒，实测传播 3 分 32 秒，**走到就必误报**）。触发它的是**重复提交**：`0aef101d` 在 `04adaf08` 被受理后 12 秒又点了「应用」，必然吃 `ErrorCode 1679`（这件装备还有变更在处理中）——**分钟级状态**，而应用既没有重入闸、又把它当失败上报。**已改完**：① `insertSocketPlug` 不再丢写响应体（`readSocketPlugWriteOutcome`，读不懂就回落 `null`，绝不抛）；② 主进程删掉裁判席，重发用尽即如实回 `deferred_socket_indexes`，留痕新增 `submit-deferred` 并补 `socket_index` / `plug_hash`；③ 渲染层加 `deferred` 中性档（不落地、不报错、保留选择）与重入闸 `if (props.isRunningItemAction) return;`（DIM 同款）；④ 核对窗口从 ≈3.3 分钟拉到 ≈12.5 分钟，终态措辞改成「窗口内未观察到，不代表写入失败」。**与 DIM 有意分歧**：不照搬「响应体即权威」——没有证据说明写响应体比读路径新，照搬会把用户刚选的 Perk 弹回旧的；分工改为**显示按意图、响应体负责暴露「受理但被静默拒绝」**。同时修正了 T77 三处已证伪的判断（窗口长度、`reflected: true` 这条验收判据、「不采用写响应体」）。**T80-5（同轮实窗带出）**：认识的 Bungie 游戏状态拒绝不再透传英文原文——1634（角色在活动中必须回轨道）换成中文动作句，并标成不可重试、归入 `validation`；不认识的码仍透传原文，宁可给原文也不编不准的翻译。复验时顺带确认写面板不再出现 `Bungie request failed: HTTP 500 (ErrorCode 1634: ...)` 这种英文行 |

## 说明

- `T数字` 与 `Bug #数字` 创建后保持全局唯一且不再改号；本表按任务编号升序排列，优先级和状态以对应列为准，不通过表格位置表达。
- 已完成的 Bug 与任务直接从本表删除，不保留归档行；追溯使用 Git 历史与对应 backlog。
- T41「邮政官安全处理」已取消，编号冻结且不再复用。
- T55 / T57 / T59 / T60 / T61 与 Bug #80–#87 已并入 T56，编号冻结保留作追溯，不单独占行。
- T76 的亲和性机制保留并生效，但它对「换 Perk 后读回旧配置」这个 bug 的结论已被证伪（2026-09-18 的留痕推翻了它）；该 bug 的修复改由 **T77** 承接，T76 编号冻结保留作追溯，不单独占行。
- **T78 是 T77 的实窗复验修复轮**（受理状态跨弹框存活、补上第四份视图、面板去断言），说明并入 T77 那一行与同一份 backlog，不单独占行；编号冻结保留作追溯（代码注释里引的就是它）。
- T45 / T44 / T46 / T43 只**消费**推荐结果，不并入 T56。
- T56 口径（2026-09-16 定）：**DIM 愿望单只是数据来源的一种格式，不是面向用户的概念**；判定、标签、文案一律从数据来源取，禁止硬编码。
- UI 只维护 `packages/ui` 的共享产品实现；Web 用于快速预览，Desktop 用于真实功能验收。
- 自动验证策略见 `AGENTS.md`：默认不跑本地测试 / 构建 / 打包，只有用户明确要求时才执行。
