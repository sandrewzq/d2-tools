# 当前待办

> 更新时间：2026-08-28
> 本文件只保留当前任务、状态和下一步。详细范围与验收标准由对应 backlog 维护，阶段过程使用 Git 历史追溯。

## 健康度

| 检查项 | 状态 | 备注 |
|---|---|---|
| GitHub CI | ⏳ push 后异步执行 | 执行 frozen install、`pnpm test`、共享 Shell 视觉契约、UI 合同静态门禁和构建后的 `pnpm typecheck:ci`；普通 push 不等待结果 |
| Release 门禁 | ⏳ 发版时执行 | 必须通过对应平台的 Git Release 入口完成本地门禁、GitHub Actions、安装包和 GitHub Release |
| Agent 自动验证 | ⛔ 默认禁用 | 只有用户明确要求本地测试、构建或打包时才执行 |

## 当前任务

| 编号 | 优先级 | 状态 | 任务 | Backlog | 下一步 |
|---|---|---|---|---|---|
| T14 | P1 | ✅ 开发完成，待整体验收 | 应用配装创建流程与逐部位护甲自动配甲 | [任务说明](work/backlog/T14-armor-planner-slot-aware-mods.md) | 游戏内/应用配装已分离；方案库、只读详情、2–3 对比、固定槽位编辑、逐槽位选择、自动配甲、穿戴核对、保存到游戏内槽位、freshness、离开保护和响应式页面栈均已落地；游戏内配装现已按可确认 bucket 分组并显示 Socket、未定位和辅助操作边界，下一步使用真实账号进行跨端整体验收 |
| T15 | P1 | 🟡 补充改造已落地，待验收 | 本地优先账号数据与统一缓存层 | [任务说明](work/backlog/T15-local-first-account-data-cache.md) | 写操作双层状态、`pending_sync` patch、旧快照守卫、主进程轻量确认和事件驱动 UI 已落地；下一步使用真实账号验收单件、多件、仓库来源、部分失败和长时间 Profile 延迟 |

## Bug

| 编号 | 优先级 | 状态 | 问题 | 修复范围 | 下一步 |
|---|---|---|---|---|---|
| Bug #1 账号 | P2 | ✅ 已修复 | 账号页材料清单错误复用活动表格三列布局，导致图标放大、名称错位和元信息换行 | 账号材料专用三列连续行布局 | 整体验收时使用真实材料数据确认长名称、数量和窄屏显示 |
| Bug #2 仓库 | P2 | 🟡 待验收 | 同名整理读取完整 Roll 后从当前启用视图自动跳变，导致比较矩阵高度和差异语义突然变化 | 默认稳定显示当前启用；完整 Roll 显式切换；加载与失败状态固定展示 | 使用真实账号核对首屏、加载完成、失败重试和手动切换完整 Roll |
| Bug #3 构建 | P1 | ✅ 已修复 | T15 接入后 services / desktop TypeScript 构建失败，阻断开发启动脚本 | 修正 `DataResource` 泛型推断、快照提交守卫、实例详情清理 Promise 类型和账号 Session 状态 Promise 类型 | services build、desktop typecheck、desktop build 已通过 |
| Bug #4 账号 | P2 | ✅ 已修复 | 光等详情同时展示“八槽最高”和真实可执行方案，理论值不能同时装备且与一键操作结果不一致 | 删除八槽理论值与双 Tab；角色摘要改为账号可装备最高；浮层只展示本角色与仓库范围内的一键装备方案，并保留跨角色差距提示 | 整体验收时使用真实账号确认账号最高、一键可达、跨角色来源提示和窄屏布局 |
| Bug #5 账号 | P1 | ✅ 已修复 | 装备详情只刷新实例属性便提前提示成功，弹窗位置与账号页使用旧来源，写后完整刷新还可能用 Bungie 旧快照覆盖局部更新 | 单件写操作先更新中央账号 Store；详情位置从账号数据实时派生；后台快照确认后统一提交成功，失败恢复服务器数据并显示类型化提示 | 使用真实账号验收装备、转移、取回、锁定的提交中、确认中、成功、同步超时和明确失败状态 |
| Bug #6 配装 | P1 | ✅ 已修复 | 切换角色后游戏内配装把 Bungie 的 `"0"` 和未设置 Plug Hash 当成真实记录，显示“物品 0”“未知槽位”和重复 Plug | core 归一化过滤无效实例与占位 Plug，并按有效实例数统计槽位 | 使用真实账号切换多个角色，确认空槽不再出现在已保存列表，部分槽位只显示真实装备和有效模组 |
| Bug #7 账号 | P1 | 🟡 已修复，待真实账号验收 | 一键装备最高光等即使只需装备 1 件仍长时间停在前台确认；实测 Bungie `EquipItems` 约 0.8 秒即返回成功，但旧实现又执行 8 次完整账号快照读取并等待约 43 秒 | 已删除前台完整账号预刷新、无限轮询和固定 `15s` 等待；单件改用 `EquipItem`，成功后立即提交受保护的 `pending_sync` patch并解除按钮，主进程只读取 `CharacterEquipment` 后台确认 | 重新复现单件最高光等，确认写接口返回后页面立即更新且不持续刷新；继续验收同步延迟、部分成功、后台任务和最终静默合并 |

## 说明

- UI 只维护 `packages/ui` 的共享产品实现；Markdown 合同记录稳定约束，Web 用于快速预览，Desktop 用于真实功能验收。
- UI 合同静态门禁覆盖稳定标记、表面枚举、排版、层级、菜单主题色和选中态方向线，避免菜单实现重新取得共享视觉职责。
- 新增 Bug 必须使用全局唯一编号并在本文件中登记。
