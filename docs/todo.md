# 当前待办

> 更新时间：2026-08-08
> 本文件只保留当前任务、状态和下一步。详细范围与验收标准由对应 backlog 维护，阶段过程使用 Git 历史追溯。

## 健康度

| 检查项 | 状态 | 备注 |
|---|---|---|
| GitHub CI | ⏳ push 后异步执行 | 执行 frozen install、`pnpm test`、共享 Shell 视觉契约、UI 合同静态门禁和 `pnpm typecheck`；普通 push 不等待结果 |
| Release 门禁 | ⏳ 发版时执行 | 必须通过 `tools\git-auto-release.cmd` 完成本地门禁、GitHub Actions、安装包和 GitHub Release |
| Agent 自动验证 | ⛔ 默认禁用 | 只有用户明确要求本地测试、构建或打包时才执行 |

## 当前任务

| 编号 | 优先级 | 状态 | 任务 | Backlog | 下一步 |
|---|---|---|---|---|---|
| T3 | P1 | 🟡 待验收 | 商人结构、覆盖与官方获取来源 | [任务说明](work/backlog/T3-vendors-and-drop-sources.md) | 验收真实地点覆盖、跨重置边界、Offer 时效、去重和机灵模组 A/B；确认声望位于首个内容章节、吸顶导航可定位全部章节，非装备服务与任务明确提示游戏内操作 |
| T4 | P1 | 🟡 待验收 | 配装工作台按全局视觉合同重排 | — | 验收角色平铺切换、工作台唯一外框、游戏内装备信息、本地方案编辑与窄屏纵向布局 |
| T5 | P1 | 🟡 待验收 | 仓库侧边筛选、十列紧凑装备网格与同名整理矩阵 | — | 使用真实账号数据验收 `44px` 图标、DIM 同位的官方阶级覆盖图、武器彩色属性图标、槽位、弹药、整理/锁定/配装状态、护甲差异摘要、响应式列数、保护信号 AND 筛选、同名参考差异、暂存应用和保护冲突确认；确认绿色锁定标记位于卡片底部状态行且不遮挡装备图标 |
| T6 | P1 | 🟡 待验收 | 资料库根据 Perk 反查装备 | — | 重建并重启 Desktop 后搜索“爆炸箭头”，确认普通与强化合并为一条 Perk 家族结果，分别显示变体效果和关联数量，总数按装备版本并集计算；展开后按页加载真实 Manifest 关系，检查官方图标与版本水印、名称、稀有度、类型、框架、弹药、伤害属性、发布版本、普通/可强化标记、历史来源和详情入口；天堂暴政等同名不同发布版本必须独立显示，同一发布版本的技术重复定义不得重复；无关联、加载、失败重试、旧运行时阻断和加载更多状态均明确可用 |
| T7 | P1 | 🟡 待验收 | AI 工作台、原生能力与攻略配装工作流 | [任务说明](work/backlog/T7-guides-library-and-derived-targets.md) | Armor 四模式、攻略聚合约束、稳定 `result_id`、执行前失效门禁、全链路日志、真实实例方案 DIM 导出、七个 AI 只读能力（含本地攻略搜索）、不可变会话快照、脱敏能力审计、三类 AI 成果交接、独立攻略库，以及版本化独立装备目标库已接入。攻略确认可派生正式武器目标、稳定 `armor_constraint_draft` 和账号绑定的 `loadout_candidates`；配装页可复选确定命中与替代实例形成未保存草稿，Armor acquisition 缺口可保存为带结果引用的基础属性目标。版本化 `GuideDerivedRelation` 已记录攻略、目标、Armor 结果、候选成果和两类攻略来源本地方案关系；攻略页可精确定位装备目标或本地方案，目标行和攻略来源方案也可经有效关系返回原攻略，Armor 缺口可跳到对应结果/候选引用并明确要求重算。下一步使用真实账号和 Desktop 实窗验收攻略确认、候选交接、目标追溯、Armor 重算提示、方案保存与删除关系清理；`guide_extraction_patch` 作为后续可选增强搁置，不作为本轮验收前置条件 |

## Bug 列表

| 编号 | 状态 | 问题 | 范围与处理 | 验证 |
|---|---|---|---|---|
| Bug #15（武器版本与来源） | 🟡 修复待验证 | 资料库武器版本筛选会把同一官方定义误作多个版本；获取来源混用技术词，并把跨角色重复返回的聚焦条目显示多次；“武器”等子商人名称缺少业务归属，购买资格未知时还会同时显示“当前可获取”和“当前无法购买” | Core 和 SQLite Catalog 统一选择官方主定义；来源区分历史与当前状态；角色范围的同一商人条目跨角色合并，聚焦子商人转换为“先锋武器聚焦”等完整名称；当前入口、明确可购买和条件未满足分别展示，不暴露内部库存序号 | 重启 Desktop 后检查“庆典飞行”的版本与历史来源；打开“暗夜魅影”，确认只显示一条“先锋武器聚焦”，价格保留但不显示库存序号，购买资格未知时显示“当前有获取入口”，不再同时出现“当前可获取”和“当前无法购买”；再检查真实商人可购买及条件失败条目 |
| Bug #16（武器详情 Roll 列） | 🟡 修复待验证 | 武器配置区固定为五列，固有能力加五个 Roll 插槽时末列换行；插槽只按枪管、弹匣和特性粗分类，特殊武器列名错误，资料库预览截断高索引 Socket，实例 Socket 不完整时还可能丢失当前选择 | 资料库预览和详情配置宽屏使用六轨，完整池与实例配置共用真实 Socket 顺序；按插槽类别显示柄芯、握把、弓弦、箭杆、偃月杆、枪托、核心升级等名称，并按定义池补齐可确认的实例当前选择 | 检查“暗夜魅影”常规五列；检查“英勇利刃”的柄芯、握把、Perk、核心升级和后置 Perk；再检查“玫瑰”“零号修订”、弓、偃月与普通刀剑，确认资料库预览和详情均无末列换行、错名或丢列 |
| Bug #17（装备后角色光等不刷新） | 🟡 修复待验证 | 一键装备最高光等或单件装备成功后，装备位置通过局部 patch 更新，但角色 `light` 仍保留写操作前的值；完整 patch 被误判为无需刷新账号摘要 | 成功的装备 patch 继续立即更新装备位置，同时强制刷新完整账号摘要，从 Bungie 重新读取角色光等；转移和锁定等完整 patch 继续只做局部更新 | 装备更高光等物品后等待账号刷新完成，确认角色摘要光等随 Bungie 返回值更新；再检查仓库锁定与普通转移不会额外触发完整刷新 |
| Bug #18（装备详情锁定状态） | 🟡 修复待验证 | 仓库卡片显示已锁定，但详情请求未读取 `ItemCommonData`，详情合并后把锁定状态覆盖为未知，界面又误显示为未锁定 | 详情请求补充 Bungie `307` 组件；缺失锁状态时保留列表已有值；状态仍未知时明确显示未知并禁用锁定操作 | 从仓库打开已锁定和未锁定装备，确认卡片、详情状态与锁定操作一致；再执行锁定和解锁各一次，确认刷新后状态正确 |
| Bug #19（仓库筛选切换卡顿） | 🟡 修复待验证 | 真实仓库筛选切换时同步重复计算未展示的全量统计；列表还会向最多 200 张装备卡传入每次新建的空选择集和空回调，导致 `React.memo` 失效并整批重渲染 | 仓库内容层按页面实际使用结果拆分筛选派生计算，移除切换时未使用的愿望单、目标和重复分区统计；空选择集与空回调改为稳定引用，使未变化卡片跳过渲染 | 使用真实约 485 件仓库连续切换稀有度、弹药和伤害属性，确认按钮响应与结果刷新无明显停顿；再检查筛选数量、槽位和框架选项保持正确 |
| Bug #20（跨页面装备图标重复加载） | 🟡 修复待验证 | Desktop 切换一级菜单时会卸载非活动页面，各菜单直接创建新的图片节点，装备、Perk、商人物品和伤害属性图标即使命中 HTTP 缓存也会重新排队解码；仓库主图与覆盖图加载优先级不一致，还会短暂只显示阶级覆盖图 | `packages/ui` 新增统一游戏资源图片组件，有限保留已成功加载的图片节点引用并复用解码缓存；仓库首屏、账号装备、配装和详情使用即时加载，长列表继续懒加载，图片失败统一回退原有占位内容 | 在 Desktop 依次打开仓库、账号、配装、资料库、商人和装备详情后反复切换，确认已显示图标不再成批空白重绘；检查仓库首屏主图与阶级覆盖图同步出现，并观察长列表滚动与进程内存没有明显回退 |
| Bug #21（仓库护甲套装筛选缺失） | 🟡 修复待验证 | 初版套装筛选错误地只根据账号已持有装备生成选项；账号没有返回套装物品时控件会被禁用，未持有套装也永远不会出现在目录中 | 账号定义加载器继续按 `equipableItemSetHash` 补充物品套装标识；资料库 worker 从完整 `DestinyEquipableItemSetDefinition` 生成轻量套装目录，仓库用目录提供全部选项、用账号物品计算持有数量，并区分目录加载中、可用和失败状态 | 使用真实账号进入仓库护甲模式，确认完整套装目录可选且实际持有数量正确；选择持有套装后只保留对应物品，选择持有数为 0 的套装后显示空结果；无套装字段的护甲不误归类，资料库不可用时显示明确提示，重置筛选后恢复全部护甲 |
| Bug #22（全应用战斗标签对比度） | 🟡 修复待验证 | 武器摘要、仓库筛选和列表分别维护弹药与伤害图标，主要弹药、动能与勇士克制还会回退到通用浅灰或纯文字样式；详情还会优先显示来源不稳定的弹药图片，导致与筛选符号不一致 | `packages/ui` 新增共享战斗图标组件，集中管理三种弹药、六种伤害属性与三种勇士克制；伤害和反勇士使用 Manifest 官方图标，弹药在仓库筛选、装备单元、资料库结果和武器详情中固定使用一、二、三格共享符号，不再读取详情的非标准弹药图片 | 分别在亮色和暗色主题检查仓库筛选与装备单元、资料库结果和武器详情；覆盖主要、特殊、重型弹药，六种伤害属性及三种反勇士，确认图标清晰、样式一致、未选中与选中状态可区分 |
| Bug #23（装备详情两阶段等待） | 🟡 修复待验证 | 打开账号武器或护甲时先渲染摘要组成的半成品详情，再等待本地定义和 Bungie 实例详情同时完成；关闭弹窗还会清空 renderer 详情缓存，导致重复打开持续出现约两秒的中间页；护甲还会误启动武器专用推荐请求 | 共享详情首帧直接渲染账号快照，定义与实例详情并行分段回填，正文不再等待任一路请求后才出现；总忙碌状态覆盖两条关键请求，区域内使用独立状态显示读取进度；关闭弹窗只取消过期请求并保留有界缓存，账号或 Manifest 范围变化时才清空；辅助请求延后启动，并按装备类型仅为武器读取社区 Perk、个人武器知识和同名版本 | 从仓库、账号和资料库分别打开武器与护甲详情，确认正文首帧直接出现且不再停留中间页；关闭后重复打开同一装备应即时复用详情；实例属性、Perk、锁定、来源和武器推荐随后正确回填，连续切换装备不会串数据 |
| Bug #24（同图样武器版本被合并） | 🟡 修复待验证 | 资料库搜索已从 SQLite 索引展开同名武器版本，但 Core 二次去重只按 `weaponPatternHash` 判断身份；旧版与复刻版共享图样 Hash 时会被误合并，修复后列表又没有直接显示版本，仍难以快速区分实体 | 武器图样去重键补充 Manifest 发布 trait；同一发布版本的技术重复定义继续合并，不同发布版本分别保留；资料库结果名称旁直接显示玩家可识别的发布摘要 | 重启 Desktop 后搜索“天堂暴政”，确认 `2721249463` 与 `3388655311` 两个正式武器版本均显示，并分别标出“遗落之族”和“深渊赛季”发布摘要；图样、Dummy 和 Mod 条目不进入结果；再抽查一个同发布版本的重复定义，确认未产生重复结果 |
| Bug #25（装备详情版本与当前对象不统一） | 🟡 修复待验证 | 武器和护甲分别维护身份区版本与当前对象样式；发布版本在左右两侧重复，武器版本省略年份，护甲把版本事实染成成功绿；“当前查看”和“对象”内容重复，武器还在头部重复显示随机 Roll 配置 | 新增武器与护甲共用的上下文账本，只保留入口、当前查看、位置和条件化版本字段；资料库多版本继续使用 Hash 下拉切换，其他对象显示静态发布版本；统一完整年份/赛季/名称、蓝色当前对象状态和版本水印，Manifest 技术版本留在折叠追溯区 | 分别从资料库、仓库、账号和商人打开武器与护甲；确认左侧无重复版本，右侧结构与当前查看样式一致，武器显示完整年份且不再出现头部配置；资料库多版本可正常切换并更新图标、属性、Perk 与来源，单版本和账号实例不出现无效下拉 |
| Bug #26（CI 契约未同步） | 🟡 修复待验证 | Manifest 测试仍断言旧组件集合与抓取数量；UI 检查器漏收正式合同已有的 `workspace-frame`，仓库卡片覆盖层绕过语义 token；武器详情把可复用插件与普通已选插件合并后，仅靠属性存在性判断读取扩展字段，类型仍为 `unknown`，导致 CI 测试和 typecheck 持续失败 | 同步 Manifest 契约与生命周期期望；补齐 UI 检查器表面枚举并让仓库覆盖层使用菜单语义层级和主题 token；武器详情增加明确的复用插件类型守卫，仅对可写插件读取插入状态 | 由 `0.0.15` Release 本地门禁和 GitHub CI 验证全部测试、类型检查、视觉合同与发布流程 |
| Bug #27（一键最高光等虚假成功） | 🟡 修复待验证 | 最高光等按槽位独立选取，可能同时选择多件异域武器或护甲；批量装备又丢弃 Bungie 的逐件 `equipStatus`，把被忽略或被规则拒绝的物品全部记为成功；仓库转移失败的物品仍继续进入装备阶段；真实失败只显示数字状态码，无法直接判断角色位置或物品同步问题 | 按武器、护甲分别计算最多一件异域装备的最高总光等组合；保留并检查 Bungie 批量装备逐件结果，对刚转移但尚未同步的缺失结果和 `1623` 短暂重试；批量结果记录成功、失败实例 ID 与失败原因，后续只装备转移成功的仓库物品；`1671` 明确提示返回轨道、社交空间或退出游戏 | 使用包含多件高光等异域装备及仓库候选的真实角色执行一键装备，确认最终组合遵守异域限制、成功数量与游戏实际一致；在活动地点执行时确认直接显示位置限制提示；返回轨道后重试并确认可装备；模拟仓库空间不足、物品未同步或单件装备失败时，确认失败项不再显示为成功且原因可读 |
| Bug #28（铁旗状态与倒计时误判） | 🟡 修复待验证 | 首页把全局周更时间直接显示为铁旗倒计时，并把长期存在但 `canPurchase=false` 的铁旗聚焦子商人库存当成活动开放证据；角色活动没有铁旗时仍显示“正在开放” | 铁旗状态以角色活动和公共里程碑为准，聚焦库存只保留为库存信息；仅当铁旗公共里程碑提供 `startDate/endDate` 时显示距开放或距结束时间，否则明确显示时间待确认；升级首页缓存版本，并在已确认边界到达时重新读取活动 | 铁旗未开放时确认首页显示“当前未开放”且不再出现周更倒计时；只有聚焦子商人时不得显示开放；活动开放但无里程碑结束时间时显示“结束时间待确认”；存在官方里程碑边界时显示对应倒计时，并在边界后刷新状态 |
| Bug #29（语义色条边框反复出现） | 🟡 修复待验证 | 菜单和普通组件可直接声明粗左边线并覆盖共享表面合同，导致铁旗等普通对象无条件出现状态色边框；现有 UI 合同门禁没有检查边框所有权 | 共享 `Callout` 成为粗左侧语义色条的唯一所有者；首页反馈、装备详情、配装、设置和商人提示迁移到稳定标记；普通选中态改用 `1px` 内描边；静态门禁禁止其他 UI 样式声明大于 `1px` 的左侧或 `inline-start` 边线 | Review 各菜单提示、警告、失败和 AI 区块的色条语义；确认铁旗卡片及普通对象无粗左边框；后续 CI 由 `ui:contract:check` 拦截私有粗左边线 |
| Bug #30（仄库存缺少整体卡片边界） | 🟡 修复待验证 | 仄库存标题、倒计时、库存状态和来源散落在裸页面章节中，只有内部空态单独带框，与首页其他摘要模块不一致 | 仄库存改为单一 `summary-frame`，标题栏、库存内容和来源统一收进外框；内部摘要与空态移除重复外框，保留必要分隔线和 Offer 对象卡 | 在有库存、未开放、加载和失败状态下检查模块始终只有一圈首层外框；在 `1280 / 980 / 760px` 检查标题、倒计时和操作不溢出 |
| Bug #31（刷新动作权重不一致） | 🟡 修复待验证 | 页头和设置分别按页面临时指定刷新按钮颜色，账号刷新与重新授权同权，首页和商人在已有自动刷新时仍长期占用主操作，失败恢复又与页头或内部空态重复 | 按页面职责、人工频率、自动覆盖和阻断状态分配权重：账号与仓库正常刷新为 Primary，重新授权为 Secondary；首页与商人正常刷新为 Secondary，失败恢复临时升权；账号、仓库和首页已有局部主恢复入口时隐藏页头重复动作；进行中保持原标签并使用 `aria-busy` | 对照 Web 与 Desktop 检查账号、仓库、首页、商人和设置账号分区；覆盖正常、无数据、加载和失败状态，确认每个区域最多一个主按钮、恢复入口不重复且加载时按钮宽度不跳动 |
| Bug #32（商人后台同步误锁手动刷新） | 🟡 修复待验证 | 商人进入页自动读取、详情补全、仄边界更新和手动刷新共用 `refreshState`，页头按钮在任何后台请求期间都会被禁用，用户无法主动接管刷新 | 新增独立手动刷新状态；后台请求继续更新商人状态区但不锁页头按钮，手动请求期间才禁用；请求被角色、商人或后台刷新接管时立即释放旧手动状态，并由请求序号防止旧请求提前解锁新请求；有缓存的失败由页头刷新临时升权，完全无数据时由内容空态主按钮恢复 | 进入商人页并在后台读取期间点击页头刷新，确认请求可执行；手动刷新期间按钮只禁用一次；手动刷新中切换角色或商人后仍可再次刷新；切换商人触发详情补全时仍可点击；无数据失败时只显示内容空态主恢复按钮 |
| Bug #33（同名整理交互语义不清） | 🟡 修复待验证 | 同名页面把比较基准、差异查看、整理草稿和批量应用压在同一层；武器列只显示当前启用 Perk，紧凑账号快照会被误当成完整 Roll；Roll 单元又同时使用整格描边、Perk 条目边框和重复“额外 / 当前 / 完全不同”文字，明暗主题下层级拥挤且难以横向扫描 | 工作区按需读取实例完整 Roll，并以相同、部分重合、完全不同标记集合关系；比较矩阵改为左侧状态条和短标签表达差异，Perk 使用无框分隔行，当前或可切换状态固定在右侧，“仅此实例”并入名称行；Roll 列增加可读宽度并保留横向滚动；Perk 图标复用主题图标底板；护甲展示总值与六维，实例保护和推荐证据分层 | 使用包含共同未启用 Perk、双选 Perk、同名不同版本武器和多件护甲的真实账号验收；确认完整 Roll 只包含实例实际拥有项；明暗主题下检查名称、图标、当前状态和差异标签可快速扫描，没有整格套框或低对比文字；基准切换只改变差异显示，草稿应用和保护冲突确认保持可用 |
| Bug #34（账号装备卡片高度与信息层级） | 🟡 修复待验证 | 账号装备卡把“配装引用”和“正在打开详情”作为额外文本行追加，命中状态的武器卡会比同槽位候选更高；卡片还重复列标题中的“当前角色装备 / 当前角色背包候选”和行标题中的槽位名称，导致真正有用的光等、护甲总值与锁定状态被挤到省略号中 | `AccountItemView` 改为结构化核心事实和对象状态；武器与护甲卡固定为 `72px` 三行结构，名称、核心事实、对象状态各占一行；来源与槽位只由外层标题表达，武器展示类型、稀有度和光等，护甲展示稀有度、光等与总值，末行承载锁定、配装和打开状态 | 在账号页检查武器和护甲各槽位；确认卡片内不再重复来源与槽位，普通卡、配装引用、打开详情中、长名称和长事实文本高度一致，光等与护甲总值优先可见；在宽屏五列及 `1280 / 980 / 480px` 响应列数下确认不溢出、不拉长整行 |
| Bug #35（装备详情加载后布局跳变） | 🟡 修复待验证 | 详情首帧使用账号快照渲染三列属性行，定义详情返回后新增标准值和差异列；定义请求还会先于实例请求解除 `is_detail_loading`，版本字段从静态文本变为不同尺寸的下拉框，导致页面看似完成后继续重排 | 关键加载状态拆分为定义、实例和版本；账号实例属性区固定为当前值、轨道、标准与差异四列，比较区常态保持两行并将修正来源合并显示，待应用变化并入同列；版本文本和下拉框共用同一字段外壳；优先使用账号快照中的伤害与反勇士信息 | 从账号、仓库和同名整理首次打开未缓存武器详情，确认弹窗外壳、版本字段、属性列宽、进度条起点和正文位置不在加载完成后跳动；11 项属性保持紧凑密度且完整修正可通过悬浮查看；定义与实例请求都完成后才结束总忙碌状态 |
| Bug #36（Perk 搜索后黑屏） | 🟡 修复待验证 | 资料库 Renderer 已切换到新的 Perk 合同，但本地 `core/services/preload dist` 仍可能返回旧 `related_items` DTO；搜索结果进入页面后直接访问缺失的 `related_groups`，React 渲染异常导致整页黑屏，旧 preload 展开时也没有分页接口 | App 数据边界统一归一化新旧 Perk DTO，合并相同 Perk 定义并从旧关联摘要恢复数量和分类；筛选与 ViewModel 对缺失字段保持防御；runtime contract v2 不可用时阻断旧关联摘要并提示重新构建重启，不再调用不存在的分页方法 | 在未重建旧 dist 的混合状态下搜索任意 Perk，确认页面不黑屏，关联区只显示升级提示且不渲染旧摘要；完成构建并重启后搜索“爆炸箭头”，确认使用精确数量、分页关联装备和版本化摘要 |
| Bug #37（Perk 家族、图标与版本） | 🟡 修复待验证 | “爆炸箭头”的普通与强化 Sandbox Perk 被按描述拆成两行；新 Renderer 与旧 Desktop runtime 混跑时，关联装备 DTO 缺少图标、发布版本和变体信息，却仍被渲染成“版本待确认”的固定摘要 | Core 以 Inventory Plug 名称、图标和类别建立 Perk Family，普通/强化作为变体保留；SQLite 分页按 canonical 装备版本取并集并返回每行命中的变体；资料库增加 runtime contract v2 能力握手，旧合同直接阻断关联列表，不再展示伪完整摘要 | 重建并重启后搜索“爆炸箭头”，确认只显示一条结果，普通 41 件、强化 32 件和总计 41 件按当前 Manifest 数据展示；展开后确认旧版与深渊赛季版天堂暴政分行，只有支持强化的版本显示“可强化”；再用旧 dist 混跑确认只显示重启提示，不出现空图标和“版本待确认”摘要 |
| Bug #38（资料库 Tab 选中态） | 🟡 修复待验证 | 资料库“装备 / Perk 与框架”切换只使用接近的面板底色，难以快速判断当前 Tab；首轮局部强化又引入了与全局规范不一致的加粗、内描边和底线 | 资料库 Tab 接入全局 `data-ui-kind="segmented-control"` 合同，选中态统一消费 `selected-fg`、`state-selected-bg` 和 `state-selected-border`；菜单 CSS 只保留两列尺寸、分隔和未选中悬停，不再维护第二套选中视觉 | 分别切换两个 Tab，确认与配装、仓库等全局分段控件的选中态一致；静止、悬停和键盘焦点互不混淆，窄侧栏下文字不溢出且布局不跳动 |
| Bug #39（T7 本地 CI 类型构建阻断） | ✅ 已修复 | Armor 3.0 与攻略聚合约束接入后，Core、UI、Desktop 主进程、跨端打包、行为与架构断言、UI 合同、视觉采样和跨层 DTO 连续暴露本地 CI 阻断 | 修复解析与判别收窄；补齐 App 分域 alias 和 Services 浏览器安全子路径；同步共享 ViewModel、攻略导航、AI 页面上下文、UI 表面与选中态合同；固定视觉检查 Electron 设备缩放；设置模型补齐执行验证筛选，装备搜索、详情和账号实例链路统一透传 `class_type` | `pnpm test`、`pnpm visual:shell-contract`、`pnpm typecheck` 本地通过；未提交，等待 Review |
| Bug #40（攻略导入入口不可发现） | 🟡 修复待验证 | 攻略 URL 读取能力被藏在“新建攻略 → 来源类型 → 链接正文”三级操作里，攻略列表没有独立导入入口 | 列表工具栏增加“导入攻略”；点击后直接创建链接来源草稿、显示并聚焦 URL 输入，继续复用受控读取、预览确认和本地保存链路；Web 与 Desktop 共用同一页面行为 | 重启 Desktop 后点击“导入攻略”，确认无需切换来源类型即可输入链接、读取正文、采用预览并保存 |
| Bug #41（社区推荐仍使用旧导入交互） | 🟡 修复待验证 | 推荐数据页仍常驻 JSON / CSV 大文本框，并把解析、启用和清除平铺为同权按钮；Desktop 进入页面时不读取已保存表，导入或移除后当前账号匹配也不会立即重算 | DIM Wishlist 与社区推荐改为连续数据源管理行，首层显示真实启用状态、表名、规则数和模式覆盖；导入按需展开并经过检查预览后确认启用，移除要求二次确认；Desktop 补齐本机表初始读取、失败重试和强制刷新当前账号社区匹配 | 重启 Desktop 检查已保存表状态；导入 JSON / CSV 后确认无需刷新账号即可更新仓库匹配；替换、解析失败、读取失败重试和二次确认移除均保持可用；窄屏与 AI 抽屉开启时不溢出 |

## 说明

- UI 只维护 `packages/ui` 的共享产品实现；Markdown 合同记录稳定约束，Web 用于快速预览，Desktop 用于真实功能验收。
- UI 合同静态门禁覆盖稳定标记、表面枚举、排版、层级、菜单主题色和选中态方向线，避免菜单实现重新取得共享视觉职责。
- 新增 Bug 必须使用全局唯一编号并在本文件中登记。
