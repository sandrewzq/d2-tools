# 当前待办

> 更新时间：2026-06-23
> 维护规则：这是唯一当前待办文档。当前需求、bug、验收状态和短期优先级统一记录在这里；已经完成、取消或确认无效的条目直接删除，不再保留过程性清单。

## 当前健康度

| 检查项 | 状态 |
|---|---|
| `npx pnpm@9.15.0 test` | 通过：101 个测试文件，335 个测试 |
| `npx pnpm@9.15.0 typecheck` | 通过 |
| `npx pnpm@9.15.0 docs:check` | 通过：包含文档结构检查和全仓 UTF-8 / 乱码守卫 |
| 本次 GUI 验收 | Bug #12 通过；Bug #16 未通过（降级提示未正确显示）；护甲属性对比、本地推荐表联动、社区推荐可信度已过时删除；社区推荐功能需要重做 |

构建提醒：

- Vite 构建会提示 `DiagnosticsPanel.tsx` 和 `VaultPanel.tsx` 同时被静态和动态导入，当前不阻塞构建。
- npm 会提示 `.npmrc` 中 Electron mirror 配置未来可能不再被 npm 支持，当前不阻塞构建。
- `docs:check` 已接入 `encoding:check`，用于拦截非法 UTF-8、典型 mojibake 和连续问号造成的信息丢失。
- `encoding:check` 已补充多组 UTF-8 被旧代码页误解码后的片段守卫，例如 `\u7441\u546d`、`\u6d5c\u5b2a`、`\u9352\u55d8`、`\u5be4\u9e3f`、`\u93bf\u5d84`；发现用户可见文案乱码时应先补守卫测试，再修正文案。

## 当前 Bug

### P0 · Bug #5：今日/本周页遗失区域数据仍为空

现象：

- 桌面端和此前的真实接口复核都显示 `lost_sector` 为空。

当前判断：

- 当前 Bungie 公共里程碑和本地已补齐的 `DestinyMilestoneDefinition` 里，仍没有可确认的遗失区域条目。
- 不能用“传说 / 大师”等泛关键词兜底，否则会误把其他高难活动当成遗失区域。

下一步：

- 继续查 Bungie 公共数据源里是否还有其他可稳定识别遗失区域的来源。
- 只有在 Bungie API 或本地 Manifest 能确认时才展示，不做猜测。

验收：

- `packages/core/test/daily.liveData.test.ts` 覆盖遗失区域识别。
- 首页“今日 / 本周”面板在真实账号下能显示可确认的遗失区域，或明确说明当前公共数据源缺失。

### ✅ P1 · Bug #12：装备详情属性展示 — 已验收通过（2026-06-23）

验收结果：

- 护甲详情：六维属性和总值显示正确。
- 武器详情：射程、稳定性、操控性、装填速度、弹匣、RPM 等数值显示正确。
- 属性区块位置靠前，打开弹窗第一眼能看到核心数值。
- 不同装备、不同屏幕尺寸下属性展示稳定。

### P1 · Bug #16：社区推荐降级提示未正确显示

验收结果（2026-06-23）：

- 模拟 light.gg 失败场景后，未正确显示降级提示，用户无法区分”没有推荐”和”已降级显示其他来源”。

当前判断：

- 社区推荐功能整体需要重做，降级提示问题将在重做中一并解决，不再单独修。

## 当前需求

> 会议需求重组分析见 `docs/work/backlog/2026-06-23-meeting-product-requirements-analysis.md`。

### P0 · 首页日常信息与奖励进度工作台

目标：

- 首页不再承载任务助手或 AI 对话，专注展示玩家每天打开后最想确认的进度、轮换和奖励信息。

会议结论（2026-06-23）：

- 首页优先展示光等奖励进度、遗失区域、老九、枪匠、今日轮换和奖励等级。
- “今日轮换”目前语义还不够清楚，需要先拆成可确认的数据项：每日活动、每周轮换、商人库存、奖励重置和来源状态。
- 周报式首页是目标方向，但“撒哈拉 / 周报”所需数据来源还不清晰，先做数据来源调研，不做猜测展示。
- 奖励等级等低频或解释型信息可以放到首页下方，不挤占首屏核心区域。

任务：

- 继续处理 `Bug #5`：只在 Bungie API 或 Manifest 能确认时展示遗失区域。
- 研究“今日轮换”的具体内容边界，整理成可实现的数据清单。
- 研究“撒哈拉 / 周报”方向需要哪些数据源，哪些能从 Bungie 直接确认，哪些需要外部资料或暂缓。
- 强化商人售卖解析，优先覆盖老九、枪匠、艾达、圣人、拉乎尔等常用商人。
- 设计首页信息层级：首屏展示今日必看和奖励进度，下方展示奖励等级、来源状态和说明。

验收：

- 首页首屏能清楚展示玩家当天最需要看的进度和轮换，不出现任务助手或 AI 长对话。
- 今日轮换、遗失区域、商人库存都能说明数据来源；缺数据时明确提示，不用泛关键词猜测。
- `packages/core/test/daily.liveData.test.ts` 覆盖商人售卖、轮换和遗失区域解析。
- `packages/desktop/test/daily-panel-items.test.ts` 或新增测试覆盖 UI 文案。
- 首页在无数据、部分数据、完整数据三种状态下都有清晰反馈。

### P0 · 全局 AI 助手与任务助手侧边栏

状态：

- 已完成第一阶段：左侧主导航不再把 AI 助手作为独立页面，改为全局助手入口。
- 已完成第一阶段：全局右侧侧边栏支持“AI 助手 / 任务助手”切换、打开和关闭。
- 已完成第一阶段：AI 助手复用现有账号、仓库、每日和活动上下文，并新增历史记录的查看、切换和清空。
- 已完成第二阶段：AI 助手会把当前页面上下文写入 AI 请求，区分首页、账号、仓库、配装、资料库和设置页的分析重点。
- 已完成第二阶段：AI 聊天历史已持久化到本地 renderer 存储，重启后仍可读取最近记录；未读取账号数据时也能查看历史。
- 已完成第三阶段：AI 历史升级为多会话列表，支持新会话、切换会话和保留切换后的历史消息；当前仍不支持会话并行生成。
- 已完成第四阶段：仓库页会把当前筛选条件写入助手上下文，配装页会补充缺失件摘要，资料库页会补充当前搜索条件。
- 已完成第四阶段：任务助手从静态骨架升级为可粘贴任务 / 攻略文本的上下文树，能提取步骤、匹配账号装备并生成 AI 问答节点。
- 已完成第四阶段：AI 历史继续保留在侧边栏内二级面板，不另做弹窗；当前不支持会话并行生成。

目标：

- AI 助手从独立页面移动到全局侧边栏，任务助手也从首页移出，让用户在账号、仓库、配装、资料库等页面都能随时使用 AI。

会议结论（2026-06-23）：

- 任务助手不放首页，改为左侧导航或侧边栏能力。
- AI 助手作为全局侧边栏存在，不再占用首页主体内容。
- 任务文本、攻略内容和 AI 对话可以形成树状结构：左侧选择任务 / 攻略上下文，右侧进行对话。
- AI 历史记录需要保留，但是否单独弹出或作为侧边栏内的二级面板，还需要评估。

验收：

- 首页主体不再出现 AI 长对话或任务助手大区块；AI 入口在全局助手区域。
- 在账号、仓库、配装、资料库页面都能打开 AI 侧边栏并读取当前页面上下文。
- 连续对话不会把当前页面内容向下推远，桌面端为右侧侧边栏，窄屏为纵向区域。
- 历史记录能按会话查看、切换或清理，并持久化在本地 renderer 存储。
- 任务助手能展示用户粘贴的任务 / 攻略上下文树，并提取步骤、关联装备和 AI 问答节点。

### P1 · 账号与仓库页面 UI / UX 重构

目标：

- 账号页和仓库页从“能展示数据”升级为更接近游戏内和 DIM 使用节奏的工作区。

会议结论（2026-06-23）：

- 账号页需要把角色做成一级切换入口，优先突出当天正在玩的角色。
- 账号页需要展示更多账号内容，例如货币数量、材料、邮政官和角色关键摘要。
- 仓库页参考 DIM 和 POE2 仓库页的组织方式，转向更适合扫描的平铺卡片式布局。
- 仓库页需要支持自定义标签和按标签筛选，例如“愿望单”“保留”“待刷”“配装用”等。
- 装备详情页需要展示模组加成数值，并支持自定义标记。
- 装备筛选需要继续增强，后续补套装筛选；T1 / T2 / T3 / T4 / T5 的含义和处理规则待研究。

任务：

- 重构账号页角色入口：角色作为一级 tab 或卡片切换，角色装备 / 背包 / 货币跟随切换。
- 账号页补充货币、材料和常用资源摘要，不只显示装备。
- 仓库页改造为更适合扫描的卡片 / 平铺布局，保留列表密度和批量操作能力。
- 扩展仓库自定义标签能力，支持标签筛选、标签管理和详情页快速标记。
- 在装备详情页展示模组带来的属性加成，明确区分基础属性、模组加成和最终值。
- 调研并设计套装筛选、T1-T5 分层规则，确认它们来自游戏内字段、社区规则还是本地用户定义。

后续验收：

- 账号页首屏能一眼切换角色，并看到该角色当天最相关的信息。
- 仓库页能按自定义标签筛选，标签状态在卡片和详情页一致。
- 装备详情页能展示基础属性、模组加成和最终值，不误导用户。
- 套装筛选和 T1-T5 规则在实现前有明确数据来源和产品语义。

### P1 · 增强愿望单与目标刷取标记

目标：

- 从“导入社区 wishlist”升级为用户可自己定义目标：指定武器 perk 或护甲属性阈值，刷到符合条件的装备后自动标记收藏或目标命中。

会议结论（2026-06-23）：

- 武器：用户可以指定目标 perk 组合，刷到后自动标记收藏或愿望单命中。
- 护甲：用户可以指定目标属性数值，例如 Armor 3.0 六维最低值组合，刷到后自动标记。
- 这类能力应该和仓库自定义标签、详情页标记、筛选联动。
- 社区推荐旧框架需要重做，增强愿望单应成为新的本地目标系统基础。

任务：

- 设计本地目标规则模型：武器 perk 条件、护甲属性条件、标签动作和匹配说明。
- 把当前护甲属性筛选能力沉淀为可保存的目标规则，而不只是一次性筛选条件。
- 支持目标命中后在仓库卡片、装备详情和同名对比中显示标记。
- 设计“自动标记收藏 / 自动加标签 / 只提示不写入”的安全边界。
- 社区推荐重做时复用同一套来源、目标和命中解释模型。

验收：

- 用户能创建一个“指定 perk 武器”目标，仓库中符合条件的装备有明确命中标记。
- 用户能创建一个“指定护甲属性最低值”目标，符合条件的护甲能被筛出并标记。
- 目标命中解释清楚显示哪些条件满足、哪些不满足。
- 自动写入收藏或标签前有可配置的安全策略，不默认执行高风险写操作。

### P1 · AI 抄作业与自动配装方案生成

目标：

- 让用户输入攻略、视频文案或配装资料后，AI 能解析其中的装备、星象、属性、模组、武器和异域要求，并结合账号数据给出可执行配装方案。

会议结论（2026-06-23）：

- 当前手动调整配装学习成本过高，需要 AI 辅助“抄作业”。
- AI 需要分析攻略中需要的装备、星象、属性值、模组、武器和异域装备。
- 输出要包含匹配度：账号里有哪些、缺哪些、哪些需要升级、哪些需要去刷。
- 最终要生成可操作方案，而不是只给文字建议。

任务：

- 设计攻略输入结构：纯文本、链接摘要、用户粘贴的配装说明和手动补充条件。
- 设计 AI 解析输出 schema：职业、子职业、星象 / 碎片、异域、武器、模组、属性目标、关键循环。
- 设计账号匹配逻辑：已拥有、缺失、可替代、属性不足、需要升级、需要刷取。
- 设计方案生成结果：推荐装备组合、替代方案、缺口清单、刷取建议和操作步骤。
- 与现有配装模板、仓库目标规则、装备详情联动。

验收：

- 用户粘贴一段攻略后，AI 能结构化列出攻略要求。
- 工具能对照账号数据给出匹配度和缺口清单。
- 输出能生成至少一个可保存的本地配装方案草稿。
- 缺少装备时给出刷取或替代建议，而不是直接失败。

### P2 · 装备详情游戏内风格 UI 收尾

状态：

- 设计已确认：顶部核心详情按游戏内装备详情结构展示，下方接 DIM 式工具区。
- 顶部包含装备图标、名称、职业 / 部位 / 稀有度、光等、六维属性、总计和能量。
- 下方工具区保留同名对比、社区推荐、AI、备注和写操作。
- 已完成第一版实现：共享装备详情弹窗顶部改为游戏内风格卡片，下方工具区抽为 `ItemDetailTools`。
- 护甲能量已接入 Bungie 账号摘要里的真实实例能量字段，详情页能显示已用 / 容量 / 剩余；没有实例能量时仍显示“未读取”。
- 当前代码层验证已通过，仍待真实账号 GUI 验收确认视觉细节。
- 设计文档见 `docs/work/backlog/2026-06-23-item-detail-game-style-ui-design.md`。

后续重点：

- 增加装备模组加成数值展示。
- 增加详情页自定义标记入口。
- 与增强愿望单、目标命中、仓库标签联动。

验收：

- 护甲详情首屏顶部像游戏内装备页，六维只显示生命值、近战、手雷、超能、职业、武器。
- 工具区能力不丢失，用户仍能找到同名对比、目标命中、AI、备注和写操作。
- 1366px 和 1920px 桌面尺寸下，标题、属性条、能量、工具入口不重叠、不溢出。

### P3 · 活动复盘增强

目标：

- 补齐 Raid Report / Destiny Tracker 方向的基础可读复盘。

当前问题：

- 当前活动能力更偏“能拉到记录”，离玩家真正想看的“最近打了什么、和谁打的、打得怎么样”还有距离。
- 缺少把原始活动记录整理成可回顾、可比较、可扫描摘要的展示层。

任务：

- 按 Raid / Dungeon / 常规活动分组最近记录。
- 增加队伍成员、完成时间、失败 / 完成状态、关键统计的可读摘要。
- 提供“最近 10 场活动”快速复盘视图。

预期结果：

- 用户打开后，不用逐条读原始记录，也能快速回忆最近活动情况。

### P4 · 桌面发布体验闭环

目标：

- 从可运行程序，逐步走向正式桌面软件体验。

当前问题：

- 安装器和自动更新第一阶段已按 NSIS 方案落地，但从普通用户角度看，备份、恢复和问题诊断还没有形成完整闭环。
- 功能本身和“可长期使用的软件体验”之间还有一段产品化收尾工作。

任务：

- 维护开发期和发布期两条独立链路：开发期使用 `npx pnpm@9.15.0 dev:desktop` 或 `scripts/dev-desktop.ps1` 热启动桌面应用，发布前走 NSIS 安装器打包验证。
- 用真实 GitHub Release 验证设置页检查更新、下载更新、重启并安装链路。
- 确认手动覆盖安装时目标目录占用提示符合预期。
- 增加本地备份 / 恢复向导。
- 评估托盘图标是否真的符合使用场景。
- 梳理 `%APPDATA%/d2-tools` 数据目录迁移、备份和诊断导出的用户说明。

预期结果：

- 让用户即使不懂开发环境，也能更安心地安装、升级、迁移和排查问题。

## 暂不优先

### 装备模板 / 本地配装模板使用引导（原 `Bug #3`）

- 当前问题：已有配装相关能力，但用户不容易理解“模板”“当前装备”“本地保存方案”之间的区别，也不知道入口和推荐使用顺序。
- 后续目标：补清晰的引导、空状态说明和典型使用场景，降低第一次使用门槛。

### 完整 TWID 级日报 / 周报聚合（原 `Bug #7`）

- 当前问题：当前“今日 / 本周”更偏结构化接口数据展示，还不是接近 TWID / 中文日报机器人的完整聚合视图。
- 后续目标：补更多轮换、活动提示、重点摘要和分享文本，让它更像玩家每天愿意看的摘要页。

### DIM 整理工具文案与交互边界重命名（原 `Bug #9`）

- 当前问题：当前“整理”“清理”“候选”“建议保留”这些词在语义边界上还不够稳，容易让用户误解为工具会直接执行高风险写操作。
- 后续目标：把文案改得更接近“分析建议 / 候选清单 / 人工确认执行”，减少误解和心理负担。

### 完整 DIM 级拖拽式装备移动

- 当前问题：当前已有写操作和装备转移基础能力，但还没有 DIM 那种高自由度、低摩擦的拖拽移动体验。
- 后续目标：支持更直观的角色/仓库间拖拽、批量移动和冲突反馈。

### 完整护甲属性优化器

- 当前问题：目前能看单件护甲属性、基础评分和筛选条件，但还没有真正的“整套护甲搭配求解器”。
- 后续目标：支持按职业、属性目标、模组约束和异域装备组合计算更优配装方案。

### Destiny Tracker 级排行榜

- 当前问题：当前活动和账号数据还没有形成可比较、可排序的战绩排行榜视图。
- 后续目标：补玩家常见的 PVP / PVE 统计排行、关键指标对比和阶段性表现趋势。

### Raid Report 级完整副本复盘

- 当前问题：当前活动复盘仍偏基础记录展示，离 Raid Report 那种副本履历、通关细节和队伍表现分析还有明显差距。
- 后续目标：补齐副本级记录、历史趋势、关键节点和队伍协作视图。

### 默认内置未授权社区表格数据

- 当前问题：社区推荐能力已经在扩展，但默认内置第三方社区表格会带来授权、维护和过期风险。
- 当前结论：在明确授权前，不内置任何未授权社区数据，只保留导入能力和本地使用链路。

### 公网服务、多用户共享链接、图片化报告发布

- 当前问题：这些能力会把本地单机工具扩展成带发布、分享、托管属性的产品，复杂度和安全边界会陡增。
- 当前结论：现阶段继续坚持“本地优先、单用户、自主授权”，暂不扩展到公网服务和分享分发场景。

## 验证命令

```powershell
npx pnpm@9.15.0 docs:check
npx pnpm@9.15.0 test
npx pnpm@9.15.0 typecheck
```
