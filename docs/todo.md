# 当前待办

> 更新时间：2026-09-14
> 本文件只保留当前任务、状态和下一步。详细范围与验收标准由对应 backlog 维护，阶段过程使用 Git 历史追溯。

## 健康度

| 检查项 | 状态 | 备注 |
|---|---|---|
| 本地 CI | ✅ 2026-09-10 通过 | 已通过文档、编码、全部 workspace 构建、490 项行为测试、质量门禁（含 UI 合同）、62 项架构测试、共享 Shell 视觉合同和全量类型检查 |
| GitHub CI | ⏳ push 后异步执行 | 执行 frozen install、`pnpm test`、共享 Shell 视觉契约、UI 合同静态门禁和构建后的 `pnpm typecheck:ci`；普通 push 不等待结果 |
| Release 门禁 | ✅ 0.0.22 已发布 | 已通过 macOS 发布入口完成本地门禁、GitHub Actions、Windows NSIS 安装包和 GitHub Release |
| Agent 自动验证 | ⛔ 默认禁用 | 只有用户明确要求本地测试、构建或打包时才执行 |

## 当前任务

| 编号 | 优先级 | 状态 | 任务 | Backlog | 下一步 |
|---|---|---|---|---|---|
| Bug #80 | P1 | 🟠 代码完成，待本地启动复验 | 仓库结果排序下拉被移入结果命令栏，左侧筛选栏无法再选择排序 | — | 结果排序已挪回左侧筛选栏底部「排序」区（选中「推荐权重」时显示权重提示），结果命令栏只保留结果摘要与批量入口；重启 Desktop 确认左侧可切换排序、结果顺序立即生效，命令栏不再有重复排序入口。 |
| Bug #81 | P1 | 🟠 代码完成，待本地启动复验 | 仓库「位置（查看范围）」占用左侧筛选栏，结果命令栏留出大片空白 | — | 位置已按用户要求移入结果命令栏（结果摘要与批量入口之间），改为横向分段按钮并保留各项数量，左侧筛选栏移除「位置」区并顺带清理了失效的位置 props 与样式；重启 Desktop 确认武器模式下位置可切换、数量正确，护甲/装备模式不显示位置入口。 |
| Bug #82 | P1 | 🟠 代码完成，待本地启动复验 | 左侧筛选分区标题冗余且过长，筛选顺序不符合使用习惯 | — | 已去掉全部分区标题，改为连续筛选块；武器模式顺序固定为类型 → 槽位 → 弹药 → 伤害 → 反勇士 → 阶级 → 稀有度 → 锻造 → 武器类型 → 武器框架 → 锁定 → 玩家标记 → 结果排序；护甲与装备顺序同步排列。去掉分区标题后按钮网格类筛选项（物品类型、槽位、部位、位置）失去标签，已统一补上 `.vault-filter-label-field` 标签结构，并把槽位网格改为两列避免文字截断。重启 Desktop 确认三种模式顺序、每项标签齐全、控件间距与重置行为正常。 |
| Bug #83 | P1 | 🟠 代码完成，待本地启动复验 | 武器框架筛选展开后 chips 列表过长，占用大量左侧高度 | — | 武器框架已改为单选下拉（全部框架 / 单个框架，各自带命中数量），移除折叠展开区与多选 chips；`VaultFrameFilter` 由数组改为单值，查询索引 facet 由多值并集改为单值过滤，重置与切组行为同步；同时彻底清理了 `.vault-frame-*` 的 128 行失效 CSS。重启 Desktop 确认下拉只显示一个框架选项、命中数量与结果一致、切换分组后恢复「全部框架」。 |
| Bug #84 | P1 | 🟠 代码完成，待本地启动复验 | 左侧筛选中武器槽位与锻造没有图标和颜色区分，不如弹药/伤害直观 | — | 槽位改用自绘字形 + 游戏事实色：动能＝方块·中性蓝灰、能量＝三角·绿色、威能＝圆形·紫色，按钮内改为「图标 + 名称 + 数量」两端对齐；锻造沿用仓库卡片的菱形语言，可锻造＝实心琥珀、非锻造＝空心中性。均不仿造游戏图标、不只靠颜色表达。重启 Desktop 确认两组筛选的图标、色调与选中态。 |
| Bug #85 | P1 | 🟠 代码完成，待本地启动复验 | 推荐来源页导入导出混在一起、按钮看不出方向，模板与导入被拆到不同分组 | — | 页面按方向分成「导入」「导出」两组：导入＝人工推荐 CSV（点击打开导入弹框）＋ DIM Wishlist（在线导入 / 导入 Wishlist 文件 / 粘贴 Wishlist / 移除 Wishlist）；导出＝导出当前推荐（导出为 CSV）。人工推荐 CSV 的空白模板已从导出组移入导入弹框，弹框内按「1 下载模板（中文 / 英文）→ 2 选择填好的 CSV 文件 → 校验预览 → 确认导入」排列，带初始焦点、Escape 关闭与 Tab 焦点陷阱。按钮文案全部带明确动词并补齐 aria-label；「查看详情」保持独立弹框。重启 Desktop 确认分组、导入弹框流程与焦点行为。 |
| Bug #86 | P1 | 🟠 代码完成，待本地启动复验 | 推荐来源页提示行与上方面板左右错位；「复制验收报告」属于诊断能力却常驻首屏；知识库未可用提示不给下一步动作 | — | 对齐：给 `vault-recommendation-data-panel` 补 `margin: 12px`，与提示行、来源管理区回到同一 12px 基准（此前只有新面板贴边）。诊断：按合同把「复制验收报告」从首屏移入来源详情弹框底部（合同允许「来源详情弹框或设置诊断」），复制成功/失败复用现有反馈通道。文案：`formatRecommendationScanDetail` 改为按 `blocking_reason` / `issues[].code` 结构化原因给出行动导向说明（不再优先透出上游技术文案，也不做字符串推断），未可用时明确「导入武器推荐 CSV 后才会参与核对」。重启 Desktop 确认三处对齐、详情弹框内诊断区、提示文案。 |
| Bug #87 | P2 | 📝 候选方案，待排期 | 「复制验收报告」按合同 239 条应完全归入「设置 → 诊断」 | — | 现落在来源详情弹框（合同 211 条允许的两种归宿之一）。完全迁到设置诊断需要让设置页拿到账号武器集合、`matchCommunityVaultItems` 与扫描状态，属于跨 provider 改动；确认有实际需要时再排期。 |
| Bug #88 | P1 | 🟠 代码完成，待本地启动复验 | DIM 导入后卡片来源显示「未标注来源 #N」这类内部编号 | [T56](work/backlog/T56-unified-recommendation-model.md) | 真因是匹配缓存：revision 只由旧表指纹 + 算法版本决定，同文件重复导入指纹不变，旧标签一直被复用。已修：来源身份改为结构化分组、标签导入期固化；DIM 取消双写与旧表优先读，三层新模型成为唯一读写路径；缓存 revision 纳入新模型指纹。已导入数据无需重导，重启即生效。 |
| Bug #89 | P1 | 🟠 代码完成，待本地启动复验 | DIM 愿望单几乎整排「未命中」，锻造强化特征不算命中 | [T56](work/backlog/T56-unified-recommendation-model.md) | 真因：DIM 组合只比 perk Hash，人工 CSV 有名称兜底，两条路径口径不一致；愿望单写基础特性、账号装同名强化特征时全判未命中。已统一为「同一 Hash 或同一名称」，并补 `socket_plugs.name` 回退路径。实测至少 136 件从零命中变为有命中。 |
| Bug #90 | P1 | 🟠 代码完成，待本地启动复验 | 同一把武器卡片显示 5/5、详情显示 4/5，且详情只列出 Aegis 的 3 套组合 | [T56](work/backlog/T56-unified-recommendation-model.md) | 真因：详情自带一套只比 Hash 的判定，且 `slice(0, 3)` 按「最接近」排序把其他来源挤掉。已修：详情改消费匹配事实、逐栏结果由事实层一次算出、按来源全量展示、条目 id 改用规则 ID。 |
| T1 | P1 | 📝 候选方案，待排期 | 全应用英文国际化补齐 | [任务说明](work/backlog/T1-english-internationalization.md) | 先补齐页面标题、顶部状态和共享弹窗，再按仓库、装备详情、配装、AI 助手顺序迁移；完成核心工作流前不宣称完整英文支持。 |
| T23 | P2 | ⏸️ 等待官方书面授权 | D2Checkpoint 进度机器人集成 | [执行 backlog](work/backlog/T23-d2checkpoint-integration.md) | 用户已于 2026-09-08 向 `support@d2checkpoint.com` 发送授权申请；收到回复前不进入开发。 |
| T43 | P1 | 🟠 开发完成，待真实账号验收 | 任务待处理中心 | [任务说明](work/backlog/T43-pursuit-attention-center.md) | 使用真实账号在“账号 → 本周行动 → 任务与赏金”核对三角色任务、赏金、角色目标和赛季挑战的数量、进度、追踪、到期与完成待处理状态；首页任务卡已取消。 |
| T44 | P1 | 🟠 已修正奖励来源，待真实账号验收 | 光等提升路线 | [任务说明](work/backlog/T44-character-progression-center.md) | 周挑战奖励按 Objective 读取 Manifest `displayRewards` 并兼容 `dummyRewards`，普通活动掉落不再参与推荐；需核对三角色“本周可做 / 当前无可领取挑战”与游戏内一致。 |
| T45 | P1 | 🟠 首版开发完成，待真实账号验收 | 本周刷取清单 | [任务说明](work/backlog/T45-weekly-farming-checklist.md) | 页面入口调整为“账号 → 本周行动 → 本周刷取”；核对当前轮换活动、活动级装备来源、T20 推荐、账号持有与游戏内图样进度，并确认首页摘要及资料库 / 仓库 / 商人深链接不改变 T45 的独立排序；重点确认已完成、进行中、不可制作和未返回四类图样状态。 |
| T55 | P1 | 🟠 代码完成，待本地启动复验 | 仓库推荐筛选支持多来源（AND 聚合） | [任务说明](work/backlog/T20-weapon-recommendation-vault-cleanup.md) | 先与 T56 的唯一 `sourceId` 模型对齐，再重启 Desktop 多选人工来源、多个 DIM 来源与 DIM，核对每行命中件数、分段徽标与卡片来源摘要一致；来源管理快照已改为删除项按需读取、来源影响件数批量聚合，并固定管理读取回调；管理快照延后到进入“推荐来源”页，首屏点击仓库不再触发大体量 SQLite 聚合。 |
| T56 | P1 | 🟠 开发中 | 统一推荐模型与多来源导入 | [任务说明](work/backlog/T56-unified-recommendation-model.md) | 已接入空 perks 规则保留、weapon_only 语义、DIM source block 独立 sourceId、文档/来源/规则持久化、统一匹配入口，以及仓库卡片、详情和审计的多 DIM 来源摘要；DIM 已改为无内置依赖、仅手动在线或本地导入；待真实数据验收。 |
| T57 | P1 | 🟠 重新设计中，待真实账号与窄屏实窗验收 | 仓库推荐工作流简化 | [T56（已并入）](work/backlog/T56-unified-recommendation-model.md) | 推荐来源作为独立工作台直接展示平级来源列表、统计和结果；导入与管理在同一页完成，详情、规则和 DIM 作者分组进入弹框；来源删除后不再残留在管理列表或浏览筛选中；左侧锁定改为分段选择，常用状态提前，动态筛选选项按当前可得到数量排序。 |
| T58 | P1 | 🟠 首批修正已完成，待真实账号验收 | 游戏内配装完整子职业配置 | [任务说明](work/backlog/T58-ingame-loadout-subclass-fidelity.md) | 保留 Bungie 配装 Plug 的 socket 位置，按超能/技能、星象、碎片和其他配置展示；复制到应用配装与 DIM 导出保留原始 socket 映射；后续补齐 Manifest SocketTypeDefinition 后再替换启发式分类。 |
| T59 | P1 | 🟠 主体完成，剩余 ⑤⑥ 待执行 | DIM 组合展开成几十上百行、判定按组合而非栏；来源类型被当成业务分支 | [T56](work/backlog/T56-unified-recommendation-model.md) | 已确认并实现：同一来源同一武器的组合去冗余超集行后若为各栏候选的笛卡尔积，则归约成「每栏任选其一」；判定单位由「组合行」改为「栏」，x/y = 命中栏数 / 要求栏数。所有来源（含 DIM）走同一份匹配事实与同一个证据卡组件，DIM 不再独立分组、不再按类型排权重。实测 Aegis 863/863、小棒猪去冗余后 257/257 可无损归约。**批次④ 现状**：缓存 revision 已覆盖全部来源指纹、合同里两条按类型写的规则已改写（均已满足）；未完成 = 删除恒空的 `dim_wishlist` 事实字段与键收敛（64 处引用 / 14 文件）、导入期按行忽略校验。清单与逐文件执行步骤见 [T56 交接清单](work/backlog/T56-unified-recommendation-model.md#交接剩余工作与执行清单2026-09-15可脱离上下文执行)：⑤ 删组合类型标记（DIM 只产出来源事实）、⑥ 剩余 7 处 `dim_wishlist` 兼容引用；两者均在 backlog 内定位到行，可脱离上下文执行。 |

## 已完成基线

| 编号 | 发布版本 | 结果 | 记录 |
|---|---|---|---|
| T14 | v0.0.22 | ✅ 应用配装与逐部位护甲规划已完成并验收 | [完成摘要](work/backlog/T14-armor-planner-slot-aware-mods.md) |
| T20 | v0.0.22 | ✅ 武器推荐与仓库整理已完成并验收 | [完成摘要](work/backlog/T20-weapon-recommendation-vault-cleanup.md) |
| T22 | 待下一版本 | ✅ Renderer 事件驱动性能收敛、T21 发布身份成果与 Bug #79 已完成，真实账号验收和本地 CI 通过 | [正式架构结论](development.md) |
| T40 | 待发布 | ✅ 账号容量与风险摘要已完成并通过真实账号验收 | [完成摘要](work/backlog/T40-account-capacity-risk.md) |
| T42 | 待发布 | ✅ 光等状态与缺口已完成并通过真实账号验收 | [完成摘要](work/backlog/T42-power-growth-analysis.md) |
| T46 | 待发布 | ✅ 商人账号上下文首批范围已完成并通过真实账号验收 | [完成摘要](work/backlog/T46-vendor-account-context.md) |

## 说明

- `T数字` 和 `Bug #数字` 创建后保持全局唯一且不再改号；需求拆分时使用新的未占用编号。当前任务表按任务编号升序排列，优先级和状态以对应列为准，不通过表格位置表达。
- T41“邮政官安全处理”已取消，需求文档已删除；该编号冻结且不再复用。
- 已移除独立本地攻略库；攻略链接、正文和 AI 整理结果统一进入配装页生成未保存草稿，旧攻略 JSON 只作为遗留备份数据保留。
- UI 只维护 `packages/ui` 的共享产品实现；Markdown 合同记录稳定约束，Web 用于快速预览，Desktop 用于真实功能验收。
- UI 合同静态门禁覆盖稳定标记、表面枚举、排版、层级、菜单主题色和选中态方向线，避免菜单实现重新取得共享视觉职责。
- 已完成任务不再追加功能；新需求建立独立小任务，明确回归才登记独立 Bug。
