# 当前待办

> 更新时间：2026-06-27
> 这是唯一当前待办文档。已完成、取消或确认无效的条目直接删除。

## 健康度

| 检查项 | 状态 |
|---|---|
| `pnpm test`（125 文件 / 428 测试） | ✅ 通过 |
| `pnpm typecheck` | ✅ 通过 |
| `pnpm docs:check` | ✅ 通过 |
| 架构审查（2026-06-24） | 7.5/10，新增 Bug #17–#23 |
| Bug #5 根因分析（2026-06-24） | 数据源错误，已降到最低优先级，备选方案已记录 |

> 构建提醒：Vite 提示 `DiagnosticsPanel.tsx` 和 `VaultPanel.tsx` 同时被静态和动态导入，不阻塞构建。npm 提示 `.npmrc` 中 Electron mirror 配置未来可能不被支持，不阻塞构建。`docs:check` 含 UTF-8 / 乱码守卫。

---

## Bug

### 待修复

当前无待修复 Bug。

### 已修复

| # | 标题 | 日期 |
|---|---|---|
| 5 | 遗失区域数据为空 — 改用 Manifest 静态数据 + 每日轮换推算，不再依赖公共里程碑 API | 2026-06-25 |

| # | 标题 | 日期 |
|---|---|---|
| 17 | AiSettingsPanel 构造假 D2Config — 改为模型列表最小 AI 设置入参 | 2026-06-24 |
| 18 | as never 绕过跨层类型检查 — sharedTypes 改为 re-export core 类型 | 2026-06-24 |
| 21 | AI 协议归一化逻辑重复 — 统一沉淀到 core AI settings 模块 | 2026-06-24 |
| 23 | updates.ts 全窗口广播无防护 — 增加 isDestroyed() 防护 | 2026-06-24 |
| 20 | useDiagnosticsSettings 职责混叠 — 拆出诊断状态、AI/写操作设置状态、操作日志状态 hook，保留外部兼容 facade | 2026-06-26 |
| 19 | HomePage 上帝组件 — dashboard、派生状态、菜单路由、装备详情弹窗和写操作组合已拆出，保留为桌面端页面 composition root | 2026-06-26 |
| 12 | 装备详情属性展示 — GUI 验收通过 | 2026-06-23 |
| 22 | 10 分钟定时同步 timer 依赖不稳定 — 改用 useRef | 2026-06-24 |
| 16 | 社区推荐降级提示未正确显示 — 详情页在 light.gg / 社区推荐读取失败时显示降级提示，并保留 DIM 愿望单、本地目标和同名对比路径 | 2026-06-27 |
| 24 | 视觉升级后仓库点击延迟 — 仓库高密度卡片、列表项和按钮去掉动画化阴影、滤镜与过渡；仓库列表和同名对比改为首屏限量渲染 + 加载更多，并缓存同名对比实例查找 | 2026-06-27 |

### 建议（P2，暂不分配 Bug 编号）

- 已新增根目录临时输出守卫：`pnpm docs:check` 现在会拒绝 `tmp-*`、`.tmp-*`、`*.err.log` 直接出现在仓库根目录；本地临时日志统一写到 `.local-data/tmp/`
- `pageLabels` 3 处重复（GlobalAssistantSidebar / assistantContext / HomePage）→ 抽取共享常量
- ✅ ~~`collectAccountItems` 2 处重复 → 已抽取到 `utils/accountItems.ts`~~
- ✅ ~~`VaultArmorStatRule.min` 类型为 string 而非 number → 已修复为 number~~
- `useItemDetail` 缓存无限增长 → 加 LRU 或上限
- model 下拉切回时清空用户输入 → 保留为 placeholder
- IPC channel 命名硬编码 → 统一枚举

### 详细条目

---

## 当前需求

> 会议需求重组分析见 `docs/work/archive/2026-06-23-meeting-product-requirements-analysis.md`。

### P0 · 多端支持架构重构

> **目标** 当前桌面端继续交付，同时把业务规则、服务访问和前端查询层重构成桌面 / Web / 移动端可复用的边界。

**当前状态** 第一、第二和第三阶段轻量验收已完成：新增 `packages/services` 承接 Profile / Manifest / LocalData / AI 服务契约、桌面 bridge adapter 和测试型内存 adapter；新增 `packages/app` 承接跨端 `QueryState`、账号 workspace、账号页 view model、仓库 workspace、AI 助手消息入口、仓库列表主链、仓库批量选择准备、仓库清理 / 重复组 / 批量写操作前文案、配装模板命中 lookup、配装来源定位、配装对比 view model、配装缺失件计划和写操作前文案；新增边界测试拦截 app / services 反向依赖 desktop、Electron 或 UI 包。AI 助手发送消息、账号读取、仓库标签读取、本地目标规则初始化，以及仓库列表 / 筛选 / 分组 / 标签命中 / 愿望单命中 / 本地目标命中摘要，已接入 `desktop UI -> app -> services -> window.d2 / IPC`。账号页角色 tab、选中角色、摘要文案、装备 meta / key、已装备 / 背包 slot 分组、当前角色配装命中计数、材料行、邮政官预览和游戏内配装栏行已下沉到 app；配装模板命中 lookup、缺失件转移计划和 blocked reason 文案已迁到 app；最高光等装备计划、执行计划、确认文案和执行反馈文案已迁到 app，desktop 保留兼容出口。`createMemoryServices()` 已覆盖 Profile、LocalData、Manifest 和 AI 四类服务形态，可驱动账号、仓库和 AI app workspace 脱离 desktop bridge 运行。历史设计见 `docs/work/archive/multi-platform-architecture-design.md`。

**要做什么**
- 后续新增 app workspace 时同步补测试型 services adapter 覆盖，避免新业务重新绑定 desktop bridge
- 继续把新增本地存储、Manifest、Bungie 访问、AI 请求等平台能力优先收口到 `packages/services`
- 继续补 `core`、`services`、`app` 边界测试，阻止新增跨层依赖

**验收** 三个阶段已满足：桌面端现有功能不回退；`packages/app` 和 `packages/services` 可独立构建；跨端共享层不依赖 desktop、Electron 或桌面 UI；AI、账号读取、仓库主链已有真实页面链路完成迁移；账号 / 仓库 / 配装主要 view model、写操作前计划、候选选择和反馈文案已迁到 app，desktop 保留 UI 状态、confirm、clipboard 和 IPC/API 执行；测试型内存 adapter 已证明 app workspace 可脱离 `window.d2` / desktop bridge 运行。后续转为持续防回退。

---

### P0 · 首页日常信息与奖励进度工作台

> **目标** 首页不再承载任务助手或 AI 对话，专注展示玩家每天想确认的进度、轮换和奖励。

**要做什么**
- 强化 `packages/core/src/daily/liveData.ts` 的商人售卖解析（老九、枪匠、艾达、圣人、拉乎尔）
- 配合 Bug #5 处理遗失区域，只在 Bungie API 或 Manifest 能确认时展示
- 研究"今日轮换"的数据边界：每日活动、每周轮换、商人库存、奖励重置、来源状态
- 研究"撒哈拉 / 周报"方向需要哪些数据源
- 设计首页信息层级：首屏展示今日必看和奖励进度，下方放奖励等级、来源状态

**当前状态** 首页主体已保持在日常信息工作台，不承载任务助手或 AI 长对话；`DailySummaryPanel` 首屏已改为“今日必看”，增加“奖励进度”和“来源状态”摘要，并保留每日 / 每周重置、今日行动、本周周报、轮换细节、来源卡片和复制日报 / 本周重点能力。商人售卖解析和周报级数据源仍需继续补强。

**会议结论** 优先展示光等奖励进度、遗失区域、老九、枪匠、今日轮换和奖励等级。周报式首页是目标方向但数据来源待调研。

**验收** 首页不出现任务助手或 AI 长对话；数据来源能说明；测试覆盖商人售卖、轮换和遗失区域解析；三种数据状态下都有清晰反馈。

---

### P0 · 全局 AI 助手与任务助手侧边栏

> **目标** AI 助手从独立页面移到全局侧边栏，让用户在任何页面都能随时使用。任务助手也从首页移出。

**当前状态** 已完成五个阶段：左侧导航移除 AI 页面入口 → 全局右侧侧边栏 + 助手切换 → 页面上下文写入 AI 请求 + 历史持久化 → 多会话列表 + 仓库/配装/资料库上下文 → Codex/Trae 式单列聊天工作台。AI 助手现在支持会话切换、Codex 式布局、页面上下文和历史抽屉。任务助手支持粘贴任务/攻略文本、提取步骤、匹配装备、生成“可保存方案草稿”节点并补充 AI 问答节点，暂不推进重构。

**会议结论** 任务助手不放首页，AI 助手作为全局侧边栏。任务文本/攻略/AI 对话可形成树状结构。

**验收** 首页无 AI 长对话；侧边栏在任意页面可打开；历史可切换/清理/持久化；任务助手能展示上下文树并匹配装备。

---

### P1 · 全局视觉升级 v2

> **目标** 在 `ui-style-guide-v1.md` 的基础样式系统之上，按 C1 · 克制增强方向做用户第一眼能感知的全局视觉升级。

**当前状态** 已完成 C1 方向第一轮全局视觉骨架，并按卡顿反馈做了性能回调：全局 surface / border / text / accent token 已重设；Shell 左侧导航、主内容区、右侧 AI 侧栏形成更清楚的层级；按钮、输入框、select、textarea、focus、status-message、badge、tool-panel、toolbar、list-row、item-card 已统一到更明显的桌面工具视觉；首页、账号、仓库、设置和 AI 的主要容器与高频对象已接入新层级；仓库卡片、装备行和高密度列表已去掉动画化大阴影和位移，保留低成本的边框、底色和选中条反馈；账号页装备 / 背包对照面板、配装页游戏内配装槽和设置页备份迁移区已接入同一套面板样式。历史设计见 `docs/work/archive/global-visual-upgrade-v2-design.md`。

**要做什么**
- 按 C1 方向重设全局 surface / border / text / status token，让页面底层、主面板、交互层更有区分
- 强化 Shell 左侧导航、页面标题区、主内容区和右侧 AI 侧栏的视觉骨架
- 统一按钮、输入框、select、textarea、badge、status-message、tool-panel、toolbar、list-row、item-card 的真实视觉表现
- 覆盖首页、账号、仓库、设置、AI 的主要容器和高频对象，确保第一眼能看出变化
- 继续在真实 Electron 桌面壳里做 1366px / 1920px / 窄屏视觉验收，重点检查文字挤压、按钮溢出、层级过重和过亮

**验收** 打开应用能明显看出视觉升级；首页、账号、仓库、设置、AI 属于同一套系统；主操作、选中态、hover、焦点态可感知但不过度发光；高密度信息仍可扫描。

---

### P1 · 账号与仓库页面 UI / UX 重构

> **目标** 账号页和仓库页从"能展示数据"升级为接近游戏内和 DIM 使用节奏的工作区。

**当前状态** 账号页已收口为单角色即时操作台：角色一级 tab 已保留，主工作区已改为"当前角色装备与背包"同槽位对照面板，同类武器 / 同部位护甲优先左右对照；材料与邮政官下沉到低频区；游戏内配装栏已从账号页移出，统一放到配装菜单集中处理。仓库页第一版已转向平铺装备卡片，保留筛选、批量选择、标签操作、愿望单 / 社区 / 配装命中展示。自定义标签第一版已扩展到保留、关注、待刷、配装用、可清理，支持卡片直接标记、筛选、整理模式批量处理，并已在装备详情页提供本地标记入口。装备详情已展示护甲基础属性、模组加成和最终值，并在概览区增加目标命中摘要，直接提示 DIM 愿望单命中、本地护甲目标命中或未命中，并补充命中来源、写入安全说明和同名对比复查路径。套装 / T1-T5 调研仍待推进。

**要做什么**
- 账号页：继续做真实账号视觉验收，重点检查同槽位对照行在 1366px / 1920px / 窄屏下是否拥挤
- 账号页：继续补充真实账号下货币、材料、邮政官的覆盖范围
- 仓库页：继续打磨平铺卡片式布局的真实账号视觉验收；后续评估多标签、标签管理器和愿望单目标规则联动
- 装备详情：继续做真实账号视觉验收，后续打磨目标命中说明和同名对比联动
- 调研套装筛选和 T1-T5 分层规则的数据来源

**会议结论** 角色一级切换、仓库卡片化、自定义标签和模组加成展示均为 P1 优先。

**验收** 账号页能一眼切换角色并看到当天相关信息；仓库能按标签筛选；详情页展示基础属性、模组加成和最终值，并完成真实账号视觉验收。

---

### P1 · 增强愿望单与目标刷取标记

> **目标** 从"导入社区 wishlist"升级为用户自定义目标：指定武器 perk 或护甲属性阈值，刷到后自动标记。

**当前状态** 已完成本地目标规则第一版：护甲可用下拉选择 Armor 3.0 六维属性并设置最低值，条件数量不限制、不做预设；武器可从仓库已有武器下拉选择，再从该武器已有 perk 下拉添加目标组合。core 层已完成 Manifest perk 库查询（`packages/core/src/analysis/manifestPerks.ts`），支持从全量沙盒 perk 库搜索和筛选武器 perk。规则持久化到本地数据目录，并显式保存 `notify_only` 命中后处理策略。命中后在仓库摘要、`目标命中`筛选、装备卡片、装备详情概览区和同名对比行内显示，并提供命中条件解释。整理模式已支持一键选择当前结果中的目标命中装备，方便用户手动批量标记为保留、关注、待刷、配装用或可清理。当前策略只提示不自动写入装备状态，UI 已明确说明不会自动收藏、加标签或改动装备。

**要做什么**
- 将 Manifest perk 选择接入桌面端 UI（武器目标规则创建流程）
- 社区推荐重做时复用同一套来源、目标和命中解释模型

**会议结论** 武器可指定目标 perk 组合，护甲可指定 Armor 3.0 六维最低值。与仓库标签、详情页标记、筛选联动。

**验收** 用户能创建武器/护甲目标，命中后有明确标记和条件解释；自动写入前有可配置策略。

---

### P1 · AI 抄作业与自动配装方案生成

> **目标** 粘贴攻略或配装资料后，AI 解析装备/星象/属性/模组/武器/异域要求，对照账号数据给出可执行配装方案和缺口清单。

**要做什么**
- 设计攻略输入结构：纯文本、链接摘要、配装说明、手动补充条件
- 设计 AI 解析输出 schema：职业、子职业、星象/碎片、异域、武器、模组、属性目标
- 设计账号匹配逻辑：已拥有、缺失、可替代、属性不足、需升级、需刷取
- 设计方案生成结果：推荐组合、替代方案、缺口清单、刷取建议、操作步骤
- 与现有配装模板、仓库目标规则、装备详情联动

**会议结论** AI 辅助"抄作业"：解析攻略 → 对照账号 → 生成可操作方案，不只是文字建议。

**验收** 粘贴攻略后 AI 能结构化列出要求；对照账号给出匹配度和缺口；输出至少一个可保存配装草稿。

---

### P1 · 装备详情游戏内风格 UI

> **目标** 装备详情弹窗顶部按游戏内装备详情结构展示，下方接 DIM 式工具区。

**当前状态** 第一版已实现：顶部游戏内风格卡片 + `ItemDetailTools` 工具区。护甲能量已接入 Bungie 真实实例数据，护甲属性已拆分显示基础值、模组加成和最终值；详情弹框已从窄单列改为宽屏双列工作台，桌面端顶部按“装备信息 / 属性”横向展开，武器属性改为顶部双列；下方按“概览 Roll / 同名对比 / 社区推荐 / 操作”分区排布，武器实际 Roll 常驻展示，完整插槽候选默认折叠，窄屏回落单列；详情概览区已增加目标命中摘要，复用已导入 DIM 愿望单判断，并补充命中来源、不会自动收藏 / 加标签 / 改动装备的安全说明，以及同名对比复查路径。代码验证已通过，待真实账号 GUI 验收。历史设计见 `docs/work/archive/2026-06-23-item-detail-game-style-ui-design.md`。

**后续** 继续打磨自定义目标命中说明、同名对比和目标规则联动。

**验收** 护甲详情首屏像游戏内装备页（六维仅新属性）；工具区能力不丢失；1366px/1920px 下标题、属性条、能量、工具入口不重叠不溢出。

---

### P3 · 活动复盘增强

> **目标** 补齐 Raid Report / Destiny Tracker 方向的基础可读复盘。

**当前状态** core 层已完成活动类型分组（突袭/地牢/打击/PvP/智谋/赛季/遗失区域）、完成率统计、连续完成计数、最近 10 场时间线视图（`packages/core/src/activities/review.ts`）。桌面账号页已接入现有 `ActivityHistorySummary`，提供“活动复盘”面板，展示最近 PVE / PVP 完成情况、突袭 / 地牢摘要、最近 10 场活动和手动刷新入口。

**要做什么** 继续增加队伍成员、完成时间、状态、关键统计；后续把 core 层更完整的 `ActivityReview` 结构接入桌面 UI。

**预期** 用户不用逐条读原始记录，也能快速回忆最近活动情况。

---

### P4 · 桌面发布体验闭环

> **目标** 从可运行程序逐步走向正式桌面软件体验。

**当前状态** 安装器和自动更新第一阶段已按 NSIS 方案落地；安装目录占用提示已有 NSIS 守卫；设置页已提供应用更新、脱敏诊断导出、数据备份与迁移说明复制入口；`docs/development.md` 已记录关闭应用后复制整个数据目录的备份 / 恢复流程。

**要做什么** 用真实 GitHub Release 验证检查更新 → 下载 → 重启安装链路；评估托盘图标是否值得进入首版；后续如需要更完整的一键向导，再把当前复制说明升级为实际备份 / 恢复操作流。

**预期** 用户即使不懂开发，也能安心安装、升级、迁移和排查问题。

---

## 暂不优先

| 项目 | 原因 |
|---|---|
| 配装模板使用引导（原 Bug #3） | 已有能力但引导不足，后续补使用说明 |
| TWID 级日报/周报聚合（原 Bug #7） | 数据来源待调研 |
| DIM 整理工具文案重命名（原 Bug #9） | 语义边界需收敛 |
| 完整 DIM 级拖拽式装备移动 | 已有基础写操作，拖拽体验待完善 |
| 完整护甲属性优化器 | 有单件属性和筛选，整套求解器待研究 |
| Destiny Tracker 级排行榜 | 活动数据已有，排行视图待开发 |
| Raid Report 级完整副本复盘 | 副本级记录和历史趋势待补齐 |
| 默认内置未授权社区数据 | 授权和维护风险，暂只保留导入能力 |
| 公网服务 / 多用户共享 / 发布 | 坚持本地优先、单用户、自主授权 |

---

## 验证命令

```powershell
npx pnpm@9.15.0 docs:check
npx pnpm@9.15.0 test
npx pnpm@9.15.0 typecheck
```
