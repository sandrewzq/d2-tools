# 当前待办

> 更新时间：2026-08-31
> 本文件只保留当前任务、状态和下一步。详细范围与验收标准由对应 backlog 维护，阶段过程使用 Git 历史追溯。

## 健康度

| 检查项 | 状态 | 备注 |
|---|---|---|
| GitHub CI | ⏳ push 后异步执行 | 执行 frozen install、`pnpm test`、共享 Shell 视觉契约、UI 合同静态门禁和构建后的 `pnpm typecheck:ci`；普通 push 不等待结果 |
| Release 门禁 | ✅ 0.0.16 已发布 | 已通过 macOS 发布入口完成本地门禁、GitHub Actions、Windows NSIS 安装包和 GitHub Release |
| Agent 自动验证 | ⛔ 默认禁用 | 只有用户明确要求本地测试、构建或打包时才执行 |

## 当前任务

| 编号 | 优先级 | 状态 | 任务 | Backlog | 下一步 |
|---|---|---|---|---|---|
| T14 | P1 | ✅ 开发完成，待整体验收 | 应用配装主流程简化与逐部位属性模组 | [任务说明](work/backlog/T14-armor-planner-slot-aware-mods.md) | 护甲规划已收敛为“配装设置 / 推荐方案”两栏；五部位可分别直选自动、不装、+5 或 +10，固定数值后再选属性；优先级改为有序编辑，技能与碎片、规划方式、套装和装备范围收入更多条件；下一步在 `574 × 832`、`760px`、`980px` 和 `1280px` 实窗验收控件不溢出、普通路径无技术术语、优先顺序可见且键盘可操作 |
| T16 | P1 | ✅ 开发完成，待整体验收 | 应用更新链路重设计 | [任务说明](work/backlog/T16-application-update-flow-redesign.md) | 已修复更新 IPC 模块初始化时序；下一步使用真实 Windows 安装包验收检查、下载、重启安装、镜像回退和更新元数据 |
| T17 | P1 | ✅ 开发完成，待整体验收 | 武器详情 DIM 启发式重设计 | [任务说明](work/backlog/T17-weapon-detail-dim-informed-redesign.md) | 已完成当前 Roll 首屏、首次打开渐进加载、Perk 配置摘要、属性/获取 60/40、同版本武器操作栏、玩家文案与待应用/备注离开保护；下一步使用资料库定义、商人售卖和真实账号装备进行整体验收 |
| T19 | P1 | ✅ 开发完成，待整体验收 | 护甲详情三对象重设计 | [任务说明](work/backlog/T19-armor-detail-three-context-redesign.md) | 已完成当前装备、资料库版本、当前售卖三种对象的独立摘要、属性/配置/强化语义、渐进加载、同版本护甲栏和备注离开保护；下一步使用真实账号、资料库护甲和商人售卖进行整体验收 |
| T20 | P1 | 🟡 双 CSV 样表待确认 | 武器推荐汇总与仓库精确清理 | [任务说明](work/backlog/T20-weapon-recommendation-vault-cleanup.md) | 已删除旧 XLSX，并将星芒视界样表改为两个 UTF-8 BOM CSV：`武器列表` 为 22 列并单独展示 `aegis评级` 与 `Sayalarry获取优先级`，`Perk推荐组合` 为 14 列，明确包含枪管、弹匣、第三栏、第四栏和大师加成五项 Roll 推荐，并额外保留起源特性；当前包含 1 条武器记录和 15 条明确组合。下一步人工确认字段顺序、大师加成、Perk 池和组合内容，再生成完整推荐库与审核 CSV |

## Bug

| 编号 | 优先级 | 状态 | 问题 | 修复范围 | 下一步 |
|---|---|---|---|---|---|
| Bug #9 账号 | P1 | 🟡 已修复，待真实账号验收 | 一键最高光等把仓库“当前不在角色身上”和可替换异域冲突误判为永久不可装备；同时异域装备的专用库存 bucket 可能被归为 `other`，或 `can_equip=false` 的未知原因码被误放行，导致真实装备槽位/候选计算错误；写接口返回成功时还会提前把本地页面标成已装备，刷新后继续保留未确认的乐观状态；装备失败提示在手动刷新后仍残留 | 共享光等候选资格按 Bungie 原因码区分可恢复限制与硬阻断，原因码为 0 不再猜测放行；摘要转换在未知库存 bucket 时回退到 `equippingBlock.equipmentSlotTypeHash`，光等槽位层再从缓存中的弹药类型/装备类型恢复旧摘要；仓库装备进入转移计划，异域替换进入组合求解；确认期间禁用重复的一键提交，避免同角色新操作覆盖上一笔确认；单件和批量装备写入均只记录“请求已受理/同步中”，不立即应用装备 patch；权威 Profile 未反映时刷新会清除旧装备假状态，验证任务结束后触发真实账号刷新并追加“已确认/不可用”日志；设置页日志筛选和状态徽标区分已受理、已确认与失败；手动刷新同时清除已结束的 renderer 写入错误反馈，即使复用已有刷新请求也会清理上一轮账号读取错误 | 使用统一版本的真实账号确认“浮游孢子”等装备的请求受理、同步中、已确认、部分成功和失败状态；验证失败后页面不能继续显示已装备；两台设备对比同一实例 ID 的最终位置与光等 |
| Bug #10 每周活动 | P1 | 🟡 已修复，待真实账号验收 | 多角色活动数据按 Hash 过早去重，导致只有后续角色返回“周常突袭/地牢挑战”时，首页漏显示该轮换活动 | 先按活动 Hash 合并所有角色的挑战、修饰器和奖励，再判断轮换突袭/地牢；固定周常奖励不进入轮换卡片结果 | 使用多个角色分别存在/不存在挑战的账号数据，确认守望者尖塔、突袭活动等不会因角色顺序被漏掉 |
| Bug #11 配装 | P1 | 🟡 已修复，待实窗验收 | 装备选择抽屉的结果项没有按三行真实内容建立稳定高度，窄窗口下副信息溢出并与下一项重叠 | 抽屉限制为视口高度且只让结果区滚动；结果轨道和装备项改为按内容自然增高；图标、文本栅格、行高和窄屏动作位置独立约束 | 在约 `574 × 832`、`760px`、`980px` 和 `1280px` 宽度下打开武器与护甲选择抽屉，确认三行信息不重叠、列表独立滚动、搜索框初始聚焦和 Tab 焦点不跳动 |
| Bug #12 配装 | P1 | 🟡 已修复，待实窗验收 | 护甲规划把三列表单塞进 `276–304px` 固定侧栏，控件必然横向溢出；同时重复显示底层数据术语，并用无序复选框表达实际有顺序的属性优先级 | 改为设置与推荐方案两栏；五部位使用四段直选并按需显示属性；属性优先级使用有序列表；高级条件折叠；数据未读取只显示一条中性说明，具体部位只显示已确认冲突 | 在 `574 × 832`、`760px`、`980px` 和 `1280px` 实窗核对控件不溢出、技术术语不进入普通玩家路径、优先顺序可见且键盘可操作 |

## 说明

- UI 只维护 `packages/ui` 的共享产品实现；Markdown 合同记录稳定约束，Web 用于快速预览，Desktop 用于真实功能验收。
- UI 合同静态门禁覆盖稳定标记、表面枚举、排版、层级、菜单主题色和选中态方向线，避免菜单实现重新取得共享视觉职责。
- 新增 Bug 必须使用全局唯一编号并在本文件中登记。
