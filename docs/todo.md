# 当前待办

> 更新时间：2026-09-04
> 本文件只保留当前任务、状态和下一步。详细范围与验收标准由对应 backlog 维护，阶段过程使用 Git 历史追溯。

## 健康度

| 检查项 | 状态 | 备注 |
|---|---|---|
| 本地 CI | ✅ 2026-09-03 通过 | `pnpm ci:local` 已通过文档、编码、构建、行为测试、质量门禁、架构测试、共享 Shell 视觉契约和全量类型检查 |
| GitHub CI | ⏳ push 后异步执行 | 执行 frozen install、`pnpm test`、共享 Shell 视觉契约、UI 合同静态门禁和构建后的 `pnpm typecheck:ci`；普通 push 不等待结果 |
| Release 门禁 | ✅ 0.0.18 已发布 | 已通过 macOS 发布入口完成本地门禁、GitHub Actions、Windows NSIS 安装包和 GitHub Release |
| Agent 自动验证 | ⛔ 默认禁用 | 只有用户明确要求本地测试、构建或打包时才执行 |

## 当前任务

| 编号 | 优先级 | 状态 | 任务 | Backlog | 下一步 |
|---|---|---|---|---|---|
| T14 | P1 | ✅ 开发完成，待整体验收 | 应用配装主流程简化与逐部位属性模组 | [任务说明](work/backlog/T14-armor-planner-slot-aware-mods.md) | 护甲规划已收敛为“配装设置 / 推荐方案”两栏；五部位可分别直选自动、不装、+5 或 +10，固定数值后再选属性；优先级改为有序编辑，技能与碎片、规划方式、套装和装备范围收入更多条件；下一步在 `574 × 832`、`760px`、`980px` 和 `1280px` 实窗验收控件不溢出、普通路径无技术术语、优先顺序可见且键盘可操作 |
| T20 | P1 | 🟡 阶段 13 待开发 | 武器推荐严格语义、核心 Roll 决策、DIM 拆分与仓库本地优先工作流 | [任务说明](work/backlog/T20-weapon-recommendation-vault-cleanup.md) | 保留阶段 0–12 产品基线，重新开放数据与缓存主线：四人工来源清理到严格校验 0 阻塞；删除 `contains`/词干匹配；DIM 原生独立入库；旧人工来源 SQLite 标记 `unverified`；以 Perk 1/2 为核心状态；统一账号权威同步、中央 Store、列表批量锁定和完整可掉落池按需缓存 |
| Bug #13 | P1 | ✅ 已修复，待 0.0.18 验收 | 旧版本更新后应用进程 / 窗口不显示 | — | 配置读取恢复旧字段默认值；启动异常改为弹窗提示；单实例在窗口尚未创建时会尝试补建窗口 |
| Bug #20 | P1 | 🟡 已修复，待网络异常回归 | DIM 社区推荐在线检查网络异常时，弹窗长时间停留在“检查并读取中”，且关闭按钮与 Escape 同时被禁用，用户看起来像被卡在弹窗内 | 在线检查改为后台非阻塞；commit 检查超时 30 秒、Wishlist 下载超时 180 秒；UI 增加 210 秒兜底并提供本地文件回退提示；检查期间可继续使用其他入口或关闭弹窗，确认更新和本地导入仍保持阻塞保护 | 用断网、慢网和请求超时场景确认弹窗可关闭、其他入口可操作、检查完成后状态自动更新，当前推荐数据不被清空 |
| Bug #21 | P1 | ✅ 已修复，待旧版本升级验收 | 旧客户端升级后资料库更新缺少真实进度与可用性反馈，用户无法判断下载缓慢、索引处理中还是任务卡住 | — | 已增加流式下载量、中英文下载与索引阶段、旧库可用性和重试信息；应用更新与资料库更新现共用统一进度组件，顶部、全局 Dock、设置页和资料库页按同一状态语义展示阶段、百分比、下载量、速度及重试信息；旧索引按实际兼容性继续使用，确需重建时可复用同版本主 SQLite，最后切换期间查询自动等待恢复。下一步使用旧 schema 资料库、应用与资料库并行更新、慢网和失败重试场景实窗验收 |
| Bug #22 | P1 | ✅ 开发完成，待真实账号验收 | 游戏内移动装备后仓库页面不更新，手动刷新可能接受旧 Profile；转移、装备和配装写入后的局部状态可能被旧快照覆盖；首页强制刷新可能复用普通请求或底层 TTL 缓存 | [任务说明](work/backlog/Bug-22-account-refresh-write-consistency.md) | 已建立 Bungie Profile 版本水位线并统一轻量对账与完整刷新；装备只在真实 Profile 确认后更新，转移 pending 不会被旧快照覆盖；首页 force 会穿透全部缓存层；配装转移后先确认实例进入角色背包再装备。下一步用“浮游孢子”、游戏内手动转移、两台电脑强制同步和首页并发刷新场景验收 |
| Bug #23 | P1 | ✅ 阻止规则与回归检查已通过 | Aegis 原始刀剑大师属性 `Impact` 在 Starside 快照中曾被写成非官方字段“伤害”；导入器不能据此推断或改写为“冲击” | [任务说明](work/backlog/T20-weapon-recommendation-vault-cleanup.md) | 已用真实中文 Manifest 将一条合法“冲击”记录在内存中改为“伤害”验证：严格预览返回 1 条大师栏阻塞，不生成可导入结果；现有旧 SQLite 仍有 40 条历史“伤害”，需要按 Bug #24 的旧库兼容方案停用并重建，不能自动改写 |
| Bug #24 | P1 | 🟡 严格阻止已生效，待拆分 DIM 与清理本地来源 | 武器推荐 CSV 导入过去只检查表头、列数、重复键和数字格式，无法阻止不存在、错栏或非官方名称进入 SQLite | [任务说明](work/backlog/T20-weapon-recommendation-vault-cleanup.md) | 使用本地 Manifest `244213.26.06.29.2000-1-bnet.65864` 实测 2,382 条正式记录，共阻止 2,734 项：Aegis 529、DIM 汇总 2,094、Sayalarry 111，YXCRALLXY 与 LGpig 为 0。下一步删除普通 Perk 的双向 `contains`/唯一包含和大师词干归类；将 DIM 从普通 CSV 拆出并原生独立更新；旧 SQLite 标记为未验证并停止参与留拆结论；再清理 Aegis 与 Sayalarry 的 640 项非官方简称或错栏数据 |

## 说明

- UI 只维护 `packages/ui` 的共享产品实现；Markdown 合同记录稳定约束，Web 用于快速预览，Desktop 用于真实功能验收。
- UI 合同静态门禁覆盖稳定标记、表面枚举、排版、层级、菜单主题色和选中态方向线，避免菜单实现重新取得共享视觉职责。
- 新增 Bug 必须使用全局唯一编号并在本文件中登记。
