# 当前待办

> 更新时间：2026-07-31
> 本文件只保留当前任务、状态和下一步。详细范围与验收标准由对应 backlog 维护，阶段过程使用 Git 历史追溯。

## 健康度

| 检查项 | 状态 | 备注 |
|---|---|---|
| GitHub CI | ⏳ push 后异步执行 | 执行 frozen install、`pnpm test`、共享 Shell 视觉契约和 `pnpm typecheck`；普通 push 不等待结果 |
| Release 门禁 | ⏳ 发版时执行 | 必须通过 `tools\git-auto-release.cmd` 完成本地门禁、GitHub Actions、安装包和 GitHub Release |
| Agent 自动验证 | ⛔ 默认禁用 | 只有用户明确要求本地测试、构建或打包时才执行 |

## 当前任务

| 编号 | 优先级 | 状态 | 任务 | Backlog | 下一步 |
|---|---|---|---|---|---|
| T1 | P1 | 🟡 待验证 | 本地配装工作台、护甲优化与 DIM 导入 | [任务说明](work/backlog/T1-loadout-plans-and-guide-import.md) | 执行用户要求的本地验证或 CI 验证；通过前不得标记完成 |
| T3 | P1 | 🟡 待验收 | 商人结构、覆盖与官方获取来源 | [任务说明](work/backlog/T3-vendors-and-drop-sources.md) | 验收真实地点覆盖、跨重置边界、Offer 时效、去重和机灵模组 A/B |
| T8 | P1 | 🟠 进行中 | 全应用共享 UI 与视觉收口 | [任务说明](work/backlog/T8-full-application-ui-functional-convergence.md) | 重启 Desktop 主进程并刷新首页，确认铁旗与“本周加成”同行等高，且只显示玩法、倒计时、当前角色挑战和直接奖励；随后验收萨拉丁库存入口及 `light / dark × 1280 / 980 / 760` |

## Bug 列表

| 编号 | 状态 | 问题 | 范围与处理 | 验证 |
|---|---|---|---|---|
| Bug #14（全局倒计时） | 🟡 修复待验证 | 动态倒计时与普通事实值、成功状态共用颜色，首页绝对更新时间也未靠右对齐，时间层级不清晰 | 新增跨应用 `countdown` 文字语义和独立珊瑚红 `--time-accent`，浅色与深色分别提供高对比 token；首页每日、每周、仄和铁旗倒计时统一接入，绝对更新时间在刷新矩阵中靠右；商人重置日期等绝对时间保持普通事实值颜色；同步冻结原型与全局视觉合同 | 在浅色与深色首页确认三个更新时间靠右，三个刷新倒计时及铁旗倒计时使用同一珊瑚红且能与错误红、状态绿、警告黄和 AI 紫区分；检查窄布局文字不溢出，其他绝对时间未被误染色 |
| Bug #13（首页核心活动） | 🟡 修复待验证 | 日落、轮换突袭和轮换地牢卡片被整卡强制等高规则拉伸，产生大量无信息空白；完全按内容自适应后，不同奖励数量又让活动条目行高参差，视觉不整齐 | 保留三张独立活动卡片和桌面同排结构，移除所有卡片共同参与的内容拉伸；桌面端每个活动条目统一为 `98px` 固定行高，描述限制一行，奖励在固定行内自动并排；仅单条目的日落卡拉伸外框，与同排双条目卡片底边对齐，内容在拉高区域内垂直居中；`760px` 以下恢复内容自适应高度；同步更新冻结原型与首页工作区合同 | 在 Desktop 首页确认三张活动卡片仍同排且底边对齐，每个活动条目行高一致，日落内容在拉高区域内垂直居中且没有被拉伸；确认铁旗、本周加成和周末商人仍能较早进入首屏，再检查 `980px` 与 `760px` 窄布局没有横向溢出 |
| Bug #12（首页铁旗） | 🟡 修复待验证 | 铁旗已开放时仍显示“状态待确认”，且首页铁旗卡片过大，混入了低价值的库存计数、具体装备和通用熔炉概率掉落；“当前角色 0/3 2 个角色可完成”还把当前角色进度与账号角色范围混成一句 | 当前开放玩法以通用模式 `88` 返回，活动定义描述包含萨拉丁/铁旗语义；识别逻辑已补齐语义判断并优先选择带周挑战的玩法，萨拉丁 Vendor 已就绪时作为开放状态兜底。首页铁旗已改为与“本周加成”同行的紧凑信号卡，只显示玩法、开放倒计时、当前角色进度和非条件直接奖励；进度标签改为“当前角色挑战”，账号范围改为“账号内 N 个角色可参与”；库存、聚焦装备、价格和购买条件留在商人页 | 重启 Desktop 主进程使旧的 `version: 1` 首页缓存失效，强刷后确认铁旗与本周加成同行等高，显示当前玩法、挑战进度和直接奖励；确认“当前角色挑战 0/3”和“账号内 N 个角色可参与”语义分离，且不再出现零库存、具体装备或条件概率掉落 |
| Bug #11（武器详情） | 🟡 修复待验证 | 武器详情漏映射辅助瞄准、后坐方向、空中效率和弹药生成，且将空中效率错当后坐方向；账号实例详情未返回武器属性时会覆盖仓库快照；来源区未展示实时未命中状态，且账号实例 Hash 与商人售卖别名 Hash 不同时会漏掉真实 Offer；发布解析只支持 `season`，导致年度资料片、DLC 和核心池武器显示无法解析；同名 Hash 和 Manifest 定义数组索引被误称为玩家版本，账号实例还错误允许切换到其他同名定义；同名实例 Perk 图标因名称反查失败显示为空白；透明白色 Bungie 定义图标依赖局部浅色主题选择器，页面或主题宿主变化后会再次变成白块；固定配置异域被当作普通随机武器显示完整掉落池、随机 Roll 目标和普通杰作语义；异域催化插头的 `traitIds` 在账号投影中丢失，且详情未请求 `ProfileRecords(900)`，导致催化图标、真实目标进度和完成状态无法显示 | 使用官方 `DestinyStatDefinition` Hash 统一解析属性并保留账号快照兜底；实时来源按 Offer 去重并传递价格、刷新时间、角色范围、购买条件和官方失败原因，通过 `translationBlock.weaponPatternHash` 将同一发布版本的 Vendor 别名归并到当前实例，静态来源缺失时明确显示官方未标注；发布版本注册表区分 `season`、`annual`、`dlc`、`core` 和普通更新，并将 `v900/v910` 关联第27赛季“赛季：溯回”、`v950/v960/v970` 关联第28赛季“凯旋纪念碑”；标题区只显示玩家发布版本，账号实例和商人 Offer 固定显示自身版本，仅资料库定义允许切换同名版本；`quality.currentVersion`、水印和光等上限仅保留在折叠技术详情；同名实例直接传递 Profile Perk 图标；武器 Perk、实例 Perk、护甲能力与插槽、套装效果及资料库 Perk 统一使用 `game-definition-icon` 固定深色底板，不再依赖主题宿主选择器或页面局部颜色补丁；保留 Manifest 插槽来源并统一分类固定、随机和可切换配置，固定异域只显示固定 Perk、催化剂、异域杰作和来源，特殊可变异域使用独立候选语义，DIM、社区、个人知识与 AI 不再对固定异域执行随机 Roll 命中；账号详情并行读取 `ProfileRecords(900)`，只加载 `recordTypeName` 明确为异域催化的轻量 Record 定义，使用官方 `item.exotic_catalyst` trait 和本地化名称关联催化插头、Record 目标与图标；无法可靠关联定义或账号目标时不显示催化区域，也不再回退到普通大师杰作或物品目标 | 分别打开“海盗之怒”“谜团”“无时辩解”“圣爱”“不胜即亡”“加时交锋”，确认赛季、年度资料片、内容包和核心内容版本均按类型显示，其中 `v970.core` 显示“第8年 · 第28赛季 · 凯旋纪念碑 · 核心内容更新”；主信息区不再出现 `#1 · 当前 Hash` 或 `Manifest 定义版本 1/1`，账号实例和商人 Offer 不显示版本下拉；确认“加时交锋”账号实例 Hash `2624561525` 能命中萨瓦拉售卖别名 Hash `3276118833`；确认反勇士、折叠技术详情、实例上下文、同名实例 Perk 图标、官方获取来源和实时来源状态完整；在浅色与深色主题分别确认武器、护甲和资料库的 Bungie 定义图标保持深色底板且白色图形清晰；再查看同一商人的多条在售路径，确认报价、刷新和不可购买原因均完整显示；打开固定异域的资料库、商人 Offer 和账号实例，确认没有完整掉落池、随机 Perk 筛选或远程切换，催化剂与来源状态完整，三个目标来源只保留说明和收藏记录；使用“战狮”等已拥有异域确认催化图标、每项目标和总进度来自 Bungie ProfileRecords，完成对象显示 100%，无法获取可靠 Record 的对象不显示空催化卡片或“定义未返回”占位；再打开特殊可变异域，确认其候选不会被命名为普通传说武器掉落池 |
| Bug #9（T8 护甲详情） | 🟡 修复待验证 | 护甲档案用人工 `traitIds` 映射赛季、用同名搜索序号表示版本，且将对象上下文替代套装或固有能力；实时 Offer 丢弃价格、刷新和购买状态 | 已改为读取 Bungie Manifest 的赛季 Hash、发布版本类型、套装 Hash、当前定义版本和版本水印，并补齐解析字段的空值守卫；共享发布注册表区分赛季、年度资料片、DLC、核心内容和普通更新，不再向玩家暴露内部 release trait；套装效果读取 `DestinyEquipableItemSetDefinition.setPerks` 并关联 `DestinySandboxPerkDefinition`，显示每档所需件数、名称、图标与说明；资料库 schema 已升至 `4`，旧激活会重建以补齐赛季与套装定义；护甲详情保留实时 Offer 的价格、刷新、条件和失败原因 | 完成资料库更新后，使用真实 Manifest 打开赛季 27 护甲、年度资料片护甲和没有 `seasonHash` 的当前护甲，确认发布版本、套装、各档效果、定义版本和当前水印与官方数据一致；再确认角色库存 Offer 显示价格、刷新和购买状态，并验收 light / dark × 1280 / 980 / 760 |
| Bug #8（配装） | ✅ 已验证 | 打开配装页时抛出 `row is not defined`，导致 React 卸载页面并显示黑屏 | 已将游戏内配装物品行中遗留的未定义变量改为 `props.row` | 2026-07-30 通过 Desktop 开发入口刷新后点击“配装”复现并验证修复；未运行本地自动化测试 |
| Bug #10（仓库 UI） | 🟡 修复待验证 | 筛选结果区的装备卡被工作台 CSS 覆盖为单列连续列表，且回退基础卡片后出现过大留白，偏离冻结原型的紧凑平铺网格 | 已将单列覆盖限制在清理工作台；筛选结果恢复桌面三列、1280 两列、980 以下单列，以及 62px 最小高度和紧凑信息密度 | 在 Desktop 中打开仓库的“筛选列表”，确认装备平铺、断点列数、卡片密度与详情打开操作正常 |

## 说明

- T1 实现已接入，待验证：配装菜单通过同一本地方案工作台处理当前装备、DIM、攻略、护甲求解和安全应用；游戏内槽位保持 Bungie 官方对象与写操作边界。
- T8 负责已经迁入 `packages/ui` 的共享视觉系统和页面收口；铁旗已作为首页固定区域接入真实 CharacterActivities、VisibleRewards 和 Character Vendors，并通过首页缓存版本升级失效旧数据，等待 Desktop 实窗验收。
- React Prototype 已退役；后续视觉链路统一为冻结 HTML 规格、Web 快速预览和 Desktop 真实功能验收。
- 新增 Bug 必须使用全局唯一编号并在本文件中登记。
