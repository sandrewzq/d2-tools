# 当前待办

> 更新时间：2026-07-28
> 这里只保留当前健康度、未完成任务和未解决 bug。已完成 Bug 与阶段过程使用 Git 历史追溯；详细目标、切片和验收标准在 `docs/work/backlog/`。

## 健康度

| 检查项 | 状态 | 备注 |
|---|---|---|
| GitHub CI | ⏳ push 后异步执行 | 执行 frozen install、`pnpm test`、共享 Shell 原型计算样式契约和 `pnpm typecheck`，并上传 Shell 截图；agent 不等待普通 push 的 CI |
| Release 门禁 | ⏳ 发版时执行 | `tools\git-auto-release.cmd` 必须等待本地门禁和 GitHub Release 全部成功 |
| Agent 自动验证 | ⛔ 默认禁用 | 普通开发不自动运行；用户主动本地测试或打包时照常运行现有测试和类型检查 |

构建提醒：Prototype / Web 生产构建的 chunk 大小提示和 Vite CJS Node API 弃用提示当前不阻断验证。

## 当前任务

| 编号 | 优先级 | 状态 | 任务 | Backlog | 下一步 |
|---|---|---|---|---|---|
| T1 | P1 | 🟠 进行中 | 配装方案与攻略导入 | [配装方案与攻略导入](work/backlog/T1-loadout-plans-and-guide-import.md) | 配装共享页面当前切片已按冻结原型重建并保留旧模板真实操作；下一步接入新本地方案模型、批量应用和 DIM 配装分享链接导入 |
| T2 | P1 | 🟡 待推进 | 仓库推荐与清理工作台 | [仓库推荐与清理工作台](work/backlog/T2-vault-recommendation-and-cleanup-workbench.md) | 统一 DIM wishlist、本地目标、同名对比和清理清单 |
| T3 | P1 | 🟡 待验收 | 商人结构、覆盖与官方获取来源 | [商人结构、覆盖与官方获取来源](work/backlog/T3-vendors-and-drop-sources.md) | 已接入 Vendor Group / 目的地两级目录、持久缓存、启动预热和周期刷新，商人共享 UI 当前切片也已按冻结原型重建；下一步验收真实地点覆盖、跨重置边界、Offer 时效、去重和机灵模组 A/B |
| T4 | P3 | 🟡 待推进 | 活动复盘增强 | [活动复盘增强](work/backlog/T4-activity-review-enhancement.md) | 接 PGCR、完成时间推算和副本级趋势 |
| T7 | P1 | 🟡 待推进 | 社区推荐边界收口 | [社区推荐边界收口](work/backlog/T7-architecture-boundary-hardening.md) | Core、Services、App、UI 与平台壳的主边界已完成；下一步将本地社区表、个人知识及 AI/light.gg 缓存和外部调用迁入 Services，Core 只保留规则、DTO、解析和匹配。 |
| T8 | P1 | 🟠 正在迁入共享 UI | 全应用 UI 还原 | [全应用 UI 还原](work/backlog/T8-full-application-ui-functional-convergence.md) | 共享 Shell、设置菜单、统一武器与护甲详情、账号页、资料库页、仓库页、配装页及商人页均已迁入共享 `packages/ui`，当前全部待视觉验收。商人已恢复真实地点目录、纵向库存章节、Offer 对象卡、角色状态和右侧核对。下一步先验收已迁菜单，再迁移首页菜单。 |

原型视觉复查已收口：桌面顶部状态栏保持完整显示并恢复为连续直角状态组，375px 顶部状态改为可展开的连续状态列表，首页刷新信息消除横向裁切，AI 窄屏抽屉接管完整工作区宽度，折叠菜单保留无障碍名称；三份原型默认显示完整状态控制器，仅在追加 `?controls=0` 时用于无控件截图；护甲定义无固定属性时改为紧凑提示，获取来源占满阅读宽度。武器与护甲详情状态框的左侧强调线已改为统一语义映射：加载/进行中使用蓝色 `4px`，部分可用/禁用使用黄色 `4px`，失败使用红色 `4px`，中性空态保留普通对象边框。共享 Shell 代码已按该冻结基准迁入，等待规定视口和主题下的视觉验收。

设置页共享 UI 代码已按八个分区完成迁移：目录使用 `240px / 210px / 190px / 单列` 连续导航，并保持透明以承接页面内容底色，不复用一级主侧栏背景；冻结 HTML 的动态目录也已删除旧副说明，恢复八项单行导航，避免副说明、分隔线和下一项标题挤压。状态摘要使用方形 `status-matrix`，设置项使用 `282px / 260px / 单列` 右侧控制轨，版本记录与操作日志使用连续行分隔。冻结 HTML 与共享 UI 均已区分 `fact / status`：日期、版本、数量等事实值不再跟随容器状态染色，只有明确状态词使用语义色；设置命令统一通过 `SettingsButton` 输出 `34px` 高、`144px` 宽，Primary / Secondary / Danger 只改变颜色。设置页字段只覆盖 `background-color`，Select 保留全局下拉箭头和 `32px` 右侧箭头空间；账号、资料库、应用更新和操作日志时间统一为本地时区 `YYYY/MM/DD HH:mm:ss`。Bungie API Key 已遮罩，帮助框恢复普通对象边框，Desktop / Web / Prototype 不再向设置页头注入重复的“复制脱敏诊断”。当前仍待 Desktop 实窗视觉验收，不能标记为完成。

统一武器详情已完成第二轮截图对齐：商人菜单不再越界覆盖共享档案 Header，关闭按钮恢复到工具栏右侧；对象上下文改为连续账本，章节导航使用冻结原型最终级联的 `34px` 分段控件；当前 Hash 固定排为 `#1`，版本主值不再误用武器名；摘要事实区移除重复稀有度，浅色主题配置区 Perk 图标恢复深色承载底。冻结 HTML 的同名实例 Perk 缩略图也已补齐同一深色承载规则，避免透明白色图标融入浅色实例卡片；AI 主命令恢复紫底白字，禁用命令改为不透明中性灰状态，避免 Typography 颜色覆盖和全局透明度叠加导致低对比度。实例操作栏已迁入共享 `packages/ui` 结构，Desktop 仅绑定真实的锁定、装备、转移、复制计划、标签、备注、分享和配装草稿回调。当前仍待 Desktop 实窗视觉验收，不标记完成。

装备详情打开链路已移除通用全屏加载中间页：首页商人详情在已知类型时直接挂载武器或护甲框架，普通详情也仅在预览已确认类型时立即打开对应框架；类型未知的条目继续保留列表项加载反馈，等待定义确认后再挂载正确详情，读取失败时才回退到普通详情。当前仍待 Desktop 实窗验收，不标记完成。

账号页共享 UI 已按冻结原型完成代码迁移：账号目录改为完整 `tablist / tabpanel`，支持方向键、Home 和 End 切换；角色改为独立 `ContextSwitcher`，不再误作页面导航；装备区恢复武器、护甲、装备、其他四类连续分区，并按位置分别显示当前装备和背包候选，保留每个位置的数量、展开、配装引用、详情打开和进行中状态。已有快照刷新时保留类别与位置结构，两列分别显示紧凑骨架，不再用整页遮罩。活动、材料、邮政官继续绑定真实 ViewModel，补齐读取失败、无活动、无材料、邮政官为空、未配置、未登录、读取中、写操作关闭和操作进行中状态；Shell 顶部的刷新账号与重新授权保持原有真实接线。本轮只改 `packages/ui` 账号内容层、账号文案和 `.account-*` 样式，未改共享 Shell。当前仍待 Desktop 实窗视觉验收，不标记完成。

资料库页共享 UI 已按冻结原型完成代码迁移：顶部新增激活版本、最新版本、资料库状态与完整性四格直角摘要，数据来自真实 Manifest 状态，不生成激活时间、定义数量或其他当前契约没有提供的字段；装备与 Perk / 框架模式改为完整 `tablist / tabpanel`，支持方向键、Home 和 End 切换。左侧查询栏恢复主搜索、清空、当前模式筛选、真实本地历史和低频别名管理，原有框架、来源状态、Perk 池筛选收进高级筛选而未删除；右侧改为连续平面结果行，装备继续保留版本、来源、公开渠道和账号持有展开，以及详情、收藏、取消收藏和仓库定位 action，Perk 继续显示真实关联装备并保留收藏操作。未搜索、搜索中、失败、无结果、Manifest 异常、实时来源读取中 / 失败和窄窗口单列状态均已落入唯一共享页面。当前仍待 Desktop 实窗视觉验收，不标记完成。

仓库页共享 UI 已按冻结原型完成代码迁移：筛选列表、清理工作台、同名对比和推荐数据改为完整 `tablist / tabpanel`，支持方向键、Home 和 End 切换；筛选区改为全宽工作台，保留分类、槽位、弹药、锁定、本地标签、排序、多框架和护甲多属性交集条件，装备与其他分类不再被错误重置为全部。结果区使用连续平面行，并继续显示玩家标签、整理决策、DIM Wishlist、本地目标、社区推荐、配装引用、Perk、备注与详情打开状态。清理工作台只消费玩家明确标记的可清理装备，批量选择、标签、解锁、转移和复制清单均基于真实候选；同名对比、推荐导入、本地目标规则和大列表分段加载保持原 action。当前仍待 Desktop 实窗视觉验收，不标记完成。

配装页共享 UI 已按冻结原型完成当前切片迁移：删除内容层重复页头，角色切换恢复为 `ContextSwitcher` 和 `aria-pressed`；游戏内 / 本地模式使用连续分段控件与完整 `tablist / tabpanel`，支持方向键、Home 和 End；三栏工作区取消完整外框，只保留目录与摘要相邻分隔线，目录当前项不再使用左侧蓝条。游戏内主目录只显示有内容槽位，保存当前装备可选择当前角色全部真实 Bungie 槽位；装备实例按实例 ID 投影账号位置，未定位时不使用同名物品替代。本地方案继续保留创建、重命名、删除、缺失分析、转移计划、逐件转移 / 装备、来源查看和方案对比；DIM 导入尚未接通时只显示明确提示，不创建空方案。当前仍待 Desktop 实窗视觉验收，不标记完成。

商人页共享 UI 已按冻结原型完成当前切片迁移：地点和商人目录改为连续文字行，使用稳定商人 ID、`aria-current` 与真实 `data-status`，不再显示头像或首字母占位；详情恢复地点、商人身份、来源、系统时区刷新边界、可见 Offer 数、角色/护甲师模组状态和右侧核对。库存、子库存、任务、声望与等级奖励按纵向章节排列，章节内 Offer 统一为五列 / 三列 / 两列 / 单列响应式对象卡，并显示真实图标或缺图态、名称、类型、摘要、当前可确认配置、成本、数量、购买状态和失败原因。材料、任务与其他非装备 Offer 保持库存信息语义，武器和护甲继续绑定统一详情回调；详情内重复刷新按钮已移除，使用共享页头真实刷新命令。当前仍待 Desktop 实窗视觉验收，不标记完成。

全局时间合同已补齐：绝对时间统一保存 UTC ISO，显示时跟随当前系统时区；日期版本不做时区转换；完整、普通、Shell 紧凑和周期边界时间分别使用固定精度。全应用冻结原型已移除 `Asia/Shanghai`、中国时区标签、原始 ISO 直出、商人预格式化刷新文案和静态倒计时，首页、Shell、账号、资料库、商人和设置共用本地时间规则；武器详情中的 Vendor UTC 周期也改为当地日期、星期和时间。共享 UI 已新增统一时间格式模块，并接入设置后台任务、首页重置周期、商人刷新周期和设置精确时间；商人 workspace 改为传递原始 `resetAt / updatedAt / nextResetAt`，旧 Label 仅作兼容回退。资料库当前只有日期版本字段，继续按日期合同显示；护甲详情当前没有具体时间字段，不需要额外分支。账号与装备详情剩余局部时间格式后续随对应菜单迁移收口。

圆角合同已冻结：Shell、工作区、列表、摘要矩阵、摘要框与状态框使用直角；顶部 Shell 状态项组成连续边框，仅整体首尾为 4px；独立对象卡为 6px；控件与缩略图为 4px；只有短标签、计数与进度使用胶囊。原型 CSS 的历史 `3/5/9/99px` 圆角已收口到语义 token；下一步进行视觉验收并按同一合同迁入共享 UI。

AI 辅助栏的共享产品实现已重建为对话/任务双视图：宽屏作为停靠辅助栏，窄屏作为完整 Drawer，并补齐遮罩、背景隔离、焦点限制、`Escape` 和焦点恢复；抽屉头部已统一为 `64px`，设置与关闭使用 quiet 图标按钮，默认透明且仅在 hover / focus 时显示控件反馈，页签、工具栏、消息列表和底部输入区已按冻结原型改为连续结构。任务页已纠正为 AI 任务、攻略解析、账号对照与配装草稿，不再注入应用后台任务或使用后台任务数量作为页签角标；应用后台任务继续在设置菜单的诊断与操作日志分区管理。顶部和左侧 AI 入口均按整个抽屉的开关状态切换，无论当前处于对话或任务视图，再次点击均关闭；冻结 HTML 的窄屏背景隔离不再禁用 topbar。对话工具栏已恢复“新会话 / 会话记录 + 上下文状态入口”，空会话恢复两条紧凑消息，真实快捷问题收进折叠入口；底部输入区恢复为文本框与 `76px` 发送按钮的直接两列结构。下一步对深浅主题和 `980px` 临界宽度做视觉验收。

窗口关闭按钮保持 quiet 默认态，仅在 hover / focus-visible 时使用红色关闭反馈；Danger 按钮视觉只用于页面内明确的破坏性命令，不能污染窗口控制。

## 验证入口

- 普通 push：GitHub CI 异步执行，不在本地重复运行。
- Release：运行 `tools\git-auto-release.cmd` 并等待全部步骤成功。
- 专项排查：仅在用户明确指定时运行对应命令。
