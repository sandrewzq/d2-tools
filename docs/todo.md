# 当前待办

> 更新时间：2026-09-19
> 本文件只保留**尚未关闭的任务**：一行说清「还要做什么、等谁」。
> 范围、验收标准与自验记录一律在对应 backlog；已完成的 Bug 与任务直接从本表删除、不留归档行，追溯用 Git 历史与 backlog。

## 当前任务

| 编号 | 优先级 | 状态 | 任务 | Backlog | 下一步 |
|---|---|---|---|---|---|
| T1 | P1 | 📝 候选方案，待排期 | 全应用英文国际化补齐 | [任务说明](work/backlog/T1-english-internationalization.md) | 先页面标题与共享弹窗，再仓库、装备详情、配装、AI 助手 |
| T23 | P2 | ⏸️ 等待官方书面授权 | D2Checkpoint 进度机器人集成 | [执行 backlog](work/backlog/T23-d2checkpoint-integration.md) | 2026-09-08 已发授权申请；收到回复前不进入开发 |
| T43 | P1 | 🟠 待真实账号验收 | 任务待处理中心 | [任务说明](work/backlog/T43-pursuit-attention-center.md) | 三角色任务、赏金、角色目标与赛季挑战的数量、进度、追踪、到期状态 |
| T44 | P1 | 🟠 待真实账号验收 | 光等提升路线 | [任务说明](work/backlog/T44-character-progression-center.md) | 周挑战奖励按 `displayRewards` 读取；核对三角色「本周可做 / 无可领取」 |
| T45 | P1 | 🟠 待真实账号验收 | 本周刷取清单 | [任务说明](work/backlog/T45-weekly-farming-checklist.md) | 核对轮换活动、装备来源、T20 推荐与四类图样状态 |
| T56 | P1 | 🟢 代码已完成、两组判据全绿；剩余 Bug #80–#87 人工复验 | 仓库推荐：统一模型与来源去分叉 | [任务说明](work/backlog/T56-unified-recommendation-model.md) | ① **人工推荐表格要重新导入一次**（DIM 文本不用）：切换后旧表示已不再读写，不重导就看不到那份来源的内容——这是既定代价，不是缺陷；② 重启桌面端点一遍七个子项：排序入口、位置筛选、筛分区顺序、框架下拉、槽位图标、来源页混排与对齐、验收报告归入诊断（其中「验收报告」当时标记为「有实际需要时再排期」，属主动押后、未做完） |
| T58 | P1 | 🟠 待真实账号验收 | 游戏内配装完整子职业配置 | [任务说明](work/backlog/T58-ingame-loadout-subclass-fidelity.md) | 按超能/技能、星象、碎片展示并保留原始 socket 映射 |
| T62 | P2 | 📝 待拍板（A / B / C 三选一） | 护甲详情目标来源去分叉 | [任务说明](work/backlog/T62-armor-detail-target-source-defork.md) | T56 同构残留：三页签里 `配装与攻略` / `社区来源` 恒空，来源名 `"我的推荐"` 是硬编码且与 personal knowledge 撞名。处置待定：**A** 纯去分叉（建议）/ **B** 对齐武器侧再拆区块 / **C** 只删死页签 |
| T68 | P1 | 🟢 代码已改并自验通过，待用户复验 | 本地文件导入的预览与起名搬进弹框 | [任务说明](work/backlog/T68-file-import-naming-in-dialog.md) | 四条导入路的确认卡一律在弹框内；「导入文本文件」先开框、框里再选文件 |
| T69 | P1 | 🟢 代码已改并自验通过，待用户复验 | Perk 条目统一样式（本件 Roll / 固有能力 / 完整掉落池 对齐推荐区那套） | [任务说明](work/backlog/T69-perk-cell-style-unification.md) | 一个条目组件两处共用（`WeaponPerkEntry`）；换 Perk 交互未动（之后单独重构）；`unresolved_in_definition_pool` 仍无落点，单列待办 |
| T70 | P1 | 🟢 代码已改并自验通过，待用户复验 | Perk 卡片三处收尾（首屏固有能力、占位几何、当前已选的辨识度） | [任务说明](work/backlog/T70-perk-cell-followups.md) | 实窗复验 `light / dark × 1280 / 980 / 760`；三项已改完，实窗报的「打开白屏 / 一直在闪」已修。dev 窗口刷新即可，不必重新构建。未跑本地自动化验证 |
| T71 | P1 | 🟢 代码已改并自验通过，待用户复验 | 武器详情三区重排（推荐 Roll → 本件 Roll → 完整掉落池） | [任务说明](work/backlog/T71-weapon-detail-region-order.md) | dev 窗口刷新后复验三区顺序与首屏定位。这条改掉了 `equipment-details.md` 原有的「当前配置在前」和推荐证据读取时机口径。未跑本地自动化验证 |
| T72 | P1 | ✅ 代码已改完，待用户验收（2026-09-17 拍板 A+B+C） | 推荐对照区排布：卡片宽度与两列对齐 | [分析说明](work/backlog/T72-perk-comparison-layout.md) | 实窗验收等宽列网格、两侧顶对齐、栏头合并并吸附、≤760 两半堆叠。自验脚本 `.local-data/tmp/t72-perk-layout/verify-product.mjs`，8 档宽度 430 项全过。未跑本地自动化验证 |
| T73 | P1 | ✅ 代码已改完（含 09-18「无法切换」修复），待用户验收（2026-09-17 拍板 m2 + 换 Perk 进推荐区） | Perk「当前启用」辨识度 + 换 Perk 入口 | [分析说明](work/backlog/T73-perk-active-signal-and-swap-entry.md) | dev 窗口刷新后复验蓝环辨识度与浮层里的换 Perk 入口；09-18 报的「无法切换」已修，判据固化在 `weapon-detail-view.test.ts` 两条用例。未跑本地自动化验证 |
| T74 | P1 | 🟢 代码已改完，待实窗复验（2026-09-18 拍板 A） | 首页轮换活动「掉落池」的覆盖与数据来源 | [分析说明](work/backlog/T74-rotation-loot-pool-coverage.md) | 重启 dev 后看首页四张卡的掉落池是否有具体武器名。数据集已从 4 条扩到 25 个活动 / 171 条，生成脚本 `scripts/generate-activity-loot.mjs`；三条实测结论见 backlog。未跑本地自动化验证 |
| T75 | P1 | 📝 诊断已完成，候选修法待拍板（A / B / C） | 启动黑屏：窗口先显示 + 页面底色在 shell 之外仍是深色 | [分析说明](work/backlog/T75-startup-black-flash.md) | 机制已查清（黑是页面画的、不是 `backgroundColor`；亮色主题下启动画面一样是黑的），等你在 A / B / C 三个修法里拍板。真实会话时长未测 |
| T77 | P0 | 🟠 代码已改完（含 09-18 实窗复验后的 T78、T80 两轮修复）；未跑本地自动化，待重新构建后实窗复验 | 换 Perk：写入受理即确认，写后读回降级为后台静默校对 | [分析说明](work/backlog/T77-perk-write-acceptance.md) | 重启 dev 脚本后按 backlog §5.2 复验。核心是写入受理即落地并立即返回，读回改成后台静默 probe（台阶 `[0, 5, 15, 30, 60, 90]` 秒），删掉 `refresh-error` 面板态。未跑本地测试 / 类型检查 / 构建 |
| T79 | P1 | 📝 待排期 | 插槽状态收敛成一份派生结果（现在是四份并行表示） | [任务说明](work/backlog/T79-socket-state-single-source.md) | 换 Perk 一次要同时改 `sockets[].selected_plug`、各槽 `reusable_plugs[].selected`、派生的 `socket_plugs[]`、`weapon_roll` 的当前项/`owned_plugs[].selected`/指纹 —— T78 的「两个都变启用了」就是漏掉其中一份造成的。写路径靠手工逐份改、读方各挑一份读，且**没有一处能判出谁对**。目标是当前配置只留一处真源（倾向 `sockets`），`socket_plugs` 与 `weapon_roll` 的当前项都改成派生，让「漏一份」在结构上不可能，而不是再加一条守卫去钉。属重构、不改行为，需先清点读侧消费点并准备行为等价判据；排在 T77 实窗复验通过、行为确认稳定之后 |
| T80 | P0 | 🟠 代码已改完；未跑本地自动化，待重启 dev 脚本后实窗复验 | 换 Perk 的**执行路径**：主进程退出裁判席、写响应体当旁证、一次只飞一个写 | [分析说明](work/backlog/T80-perk-write-execution-path.md) | 重启 dev 脚本后实窗复验。主进程裁判席已删、写响应体当旁证、一次只飞一个写；`deferred` 中性档与 `ErrorCode 1634` 中文提示一并落地。与 DIM 有意分歧：显示按意图，响应体只负责暴露「受理但被静默拒绝」。未跑本地自动化验证 |
| T81 | P2 | 🟢 跳转与定位已实现，待实窗复验；标记待做（2026-09-18 拍板分两步） | 首页掉落池条目跳进本周刷取 | [任务说明](work/backlog/T81-home-loot-pool-to-farming.md) | 重启 dev 后点首页轮换突袭 / 地牢卡片里的「查看本周刷取」入口，看是否切到账号页对应活动。入口只对数据集覆盖到的活动出现；标记（已持有 / 推荐）留待下一轮，需把 T20 推荐接进首页简报。未跑本地自动化验证 |
| T82 | P2 | 📝 待排期 | 首页简报的定义读取失败会抛穿，且与「数据集未覆盖」不可区分 | [任务说明](work/backlog/T82-home-briefing-loot-definition-failure.md) | `homeBriefing.ts:132` 的 `getDefinitions` 未包 `try/catch`，掉落池定义读取失败抛穿的是整个首页简报；两种失败又落在同一个空态 |
| T83 | P1 | 🟠 代码已改完；未跑本地自动化，待实窗复验 | 武器详情的推荐区按对象身份分事实层 / 规则层 | [合同](work/references/ui-specs/equipment-details.md) | 资料库定义和商人 Offer 没有「本件」，而旧的单条渲染路径靠 `requirement_state` / `instance_owned` 有没有值猜自己拿到了哪一层，于是给没有本件的东西画了「来源要求 ｜ 本件拥有」对照表：第二列结构性为空，还写出「本件没有这个推荐项」。**已改完**：① `WeaponDetailContent` 按 `model.context.kind === "account_instance"` 分流成两个渲染器，账号实例保持原证据卡不动，定义 / 商人走新的规则层（一个来源一张卡，卡内一个栏位一张子卡，复用 `WeaponPerkEntry` 与既有等宽列模板）；② 两列对照组件 `RecommendationSlotComparison` 只留给账号实例，规则层连引用都没有（架构守卫已钉）；③ 整份推荐集的免责声明从逐卡重复改成推荐区顶部说一次，来源自己写的说明逐卡显示；④ 删掉 `WeaponRecommendation` 上没人填也没人读的候选字段（`match` / `match_notes` / `masterwork_names` / `mod_names` / `requirement_state` / `matched_*` / `instance_owned`）与随之失效的 CSS。**与拍板口径的一处偏差**：`presentation: "combo"` 全仓没有生产者，因此没有单独做「一组一卡」的组合布局，两种 `presentation` 共用逐栏子卡，由徽标显示「候选池」/「完整组合」区分。**未跑**本地自动化验证。 |
| T84 | P1 | 🟠 代码已改完；未跑本地自动化，待实窗复验 | 账号实例推荐对照：卡级信号跟卡片自己的事实走，环恢复常蓝 | [合同](work/references/ui-specs/equipment-details.md) | 实窗看到「当前启用」在同一栏里有一蓝一红两种样子。根因是「本件当前装着、来源没要它」这枚叉额外挂了一道「这一栏符合 / 不符」的门控（`mismatch` 取 `state === "different"`）：同一个事实在「符合」栏里静默、在「不符」栏里又变红又打叉。而「符合」说的是你**拥有**来源要的那项（哪怕没装），不等于你装的就是它——拿栏位结论去决定卡片长什么样，就把一条事实画成了两种。**已改完**：① 判据只读卡片自己的事实（`!hit && active`，去掉 `state` 门控），叉在「符合」栏里也照画；② 环不再跟着染红，颜色只表示「当前启用」，红字只作加强，形状（叉）才是那条不许丢的通道；③ 两列表头图例同步写成「环＝当前启用，叉＝来源没要」；④ 合同与架构守卫跟上。**已知代价**：一栏的表头写「符合」、栏内某张卡却带红叉——两个信号各说各的事实，实窗复验时重点看这处会不会被误读成整栏判定。**未跑**本地自动化验证。 |
| T85 | P1 | 🟠 代码已改完；未跑本地自动化，待实窗复验 | 详情浮层被来源卡吸附栏头切成两截：三层分家 | [不变量](development.md) | 实窗截图：说明浮层被切成上下两截，中间横穿一条「叉＝来源没要」。根因是条目与吸附栏头同层——条目悬停 / 聚焦 / 打开时是 `--layer-sticky - 1`（9），来源卡顶部的吸附栏头是 `--layer-sticky`（10）且不透明；浮层在条目的层叠上下文里，所以整块跟着条目停在栏头之下，而浮层是从卡片**往上**开的，第一行卡片的浮层必然跨过那条带子，中段就被吃掉。**已改完**：栏头降到 `--layer-sticky - 2`（8），顺序变成 页签(+1) > 悬停卡片与浮层(-1) > 栏头(-2) > 静态内容(0)；栏头仍然压得住从它下面滚过去的卡片（那些停在 0）。**已知代价**：鼠标停在滚到栏头下面的卡片上时，那张卡片连同浮层会一起画在栏头之上——选择是让浮层可读，而不是让卡片留在栏头下面。**未处理**：底部吸附的写面板仍是 `--layer-sticky`，推荐区的卡片离它很远，这轮不动。**未跑**本地自动化验证。 |
| T86 | P1 | 🟠 代码已改完；未跑本地自动化，待实窗复验 | 仓库武器卡瘦身：来源只列 2 条、状态与快捷操作并成一行 | [合同](work/references/ui-specs/application-workspaces.md) | 实窗里标准档武器卡 233px、同宽护甲卡 133px，卡上三处空白没人用：状态单独占一行（位置十几像素，右侧约 236px 全空）、按钮行左侧空 138.8px、来源面板备了 3 条的位置而多数武器没有 3 个来源（列表区实测用 34/53）。**已改完**：① 来源面板从 3 条收到 2 条，标题仍写全量条数与「另有 n 个」，详情全量不变；② 位置 / 锻造 / 整理状态 / 锁定与配装引用跟快捷操作按钮并成卡片底部一行，状态占左侧剩余宽度、按钮靠右；③ 图标 `52px → 44px`，卡片固定高度按密度改成 `184 / 172 / 196px`，主区改成两行网格（主区 `1fr` + 底部行 `auto`）——原先主区按内容长，余量堆在按钮下面，同排卡片的按钮不在同一条线上；来源行数少的余量现在留成卡片底色，不摊成空的面板底色；④ 删掉「按工作区容器宽度 ≤1020px 把事实行拆两列」那条覆盖：触发条件是窗户外层而卡片仍有 264px 起步，拆开反而让内容 34px 压进 22px 的轨道；⑤ 事实行间距从 grid gap 改成子项 margin——没有勇士词条时第 4 列是空的，空列照样占一个 gap，光等会被挤到离右边缘 10px。护甲与混合卡未动。**已知代价**（已由 T87 消除）：武器名换两行且有 2 个来源时，来源面板被压到 48～54px，第二条来源底部裁掉几个像素（面板自身 `overflow: hidden`，压不到按钮）。量尺在 `.local-data/tmp/vault-card-density/measure.mjs`（无头 Chromium + 仓库当前样式表 + 与 `VaultListItem` 对齐的 DOM，未入仓）：三档密度 × 两档宽度 × 8 种变体，卡片高度精确 184 / 172 / 196（紧凑档 T87 后为 178），主区与卡片溢出均为 0，同档底部行偏移一致，光等右余 0。**未跑**本地自动化验证。 |
| T87 | P1 | 🟠 代码已改完；未跑本地自动化，待实窗复验 | 仓库武器卡重设计：光等搬进身份行右缘、卡片宽度固定 | [合同](work/references/ui-specs/application-workspaces.md) | 承接 T86 的同一张卡，实窗还有两处没人用：身份行右侧整块空着（标准档卡宽 278.8 时未用 132.5px），而事实行四项等分把「反势不可挡」截成「反势不可…」；另外卡片会被 `1fr` 拉伸填满行宽，同排卡不等宽。**已改完**：① 光等从事实行最右搬到身份行第三列（`auto` 列，贴卡片右缘），事实行只剩弹药 / 属性 / 勇士三项，每项够宽，勇士词条完整；② 网格改成固定卡宽 `repeat(auto-fill, 260px)` + `justify-content: start`（紧凑 `224px`、舒适 `296px`），一行排几张只由结果列宽度决定 —— 窗口宽减左侧菜单栏（默认 `184px`，≤1280 为 `160px`，≤980 为 `72px`，≤760 为 `64px`）再减滚动条 10px 与网格内边距 24px；③ 逐处收 6px 还给来源面板：内边距上下各收 `1px`、主区行距 `4 → 3`、事实行下限 `22 → 20`，紧凑档另外把卡高 `172 → 178`（卡宽只有 224，名字必折两行，身份行要从 40 长到 51）。**实测**（`.local-data/tmp/vault-card-redesign/verify-product.mjs`，读仓库当前样式表 + 与 `VaultListItem` 对齐的 DOM，未入仓）：3 档密度 × 6 档视口共 18 项全过 —— 卡宽精确 `260 / 224 / 296`、卡高 `184 / 178 / 196`、光等在身份行、身份行右侧未用 `0px`、事实行 `0` 截断、来源面板余量 `0px` 且无裁切。一行几张（卡宽 260）：窗口 1440～1512 为 4 张、1600～1728 为 5 张、1920～2048 为 6 张、1280 及以下为 3 张。**未跑**本地自动化验证。 |
| T88 | P1 | 🟠 代码已改完；未跑本地自动化，待实窗复验 | 资料库查询：装备模式去掉筛选框，Perk 展开后左栏筛关联武器 | [合同](work/references/ui-specs/application-workspaces.md) | 装备查询是精确查询、结果不多，高级筛选基本用不上；Perk 反查武器动辄几百件，才是筛的地方。**已改完**：① 装备模式左栏只剩模式、主搜索、清空，不再渲染任何筛选控件（`LibraryEquipmentFilter` 与其计数函数保留原样，只从界面下线）；② Perk 模式左栏在「关联分类 / 关联装备」之下新增固定四个维度的分面——位置、属性、弹药类型、框架，取值来自当前展开 Perk 的关联武器，取值旁的数字是「再选上它会剩几件」（统计该取值时排除它自己这一维，否则选完一项后同维其余取值全变 0）；数字为 0 且未选中的取值禁用；③ 展开「关联装备」时一次读完全部关联武器（桌面主进程单次上限 100 条，渲染层顺序取完），否则数字只能算出已取回的那部分；④ 图标与配色复用共享战斗图标语言——位置用槽位字形、属性用官方伤害图标、弹药类型用弹条，配色走同一组元素 token，选中态压过元素色；⑤ 顺手修掉 `.library-mode-tabs` 的 `repeat(3, minmax(0, 1fr))`：只有两个页签，第三列是死的，还把「Perk 与框架」挤成两行。**已知代价**：同一时刻只筛一个 Perk——左栏只有一块面板，展开哪个就作用于哪个；切换展开的 Perk 不重置已选条件。原型在 `.local-data/tmp/library-query-redesign/prototype.html`（真实资料库数据，1504×752 / 1512×897 / 亮色共 36 张截图，无控制台错误）。**未跑**本地自动化验证。 |

## 说明

- `T数字` 与 `Bug #数字` 创建后保持全局唯一且不再改号；本表按任务编号升序排列，优先级和状态以对应列为准，不通过表格位置表达。
- 已完成的 Bug 与任务直接从本表删除，不保留归档行；追溯使用 Git 历史与对应 backlog。
- T41「邮政官安全处理」已取消，编号冻结且不再复用。
- T55 / T57 / T59 / T60 / T61 与 Bug #80–#87 已并入 T56，编号冻结保留作追溯，不单独占行。
- T76 的亲和性机制保留并生效，但它对「换 Perk 后读回旧配置」这个 bug 的结论已被证伪（2026-09-18 的留痕推翻了它）；该 bug 的修复改由 **T77** 承接，T76 编号冻结保留作追溯，不单独占行。
- **T78 是 T77 的实窗复验修复轮**（受理状态跨弹框存活、补上第四份视图、面板去断言），说明并入 T77 那一行与同一份 backlog，不单独占行；编号冻结保留作追溯（代码注释里引的就是它）。
- T45 / T44 / T46 / T43 只**消费**推荐结果，不并入 T56。
- T56 口径（2026-09-16 定）：**DIM 愿望单只是数据来源的一种格式，不是面向用户的概念**；判定、标签、文案一律从数据来源取，禁止硬编码。
- UI 只维护 `packages/ui` 的共享产品实现；Web 用于快速预览，Desktop 用于真实功能验收。
- 自动验证策略见 `AGENTS.md`：默认不跑本地测试 / 构建 / 打包，只有用户明确要求时才执行。
