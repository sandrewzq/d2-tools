# 当前待办

> 更新时间：2026-09-03
> 本文件只保留当前任务、状态和下一步。详细范围与验收标准由对应 backlog 维护，阶段过程使用 Git 历史追溯。

## 健康度

| 检查项 | 状态 | 备注 |
|---|---|---|
| GitHub CI | ⏳ push 后异步执行 | 执行 frozen install、`pnpm test`、共享 Shell 视觉契约、UI 合同静态门禁和构建后的 `pnpm typecheck:ci`；普通 push 不等待结果 |
| Release 门禁 | ✅ 0.0.17 已发布 | 已通过 macOS 发布入口完成本地门禁、GitHub Actions、Windows NSIS 安装包和 GitHub Release |
| Agent 自动验证 | ⛔ 默认禁用 | 只有用户明确要求本地测试、构建或打包时才执行 |

## 当前任务

| 编号 | 优先级 | 状态 | 任务 | Backlog | 下一步 |
|---|---|---|---|---|---|
| T14 | P1 | ✅ 开发完成，待整体验收 | 应用配装主流程简化与逐部位属性模组 | [任务说明](work/backlog/T14-armor-planner-slot-aware-mods.md) | 护甲规划已收敛为“配装设置 / 推荐方案”两栏；五部位可分别直选自动、不装、+5 或 +10，固定数值后再选属性；优先级改为有序编辑，技能与碎片、规划方式、套装和装备范围收入更多条件；下一步在 `574 × 832`、`760px`、`980px` 和 `1280px` 实窗验收控件不溢出、普通路径无技术术语、优先顺序可见且键盘可操作 |
| T20 | P1 | 🟡 阶段 0–12 代码完成，待真实账号、实窗与性能验收 | 武器推荐汇总与仓库精确清理 | [任务说明](work/backlog/T20-weapon-recommendation-vault-cleanup.md) | 仓库武器模式覆盖仓库、角色已装备、角色背包和邮政官，但默认进入“仓库整理”；“随身待检查”聚合当前角色背包与邮政官，“全账号武器”由玩家主动选择，护甲和其他装备仍只显示仓库。详情首屏复用账号快照 Roll，推荐区自动选择第一个有数据的来源。推荐结果现已统一区分绿色完整符合、琥珀色部分符合或接近推荐、低强度红色未符合、中性灰仅推荐武器、等待蓝数据不完整，并同时保留状态点和文字。下一步以真实账号抽查默认范围、四类位置、首屏 Roll、颜色辨识、邮政官取回、批量转移边界和千件武器滚动/详情冷打开耗时；护甲字段、卡片和筛选语义保持不变。 |

## Bug

| 编号 | 优先级 | 状态 | 问题 | 修复范围 | 下一步 |
|---|---|---|---|---|---|
| Bug #9 账号 | P1 | 🟡 已修复，待真实账号验收 | 一键最高光等把仓库“当前不在角色身上”和可替换异域冲突误判为永久不可装备；同时异域装备的专用库存 bucket 可能被归为 `other`，或 `can_equip=false` 的未知原因码被误放行，导致真实装备槽位/候选计算错误；写接口返回成功时还会提前把本地页面标成已装备，刷新后继续保留未确认的乐观状态；装备失败提示在手动刷新后仍残留 | 共享光等候选资格按 Bungie 原因码区分可恢复限制与硬阻断，原因码为 0 不再猜测放行；摘要转换在未知库存 bucket 时回退到 `equippingBlock.equipmentSlotTypeHash`，光等槽位层再从缓存中的弹药类型/装备类型恢复旧摘要；仓库装备进入转移计划，异域替换进入组合求解；确认期间禁用重复的一键提交，避免同角色新操作覆盖上一笔确认；单件和批量装备写入均只记录“请求已受理/同步中”，不立即应用装备 patch；权威 Profile 未反映时刷新会清除旧装备假状态，验证任务结束后触发真实账号刷新并追加“已确认/不可用”日志；设置页日志筛选和状态徽标区分已受理、已确认与失败；手动刷新同时清除已结束的 renderer 写入错误反馈，即使复用已有刷新请求也会清理上一轮账号读取错误 | 使用统一版本的真实账号确认“浮游孢子”等装备的请求受理、同步中、已确认、部分成功和失败状态；验证失败后页面不能继续显示已装备；两台设备对比同一实例 ID 的最终位置与光等 |
| Bug #10 每周活动 | P1 | 🟡 已修复，待真实账号验收 | 多角色活动数据按 Hash 过早去重，导致只有后续角色返回“周常突袭/地牢挑战”时，首页漏显示该轮换活动 | 先按活动 Hash 合并所有角色的挑战、修饰器和奖励，再判断轮换突袭/地牢；固定周常奖励不进入轮换卡片结果 | 使用多个角色分别存在/不存在挑战的账号数据，确认守望者尖塔、突袭活动等不会因角色顺序被漏掉 |
| Bug #11 配装 | P1 | 🟡 已修复，待实窗验收 | 装备选择抽屉的结果项没有按三行真实内容建立稳定高度，窄窗口下副信息溢出并与下一项重叠 | 抽屉限制为视口高度且只让结果区滚动；结果轨道和装备项改为按内容自然增高；图标、文本栅格、行高和窄屏动作位置独立约束 | 在约 `574 × 832`、`760px`、`980px` 和 `1280px` 宽度下打开武器与护甲选择抽屉，确认三行信息不重叠、列表独立滚动、搜索框初始聚焦和 Tab 焦点不跳动 |
| Bug #12 配装 | P1 | 🟡 已修复，待实窗验收 | 护甲规划把三列表单塞进 `276–304px` 固定侧栏，控件必然横向溢出；同时重复显示底层数据术语，并用无序复选框表达实际有顺序的属性优先级 | 改为设置与推荐方案两栏；五部位使用四段直选并按需显示属性；属性优先级使用有序列表；高级条件折叠；数据未读取只显示一条中性说明，具体部位只显示已确认冲突 | 在 `574 × 832`、`760px`、`980px` 和 `1280px` 实窗核对控件不溢出、技术术语不进入普通玩家路径、优先顺序可见且键盘可操作 |
| Bug #13 资料库 | P1 | 🟡 已修复，待实窗验收 | 武器资料库定义详情的 Perk/起源特性区域只按外层窗口媒体查询收缩；详情主体实际变窄时仍保持六列，导致起源特性描述被压成逐字换行 | 为定义详情弹窗和 Perk 主区域增加容器查询；在实际内容宽度不足时依次收缩为三列、两列和单列，并让窄容器下正文自然增高 | 在资料库武器详情的宽屏嵌入、窄工作区和移动尺寸下确认起源特性、空插槽和其他 Perk 卡片不再出现窄列竖排或水平溢出 |
| Bug #14 仓库 | P1 | 🟡 已修复，待导入验收 | 武器推荐 CSV 仅按“中文武器名 + 来源”判重，无法容纳 `The Eremite` 与 `The Recluse` 这类官方中文同名且确实被同一来源分别推荐的武器；首次报错的“龙息”还暴露出生成器把 LGpig 异域火箭筒 `Dragon's Breath` 的来源内容错误映射给同名微型冲锋枪 `Breath of the Dragon` | CSV 与 SQLite 改为“官方英文名称优先、武器 ID 次之、中文名称兜底”的稳定武器身份键；SQLite 升至 schema version 4，同中文名不同身份可共存；运行时同一来源优先按当前 Hash，跨版本再按官方英文名称匹配，只有中文名且歧义时不猜测；LGpig 异域表只接受官方异域品质候选，并从正式 CSV 移除错误的微型冲锋枪 LGpig 行；升级仅重建中文推荐关系表并保留 DIM 数据 | 重新选择正式 `武器推荐.csv`，确认 2,382 行预览和导入成功；分别查看两把“隐士”确认同来源记录不串用，并确认 LGpig 的“龙息”说明只出现在异域火箭发射器上 |
| Bug #15 仓库 | P1 | 🟡 已修复，待真实账号验收 | 中文推荐 CSV 导入成功后自动重新核对，却使用旧 JSON Definition 状态判断资料库完整性；当前 Desktop SQLite 资料库明明可用，仓库仍误报“资料库尚未准备完成”，导致 `0/1054` 和“推荐核对失败” | 仓库推荐 IPC 改为复用 Desktop 唯一资料库状态 `getDesktopManifestStatus()`，与顶部状态、设置页和运行时 SQLite 激活结果保持一致；导入确认后的既有强制重新核对流程继续使用，不增加玩家手动步骤 | 重启 Desktop 后进入仓库；无需再次导入，确认自动核对 1054 件账号武器并显示中文五来源结果，顶部不再出现“推荐核对失败” |
| Bug #16 仓库 | P1 | 🟡 已修复，待真实账号验收 | 当前武器 Roll 解析把 Bungie `frames` 分类的强化特性误当成框架过滤，造成 1054/1054 把武器缺失 Perk 1/2；同时未过滤 `weapon_tiering_*` 战斗特效/皮肤插槽，使 766 把武器误报 Roll 不完整。后续按当前 Hash 的 Perk 池过滤来源要求，又会让跨版本推荐栏位从分母消失 | 按 Plug 语义重建六栏 Roll并过滤系统插槽；来源身份按 Hash、官方英文名称和唯一中文名称确认，同名版本共享推荐；正式 CSV 保留的明确栏位全部进入分母，匹配同时使用 Hash、规范化名称、普通/强化身份和大师属性族，实例完整但没有要求时直接记为不符；仓库实例、商人武器和来源卡的普通路径移除版本适用信息，资料库多定义选择功能保留；CSV DIM 汇总退出运行时栏位匹配，缓存算法升至版本 6 | 重启 Desktop 重新核对真实账号，确认第三、第四栏恢复；抽查同名不同 Hash 的实例获得相同来源要求，实际拥有的旧版 Perk 能符合，实际没有时稳定显示“未符合”或“符合 x/y”，不再出现版本适用提示；DIM 只显示原生 Wishlist 组合结果 |
| Bug #17 仓库 | P1 | 🟡 已优化，待实窗性能验收 | 约 1000 件仓库中，武器卡片、推荐筛选、推荐结果和同名整理分别重复构造同一实例的来源摘要；即使列表已经虚拟化，滚动和切换筛选仍会产生不必要的数组、字符串和 Wishlist 匹配开销 | 仓库页按 `items + 实例匹配 + Wishlist` 一次建立实例来源摘要索引；武器卡片、推荐筛选、推荐结果和同名整理统一 O(1) 查表；DIM 规则继续复用 `itemHash → rules` 索引；武器网格只挂载视口和三行 overscan；SQLite 实例匹配缓存按 Roll 指纹和推荐 revision 复用，护甲列表不接入推荐摘要 | 重启 Desktop 后以约 1000 件真实仓库连续滚动、搜索、切换推荐筛选和返回同名整理；确认卡片不整页重算、焦点与滚动位置保留、护甲字段和行为无变化 |
| Bug #18 仓库 | P1 | 🟡 已修复，待真实账号验收 | 仓库主列表只展示 `account.vault.items`，角色刚获得的背包/已装备武器和邮政官武器看不到推荐；同时邮政官容器 bucket 被误当装备槽位，导致邮政官武器归为 `other`、不生成 `weapon_roll`、无法参与推荐扫描 | Core 分离当前存放容器与规范装备槽位；App 只对武器聚合全账号四类位置，护甲保持仓库范围；共享仓库 UI 增加独立位置筛选和卡片位置，账号卡增加最强推荐摘要；Desktop 按实时位置打开统一详情，批量仓库转移只接收 `source_kind=vault` | 使用真实账号分别抽查仓库、当前/其他角色已装备与背包、当前/其他角色邮政官；确认推荐结论一致、邮政官可取回、角色物品不会被仓库批量移动、护甲无变化 |
| Bug #19 仓库 | P1 | 🟡 已修复，待真实账号与性能验收 | 仓库默认混入全账号武器，普通卡片重复显示“未标记”；武器详情丢弃账号快照已读取的 Roll，冷打开仍等待单件详情接口，同时推荐区默认选中可能为空的 DIM，入口、所在位置和武器槽位语义混在一起 | 仓库默认改为“仓库整理”，新增“随身待检查”并保留主动的全账号与精确位置范围；角色位置统一为“职业 · 背包/已装备/邮政官”；普通浏览隐藏空整理状态，整理模式显示“未整理”；账号快照 `weapon_roll` 转成只读首屏插槽，辅助推荐、同名版本和实时获取请求改为空闲阶段加载；推荐区只显示有数据的来源并优先攻略推荐；详情账本分开显示打开入口、这件武器、所在位置和武器槽位 | 使用真实账号从仓库、当前背包和邮政官各打开一把传说武器，确认卡片默认范围正确、Roll 首屏立即出现、后台补齐后仍保持同一实例、推荐页不落入空来源；连续打开多把武器观察交互不卡顿，护甲详情和护甲筛选不变 |

## 说明

- UI 只维护 `packages/ui` 的共享产品实现；Markdown 合同记录稳定约束，Web 用于快速预览，Desktop 用于真实功能验收。
- UI 合同静态门禁覆盖稳定标记、表面枚举、排版、层级、菜单主题色和选中态方向线，避免菜单实现重新取得共享视觉职责。
- 新增 Bug 必须使用全局唯一编号并在本文件中登记。
