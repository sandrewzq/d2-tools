# 当前待办

> 更新时间：2026-09-16
> 本文件只保留当前任务、状态和下一步。详细范围与验收标准由对应 backlog 维护，阶段过程使用 Git 历史追溯。

## 健康度

| 检查项 | 状态 | 备注 |
|---|---|---|
| GitHub CI | ✅ v0.0.26 发布时通过 | 冻结安装、全部 workspace 构建、行为 131 / 架构 14 测试、质量门禁（含 UI 合同）、全量类型检查 |
| Release 门禁 | ✅ v0.0.26 已发布（2026-09-15） | 经 `tools/mac-git-auto-release.command` 完成本地门禁、GitHub Actions、Windows NSIS 安装包与 GitHub Release |
| Agent 自动验证 | ⛔ 默认禁用 | 只有用户明确要求本地测试、构建或打包时才执行 |

## 当前任务

| 编号 | 优先级 | 状态 | 任务 | Backlog | 下一步 |
|---|---|---|---|---|---|
| Bug #88 | P1 | 🟠 代码已完成并变异验证，待重启确认 | 登录正常却提示「登录可能已失效」 | [任务说明](work/backlog/Bug88-account-session-token-rotation.md) | 三条已改完：① `loadFreshOAuthToken` 做在途合并（原并发刷新实测 3 次 → 1 次）；② 账号 Session 按**账号身份**判会话、不按 token 字符串（token 到点轮换不再被误判成换账号）；③ 渲染层只在认证类错误时才说「登录可能已失效」。四门判据全绿（typecheck 7 包 / 行为 137 文件 543 用例 / 架构 66 用例 / docs:check），四处变异各自只让对应用例变红。**待确认**：重启桌面端，确认隔夜后再打开应用不再出现红条「登录可能已失效」 |
| Bug #89 | P1 | 🟠 代码已完成并变异验证，待用户复验 | 人工推荐表格导入报「表头不受支持」；不支持 Excel 文件 | [任务说明](work/backlog/Bug89-recommendation-table-import.md) | 用户诉求：**CSV 与 XLSX 都能导入**。两个真实来路：① 用户在 Excel 里维护的 `.xlsx`（选择器只允许 `.csv`，按文本读二进制必错）；② **上一版（v0.0.25 及更早）导出的 11 列模板**（当前只认 12 列），且界面一直写着「支持 11 列模板」——文案与实现长期不一致。已落地四条：按文件内容识别格式（格式分支只留在适配层，不引第三方依赖）、兼容上一版 11 列中英文模板、行宽改为与**该文件自己的表头**同宽 + `.xlsx` 行补齐到表头宽度、报错带上文件名与实读表头。入口改名「人工推荐表格 / 导入表格文件」，选择器扩展名加 `xlsx`/`xlsm`/`xls`（`.xls` 收进来只为给「请另存为」的明确提示）。五门全绿（typecheck 7 包 / 行为 138 文件 554 用例 / 架构 66 用例 / docs:check / test:quality），六处变异各自只让对应用例变红。**待确认**：用你自己那份文件再导一次 |
| Bug #90 | P1 | 🟠 代码已改并变异验证，待用户复验 | 导入成功后，同页下方「已导入来源」管理面板仍显示「当前来源 0 个」 | [任务说明](work/backlog/Bug90-recommendation-source-panel-stale.md) | 用户截图：仓库 → 推荐来源同页，上栏「已导入来源（1）· sayalarry · 70 条规则」，下栏却「当前来源 0 个 / 还没有可管理的推荐来源」。**先排除了数据问题**：拿用户那份库的副本直接跑服务层读取，返回 1 个来源（`sayalarry`、`active`、70 条规则、127 把武器），上栏读的是同一个库。根因是**两栏重读时机不同**——上栏导入 / 覆盖 / 移除 / 清空后显式重读，下栏只在挂载时读一次，所以「先在这一页、再导入」就留下旧快照。处置：导入链路写完后回调一次，上层自增刷新计数传给下栏，下栏把它加进重读依赖（计数只在真的改动时自增，不许用「每次渲染都重读」蒙对）。新增 1 文件 4 用例（假存储 + 走真实点击路径：导入 / 移除，并断言读取次数 1 → 2），五处变异各自只让对应用例变红；五门全绿（typecheck 7 包 / 行为 139 文件 563 用例 / 架构 66 用例 / docs:check / test:quality）。**待确认**：在应用里再走一遍「先待在这一页 → 导入」 |
| Bug #91 | P1 | 🟢 代码已改并自验通过，待用户复验 | 推荐来源页有两份「已导入来源」清单，删除动作也有两条实现 | [任务说明](work/backlog/Bug91-duplicate-imported-source-list.md) | 用户截图提问「红框部分不是重复了吗」。核实：下栏那份是**按导入文档聚合**出来的（一行一次导入，名字取文档标题），所以两份清单的集合与粒度完全相同，下栏还多给武器数、账号影响件数与启用状态。两个删除按钮不是同一函数的两个入口，而是**同一件事的两份实现**：上栏「移除」只删文档行且不碰覆盖状态（用户库里那条 2026-09-13 的 `sayalarry = 已移除` 残留覆盖行就是它留下的），下栏「删除」连规则排除与来源覆盖一起清、还带说明，并保留下游的装备目标清理与匹配缓存失效——下栏是超集。**用户 2026-09-16 拍板「只留下面的来源管理区」**：导入区不再列已导入来源，清单与删除只在管理区一处；顺带把旧删除通道整条删掉（按钮 → 渲染层动作 → preload → IPC → 服务函数，含只被它使用的读取函数），清空确认文案改指新位置。自验：三条变异（塞回重复清单 / 确认文案退回旧版 / 删除改成只停用）各只杀对应用例；五个门与 `pnpm check` 全绿 |
| Bug #92 | P1 | 🟢 代码已改并自验通过，待用户复验 | 仓库里勾选任意推荐来源后整仓变 0 件 | [任务说明](work/backlog/Bug92-vault-source-filter-empty.md) | 用户截图原文「没读取到数据」：仓库勾上 sayalarry 后「仓库整理 · 0 件」，位置与类型计数全 0。**不是没读到账号数据**：快照 1085 件、匹配缓存 764 条（23 件命中该来源）都在。拿用户库副本跑生产函数，来源管理给出的键是 `document:…f865`（一次导入 = 一行，文档级），事实层登记的键是 `document:…f865:…f865`（一个具名来源 = 一行，实例级），交集为空——名册被当权威白名单去过滤来源事实，两个键不相等就把事实全部滤掉，空集又被喂给位置 / 槽位 / 类型每一处计数，所以是整页全 0。根因是同一个「来源」两半代码用了两种粒度，且两边注释都写着「用户拍板」却方向相反；服务侧对拍测试钉实例级、界面侧夹具写死文档级，谁都没红。**定案取甲（一行 = 一次导入）**：事实层不动（服务侧既有契约本就写明「筛选与选项用分组键，消费层据此合并同一份来源的多个实例」），改由管理名册每行带出这次导入下辖的具名来源键，消费层据此把事实归队并把键换成管理面的键。自验：三处定向改坏各只杀对应用例（名册不再枚举下辖实例 / 归队时不换键 / 视图不建对应表），其中架构守卫第一版是假守卫——只断言文件里出现某个词、而那个词另有出处，已改成断言归队那一步后重跑才红；五个门与 `pnpm check` 全绿。 |
| Bug #93 | P2 | 🟢 代码已改并自验通过，待用户复验 | 导入弹框里来源名与两个按钮挤在预览卡右栏，且每次都要自己起名 | [任务说明](work/backlog/Bug93-import-identity-row.md) | 用户截图提问「样式乱了，而且可以默认生成一个名字」。两件事叠在一起：一是命名块沿用 DIM 时代的旧类名，而全仓样式表里一条对应规则都没有——它是彻底无样式的裸 label + input + 按钮；二是它作为预览卡（三栏栅格）的第 4 个子元素自动流进了第三栏，把文件名与件数挤扁、按钮越界。四个导入入口共用这一块，所以四处都乱。**处置**：命名与确认独占预览卡一整行（左栏输入、右栏两个互斥动作、提示另起一行），类名去掉格式名，并把「必须横跨整行」钉进架构守卫；输入框预填默认名——文件类取文件名去扩展名（只剥最后一段，「推荐表 v1.2.xlsx」得「推荐表 v1.2」，只剩扩展名时退回原名），在线取用户上次起的名字，粘贴取「粘贴内容」；四处同一条规则，不按格式分岔。自验：三处改坏（不预填 / 去掉扩展名改原样返回 / 命名块不横跨整行）各杀掉对应用例；五个门与 `pnpm check` 全绿 |
| Bug #94 | P2 | 🟢 代码已改并自验通过，待用户复验 | 来源行的「perk 命中」分段框撑满整栏，框内大半是空的 | [任务说明](work/backlog/Bug94-perk-filter-frame-width.md) | 用户截图提问「样式不太对」。只看截图容易看走眼，于是把这些行的标记与真实样式表原样搭成静态页、用无头浏览器按同宽渲染量尺寸：来源行宽 990，分段框宽 464（它所在的列是 `1fr`，而框写死 `width: 100%`），框内内容到 526 就结束——行一空 213、行二空 267，同一个列表两行空得还不一样多；两个下拉固定在右列，空框就夹在中间。同一行右侧的「位置」筛选是贴合内容排布的，两者不一致才有「画歪了」的观感。**处置**：分段框改为贴合内容（`width: fit-content` + `max-width: 100%`，放不下照旧换行），控件位置一个都不挪——仍在来源名右边，两个下拉仍各占一个定宽栏目，逐行对齐不受命中项多少影响。中途按「收回后整组靠右、紧贴两个下拉」改过一版，用户复看回「这样更丑啊」，遂退回原位；「整列改成按内容排布」也会让两个下拉随命中项数量逐行错位，同样不取。窄屏 700 像素以内整行归它时，删掉原先把分段框拉满整行的覆盖。自验：三处改坏（退回撑满整栏 / 补回 `justify-self` 靠右 / 条件行列宽改成 `auto auto auto`）各让架构守卫变红；写守卫时先踩了自己一脚——用「不含 `width: 100%`」判断，而 `max-width: 100%` 本身含这串字符导致假红，改成按行锚定才准。五个门与 `pnpm check` 全绿 |
| T1 | P1 | 📝 候选方案，待排期 | 全应用英文国际化补齐 | [任务说明](work/backlog/T1-english-internationalization.md) | 先页面标题与共享弹窗，再仓库、装备详情、配装、AI 助手 |
| T23 | P2 | ⏸️ 等待官方书面授权 | D2Checkpoint 进度机器人集成 | [执行 backlog](work/backlog/T23-d2checkpoint-integration.md) | 2026-09-08 已发授权申请；收到回复前不进入开发 |
| T43 | P1 | 🟠 待真实账号验收 | 任务待处理中心 | [任务说明](work/backlog/T43-pursuit-attention-center.md) | 三角色任务、赏金、角色目标与赛季挑战的数量、进度、追踪、到期状态 |
| T44 | P1 | 🟠 待真实账号验收 | 光等提升路线 | [任务说明](work/backlog/T44-character-progression-center.md) | 周挑战奖励按 `displayRewards` 读取；核对三角色「本周可做 / 无可领取」 |
| T45 | P1 | 🟠 待真实账号验收 | 本周刷取清单 | [任务说明](work/backlog/T45-weekly-farming-checklist.md) | 核对轮换活动、装备来源、T20 推荐与四类图样状态 |
| T56 | P1 | 🟠 进行中（判据①② 均已转绿，Layer 4 / 4.5 / 5 / 2 已完成，Layer 3 已完成 L3-0 / L3-1 / L3-2a / L3-2b / S1 / S2） | 仓库推荐：统一模型与来源去分叉 | [任务说明](work/backlog/T56-unified-recommendation-model.md) | 完成判据 = 两组测试全绿。① 五路对拍（摘要 / 排序 / 筛选 / 卡片 / 冲突）**已全绿**，落地点 `packages/ui/test/recommendation-source-parity.test.ts`；② 边界扫描 `packages/app/test/recommendation-source-boundaries.test.ts` **已全绿，0 处违例**（起点 40 → 视图层 Layer 5 清 21 → 服务 / 存储层 Layer 2 清 19）。Layer 1 死代码、Layer 4「DIM 愿望单」第三身份与硬编码判定、Layer 4.5 导入身份改为用户命名、Layer 5 视图层分支消除、Layer 2 来源身份抽象 + 删 legacy `external_recommendation_sets` 表族**均已完成**（4.5 含 schema 6→7、Layer 2 含 schema 7→8 的来源键改写，旧 dim 导入需重新导入一次）。**Layer 3 进行中**：L3-0 CSV 回归网、L3-1 schema v9（规则武器 / 要求进子表，`perk_hashes` 停止读写）+ v8→v9 无损回填 + 统一规则读写模块 `recommendationRuleStore.ts`、L3-2a 栏位标签表三合一、L3-2b 删 `unresolved_candidate_names` 重复通道 **已完成**；**L3-3 的 S1（存储层去格式 + schema v10 键统一迁移）与 S2（名称→hash 解析收口 + 「无要求」判定表两侧合并）已完成并验证**：`pnpm typecheck` 7 包全绿、行为 501 用例全绿、架构 64 用例全绿（边界扫描仍 0 处违例），四次变异测试均能变红。**L3-2c 投影实现合并已完成（质量重构，2026-09-16）** → L3-3 的 S3′（**原 S3 + S4 已合并，口径 2026-09-16 用户拍板：CSV 的武器身份在导入期定死成 hash，读取期只做成员判断**；子步 **S3′-0 对拍网已落地并全绿**（`packages/services/test/recommendationIdentityExpansion.test.ts`，3 条用例；口径经用户拍板由「逐条等价」改为 **「只增不减」+ 三个版本对称可见**——旧读取期的「同来源精确优先」会少显示、是缺陷、不搬；实测 1630 把武器 / 3537 → 3548 条，只多 11 条、无一变少）、**S3′-1 导入期展开已接线并全绿**（`WeaponKnowledgeValidationContext.weaponIdentityRelations`；写库的是展开后的完整 hash 集，`rule_stable_id` / `identity_key` 仍按声明身份算；接线前先跑红再转绿；typecheck 7 包 + 行为 504 用例 + 架构 64 用例全绿，边界扫描仍 0 违例）/ S3′-2 新适配器 + 删读取期匹配 / S3′-3 导入改命名 + 新建覆盖）→ S5 读取链路换血、`weapon_recommendations*` 读点归零 → S6 外部连接点 → S7 回导补 manifest → S8 schema v10 删 CSV 表族 → L3-4 缓存键归一 + 合同改写。详见 backlog〈Layer 3〉与〈L3-3 执行计划 / S1 / S2 执行记录〉。**剩余 S3′-2 → S7 已定做法（用户 2026-09-16 拍板「一」）：一次切换**——读取换血、写入换血、状态门 / 管理面 / 导出同一次落地（三者之间不存在可交付的中间态：先换读取方则没有 `kind='csv'` 的写入方，先换写入方则旧读取方读不到且状态 / 管理 / 导出同时变空），**不做临时双写、不做自动迁移**；终态形状与 7 条判据见 backlog〈S3′-2 → S7 一次切换执行计划〉。**对用户的代价：切换后 CSV 需重新导入一次，DIM 不必重导**。**L3-4b 详情页推荐区去分叉已完成（2026-09-16）**：武器详情删掉 `攻略推荐 / 我的推荐` 页签栏与 `personal`（装备目标）路径、删恒空 `combos` 分支与 `"社区推荐"` 硬编码兜底来源名、删除旧 `ItemDetailCommunity` 面板与装饰性假页签栏、`WeaponRecommendation.source` 身份字段收窄移除，并修掉一处 CSS 来源类型分叉（`[data-recommendation-source="dim"]`）；四门判据复跑全绿（typecheck 7 包 / 行为 134 文件 519 用例 / 架构 66 用例 0 违例 / docs:check）。**同时清空了架构守卫白名单**——原 7 条里 4 条指向不存在的路径、且 7 条全部零作用（整体停用后扫描仍 0 违例，等于白名单在假装守卫存在），并新增两条自检（扫描根目录必须存在、白名单每条必须压住真实违例），三处变异测试均能变红。**新发现待拍板**：护甲详情 `ArmorTargetSource`（`personal`/`loadout`/`community`）是武器侧同构分叉，其中 `"应用推荐"` 与 `"在线补充推荐"` 全仓无生产点、两个页签恒空；涉及护甲侧信息架构取舍，已拆到 **T62** 单独拍板，不在 T56 内处理。**L3-2c 归约合并已完成（2026-09-16）**：结论修正——CSV 表达「每栏任选其一」的方式是**一格多值**（`特性甲 / 特性乙`），写成展开多行会被导入期按「同一武器 + 推荐来源重复」阻塞，所以 CSV 侧不存在可归约的输入；真正的分叉是**「武器级推荐」的判据**，已抽到 core 的 `isWeaponLevelRule` 两侧共读。对拍网 `packages/services/test/recommendationSourceProjectionParity.test.ts` 4 条用例全绿，四处变异测试（短接归约 / 换回旧判据 / 往分组键塞格式前缀 / DIM 分组键退回文档级）均能变红。**Layer 6 三条全部闭合**，含**生产者侧分组键断言**。**分组粒度分叉已由用户拍板（2026-09-16）：按来源名分多行**——一个具名来源 = 一行，两种格式一致。已改 DIM 侧（`source_group_id` 取实例键），并把兼职的 `documentLevel` 拆出来（它服务的是覆盖状态继承，与分组共用变量会静默失效）；同步更正 4 处文档 / 注释契约（`ui-specs/application-workspaces.md:203`、`development.md` 2.7、`recommendationMatchView.ts`、`ui` 侧对拍夹具注释），并新增跨格式粒度用例（分组数 = 具名来源数 = 2），把 DIM 分组键退回文档级即当场变红。**同一轮实测又撞出一个独立真实缺陷并已修**：「停用整份导入」在 CSV 上**静默失效**（写的是文档键、CSV 适配器只查实例键，实测停用前 2 条、停用后仍 2 条；DIM 侧正常）。根因是这段状态继承规则在两个适配器里各写了一份，已提到 `recommendationOverrides.ts` 的 `recommendationSourceStateFor` / `recommendationRemovedRuleIdsFor` 共享，两处私有副本删除；新增第 5 条用例（文档键停用 → 全部停用；实例级优先于文档级），去掉共享函数里的文档键兜底即当场变红。**导入期校验（原 T60）已完成（2026-09-16）**：DIM 文本导入时按行挑出「按来源本意无法成立」的规则（写法不成立 / 同行重复同一 Perk / Perk 归不到唯一栏位 / 同一栏写了两个 Perk），其余照常导入；只有「丢行把作者原本写全的一组候选拆残」才整把武器跳过；资料库里查不到该武器定义时一行都不判（fail-open）。预览给出可导入条数、跳过行数、涉及武器数、整把跳过数与每类问题示例（含行号与原文）。归栏判定与读取期共用一份（`dimWishlistDiagnostics.ts`），四处变异测试各自变红，四门判据复跑全绿（typecheck 7 包 / 行为 135 文件 533 用例 / 架构 66 用例 0 违例 / docs:check）。**残留**：粘贴路径无服务端跳过摘要、在线更新（URL）路径不走校验——**这两条随 [T63](work/backlog/T63-dim-source-link-sync.md) 删掉对应通道而消失**；`unknown_perk` 存在理论误伤（先预览、可取消，仍在）。**文档残留**：已随 T63 批量校正（`user-guide.md` §8.3、`ui-specs/application-workspaces.md` 的导入路径与命名规则、`development.md`、`data-sources.md`、`destiny-tool-reference.md`；其中「笔误行按行忽略 / 整把武器跳过」的判据原样保留），`equipment-details.md` 早前已按 T56 口径改完。**人工表格核对只看了武器的一部分版本已修（2026-09-16）**：用户自己那两份表导入报「18 行 / 1 行异常将忽略」，文案「无法在这把武器任一已列版本…」是假话——两个文件都只写武器名，而「名字→官方定义」走的是搜索（有排序、有上限、并按同一武器的代表版本折叠过）。实测两句根因：「好建议」两个官方版本的起源特性分别是「问题解决者 / 加速突击」，搜索只返回前者；「允诺」的同名护甲占满前 20 条，把斥候步枪的另外两个版本挤出榜单（那行报的 4 个 Perk 恰好逐字等于被挤掉那个版本的 Perk 池）。处置只加不减：导入期装载定义池时再按**精确官方名**取该名字的全部官方版本（搜索命中保留），验收判据是**用户两份文件异常都归零**（实测 Sayalarry 18 → 0、YXCRALLXY 1 → 0，另外两份 Aegis 729 行 / LGpig 201 行无回归；把定义池改回只搜名字，两行数立刻回到 18 + 1、与用户截图逐行一致）。落地：索引 / 目录层加 `getItemHashesByExactName`（不排序、不折叠、不设上限，英文副索引取并集）、worker 桥接一条操作、导入期定义池收口到 `collectWeaponRecommendationDefinitionHashes`（= 武器 ID ∪ 搜索命中 ∪ 同名全部版本）。五门判据全绿（typecheck 7 包 / 行为 138 文件 559 用例 / 架构 66 用例 / docs:check / test:quality），四处变异各自只让对应用例变红。详见 backlog〈人工表格核对只看了武器的一部分版本〉 |
| T58 | P1 | 🟠 待真实账号验收 | 游戏内配装完整子职业配置 | [任务说明](work/backlog/T58-ingame-loadout-subclass-fidelity.md) | 按超能/技能、星象、碎片展示并保留原始 socket 映射 |
| T62 | P2 | 📝 待拍板（A / B / C 三选一） | 护甲详情目标来源去分叉 | [任务说明](work/backlog/T62-armor-detail-target-source-defork.md) | T56 同构残留：三页签里 `配装与攻略` / `社区来源` 恒空（`"应用推荐"` / `"在线补充推荐"` 全仓无生产点），来源名 `"我的推荐"` 是硬编码且与 personal knowledge 撞名。处置待定：**A** 纯去分叉（建议）/ **B** 对齐武器侧再拆区块 / **C** 只删死页签 |
| T63 | P1 | 🟢 代码已改并自验通过，待用户复验 | DIM 文本的来源入口收成两条路：本地文件 + 用户给的链接 | [任务说明](work/backlog/T63-dim-source-link-sync.md) | 用户判定原设计错误：「不应该从 github 读取！！！这条路删了。粘贴文本也删了。应该是两条路：1. 本地文件上传 2. 在线文档同步，给你一个愿望单文本链接」。**删**：写死 GitHub 地址的在线导入（含 commits API / commit SHA / etag 一整套）与粘贴文本通道。**留两条**：本地文件上传；在线文档同步——弹框里贴链接（用户拍板），下载后走与本地文件**完全相同**的一条流水线（解析 → 定义池校验 → 预览 → 命名与新建/覆盖）；链接随来源以 `source_url` 保存，来源行带「同步」，判据是内容指纹（无变化即「已是最新」，不动库）。顺带闭合两处旧残留：在线路径过去不做导入期校验、且会卡在「读取中」最长三分半。不需要改 schema。**已落地（2026-09-17）**：删掉固定地址在线导入整套（`dimWishlistUpdates` + `wishlist:online:*` + 界面在线预览框）、粘贴文本通道（含 `wishlist:save` 与 `saveDimWishlist` 合同条目），并清掉最后一处写死的上游地址（设置页「数据来源」里的 `voltron.txt`，现由全仓守卫按住）。新增链接读取（边界收口在共享的 `publicUrlReader`：协议 / 私网 / 跳转逐跳复校验 / 体积 / 单一 60 秒超时 / 不带凭据）、按内容指纹判定的再同步、来源行「同步」按钮与贴链接弹框；两条路共用同一份解析与导入期校验。**自验**：新增 26 条用例（下载边界 8 / 链接来源 8 / 同步判定 4 / 界面 6）与 3 条架构守卫（两条入口存在且粘贴与在线导入不存在 + 下载层不认识格式 + 全仓无写死上游地址），39 处定向变异各自只让对应用例变红；五门判据全绿（typecheck 7 包 / 行为 144 文件 599 用例 / 架构 72 用例 / docs:check / test:quality），`pnpm check` 通过。**一处如实记录**：「本地文件来源没有链接，因此查不到」这条没有单点变异可杀（文件来源本就不带 `source_url`，去掉 `origin='url'` 过滤结果不变），保留为设计说明；用户可见的那一半（只有带链接的行才显示「同步」）已被界面用例的变异钉住 |
| T64 | P1 | 🟢 代码已改并自验通过，待用户复验 | 导入时认出「每栏任选其一」的展开写法，不再把摊平写的行报成问题行 | [任务说明](work/backlog/T64-dim-expansion-pool-recognition.md) | 用户看到链接导入的预览卡红框「7291 行将忽略，涉及 257 把武器」，提出「一把武器9行改成合集不就行了吗」。**实测这份文件**（8985 行 / 314 把枪）：3 个 perk 及以上的行 7306 条，**每一条都被同一把枪的某条两 perk 行整个包含，0 例外**——这些行不带来任何新命中，只是作者把「每栏任选其一」摊开写的产物（历史代码早有结论：Aegis 863/863、小棒猪 257/257）。读取期本来就把同一把枪留下的行归约成合集，导入期没做同一件事，所以把摊平时的冗余行逐行报成了问题行——**要的合集一直在，只是报错了地方**。处置：导入期按同一把枪先认展开写法（留下的行本身构成一组完整候选 + 该行属「同栏两个 perk」+ 该行 perk 已被留下的某一行整个包含），认出来的行记为「展开写法的冗余」，红框只留给真正的笔误；**不改存储、不改读取、不改 schema，写进库的行与今天逐条相同**。把合集写库（来源管理显示「一把枪一条」）留待 T56 存储切换后单独定。**已实施**：服务层两个判据函数 + 预览两个计数（`merged_row_count` / `merged_weapon_count`）+ 预览卡中性说明块与 `dimPreviewNotice` 共用提示；16 项服务层用例、9 项界面用例、1 条源码级守卫（主进程那段映射没有行为用例，见任务说明「如实记录」）；19 条变异全杀（服务 9 / 界面 5 / 守卫 5）；五个闸门 + `pnpm check` 全绿 |

## 已完成基线

| 编号 | 发布版本 | 结果 | 记录 |
|---|---|---|---|
| T14 | v0.0.22 | ✅ 应用配装与逐部位护甲规划已完成并验收 | [完成摘要](work/backlog/T14-armor-planner-slot-aware-mods.md) |
| T20 | v0.0.22 | ✅ 武器推荐与仓库整理已完成并验收 | [完成摘要](work/backlog/T20-weapon-recommendation-vault-cleanup.md) |
| T22 | v0.0.26 | ✅ Renderer 事件驱动性能收敛、发布身份成果与 Bug #79 已完成 | [正式架构结论](development.md) |
| T40 | v0.0.26 | ✅ 账号容量与风险摘要已完成并通过真实账号验收 | [完成摘要](work/backlog/T40-account-capacity-risk.md) |
| T42 | v0.0.26 | ✅ 光等状态与缺口已完成并通过真实账号验收 | [完成摘要](work/backlog/T42-power-growth-analysis.md) |
| T46 | v0.0.26 | ✅ 商人账号上下文首批范围已完成并通过真实账号验收 | [完成摘要](work/backlog/T46-vendor-account-context.md) |

## 说明

- `T数字` 和 `Bug #数字` 创建后保持全局唯一且不再改号；需求拆分时使用新的未占用编号。当前任务表按任务编号升序排列，优先级和状态以对应列为准，不通过表格位置表达。
- T41“邮政官安全处理”已取消，需求文档已删除；该编号冻结且不再复用。
- 已移除独立本地攻略库；攻略链接、正文和 AI 整理结果统一进入配装页生成未保存草稿，旧攻略 JSON 只作为遗留备份数据保留。
- UI 只维护 `packages/ui` 的共享产品实现；Markdown 合同记录稳定约束，Web 用于快速预览，Desktop 用于真实功能验收。
- UI 合同静态门禁覆盖稳定标记、表面枚举、排版、层级、菜单主题色和选中态方向线，避免菜单实现重新取得共享视觉职责。
- 已完成任务不再追加功能；新需求建立独立小任务，明确回归才登记独立 Bug。
- 仓库页推荐相关已全部并入 T56（原 T55 / T57 / T59 / T60 / T61 与 Bug #80–#87），编号冻结保留作追溯，不再单独占行。**范围含三层**：任务编号、规范文档层的按类型规则（`development.md:235`/`:237`）、以及工作区未提交的仓库推荐改动；三者都在 T56 内登记。
- T56 口径（2026-09-16 定）：**DIM 愿望单只是数据来源的一种格式，不是面向用户的概念**；判定、标签、文案一律从数据来源取，禁止硬编码。
- T45 / T44 / T46 / T43 只**消费**推荐结果，不并入 T56；Layer 3 改变模型后需在各自 backlog 登记复验项。
- 已完成的 Bug 直接从待办删除，不保留归档行；追溯使用 Git 历史与对应 backlog。
