# 当前待办

> 更新时间：2026-06-22
> 维护规则：这是唯一当前待办文档。当前需求、bug、验收状态和短期优先级统一记录在这里；已经完成、取消或确认无效的条目直接删除，不再保留过程性清单。

## 当前健康度

| 检查项 | 状态 |
|---|---|
| `npx pnpm@9.15.0 test` | 通过：93 个测试文件，296 个测试 |
| `npx pnpm@9.15.0 typecheck` | 通过 |
| `npx pnpm@9.15.0 docs:check` | 通过：包含文档结构检查和全仓 UTF-8 / 乱码守卫 |
| 主要结论 | 当前不是编译或测试层面的阻塞，剩余工作集中在真实数据质量、交互成熟度和真实账号 GUI 验收 |

构建提醒：

- Vite 构建会提示 `DiagnosticsPanel.tsx` 和 `VaultPanel.tsx` 同时被静态和动态导入，当前不阻塞构建。
- npm 会提示 `.npmrc` 中 Electron mirror 配置未来可能不再被 npm 支持，当前不阻塞构建。
- `docs:check` 已接入 `encoding:check`，用于拦截非法 UTF-8、典型 mojibake 和连续问号造成的信息丢失。
- `encoding:check` 已补充多组 UTF-8 被旧代码页误解码后的片段守卫，例如 `\u7441\u546d`、`\u6d5c\u5b2a`、`\u9352\u55d8`、`\u5be4\u9e3f`、`\u93bf\u5d84`；发现用户可见文案乱码时应先补守卫测试，再修正文案。

## 当前 Bug

### P0 · Bug #5：今日/本周页遗失区域数据仍为空

现象：

- 桌面端和此前的真实接口复核都显示 `lost_sector` 为空。

当前判断：

- 当前 Bungie 公共里程碑和本地已补齐的 `DestinyMilestoneDefinition` 里，仍没有可确认的遗失区域条目。
- 不能用“传说 / 大师”等泛关键词兜底，否则会误把其他高难活动当成遗失区域。

下一步：

- 继续查 Bungie 公共数据源里是否还有其他可稳定识别遗失区域的来源。
- 只有在 Bungie API 或本地 Manifest 能确认时才展示，不做猜测。

验收：

- `packages/core/test/daily.liveData.test.ts` 覆盖遗失区域识别。
- 首页“今日 / 本周”面板在真实账号下能显示可确认的遗失区域，或明确说明当前公共数据源缺失。

### P1 · Bug #12：装备详情属性展示仍需真实 GUI 验收

现象：

- 已拥有护甲详情弹窗已经能显示六维属性和总值，但目前主要是开发态验证，缺少一次完整的真实账号桌面端人工确认。

当前状态：

- 已拥有护甲详情弹窗已支持显示六维属性和总值，perk 池保留在后续区块。

关注点：

- 属性区块是否足够靠前，用户打开弹窗后能否第一眼看到核心数值。
- 不同护甲、不同来源、不同屏幕尺寸下，属性展示是否稳定，不会被其他区块挤压。

待确认：

- 真实账号下，属性区块是否始终优先于 perk 池显示。
- 文案和信息密度是否足够清晰。

### P1 · Bug #14：AI 助手对话区过长，需要改成更适合长期对话的布局

现象：

- 连续提问后对话内容竖向堆积过长，需要频繁滚动。
- AI 对话和主页其他内容当前是前后串联关系，提问越多，主页面越像一条超长信息流。

影响：

- 用户查看今日 / 本周、账号、仓库信息后再回到 AI 区域成本偏高。
- 当 AI 成为高频入口后，会明显挤压首页其他模块的首屏可见空间。

目标：

- 让 AI 助手和主内容区并行展示，减少对主页纵向空间的挤压。

后续方向：

- 并入 `P1 · 桌面工作台布局与交互统一`，评估右侧栏、可折叠侧边栏或独立工作区布局，而不是继续追加在首页底部。
- 让最近对话、输入区和主内容区能在桌面端同时存在，减少“来回滚动找上下文”。

### P1 · Bug #16：社区推荐降级提示仍需真实 GUI 验收

现象：

- 逻辑上已经支持在 light.gg 查询失败时降级到 DIM Wishlist 或其他来源，但这件事是否被用户明确感知，还没有经过真实 GUI 验收。

当前状态：

- light.gg 单源失败时，结果已保留 `source_warnings`，UI 已支持降级提示。

关注点：

- 提示文案是否足够明确，不会让用户误以为“没有推荐”而不是“已降级显示其他来源”。
- 提示位置是否足够醒目，不会被详情区其他内容淹没。

待确认：

- 真实调用失败时，用户是否能稳定看到“light.gg 查询失败，当前显示 DIM Wishlist / 其他来源”的提示。

### P1 · Bug #17：AI 原始分析文本保留仍需真实 GUI 验收

现象：

- AI 返回结构化结果异常时，当前已经保留原始文本，但是否真能帮助用户理解“AI 返回了什么”，还没有做过真实界面确认。

当前状态：

- AI 返回无法解析的 JSON 时，已保留 `ai_analysis` 原始文本并在详情页提供展示区。

关注点：

- 原始文本展示是否可读，换行、长度和层级是否足够友好。
- 当 AI 出现异常格式时，用户能否分辨“这是降级展示，不是正常结构化结果”。

待确认：

- 真实 light.gg 返回异常格式时，原始分析文本是否可读、是否足够醒目。

## 当前需求

### P0 · 每日 / 每周面板增强

目标：

- 让首页每日信息从“能显示数据”变成“玩家每天愿意看”。

当前重点：

- 继续强化 `packages/core/src/daily/liveData.ts` 的商人售卖解析，优先覆盖老九、枪匠、艾达、圣人、拉乎尔等常用商人。
- 为商人条目继续补充可确认的关键属性；当前已支持显示“物品名（类型，稀有度；数量 货币）”。
- 把首页面板继续整理成“今日必看 / 本周轮换 / 商人库存 / 数据来源状态”。
- 与 `Bug #5` 配套处理遗失区域来源问题，但坚持“不猜测未确认数据”。

验收：

- `packages/core/test/daily.liveData.test.ts` 覆盖商人售卖和遗失区域解析。
- `packages/desktop/test/daily-panel-items.test.ts` 或新增测试覆盖 UI 文案。
- 首页面板在无数据、部分数据、完整数据三种状态下都有清晰反馈。

### P1 · 桌面工作台布局与交互统一

来源：

- 合并推进 `Bug #14`、原 `Bug #2`、原 `Bug #4`，并覆盖每日 / 本周、AI 助手、诊断状态、社区推荐提示、装备详情信息层级这些相互影响的 UI / 交互问题。

当前问题：

- 首页和部分功能页仍偏单列信息流，连续滚动距离过长；在大屏下左右留白偏多，中间内容区不够满。
- AI 助手目前更像一个长内容区，连续对话后会挤压主页其他模块，用户需要在今日 / 仓库 / AI 之间来回滚动找上下文。
- 登录状态、资料库状态、诊断信息、降级提示、来源说明分散在不同模块里，视觉层级和交互语义不统一。
- 每日 / 本周、资料库、仓库、装备详情弹窗都各自有局部优化，但缺少统一的桌面端信息架构，导致真实账号 GUI 验收很难一次性判断“整体是否顺手”。

当前状态：

- 第一阶段已进入实现：壳层内容区已改为不限制最大宽度，首页已拆为主工作区和辅助侧栏，用于减少长信息流和大屏空白。
- 状态总览卡已收敛到首页侧栏，不再在账号 / 仓库 / 资料库 / AI 助手等每个页面顶部重复显示；子页面只在真实失效时显示针对性的引导或占位提示。
- AI 助手已从页面长区块整理为聊天工作区：主列承载对话和固定输入区，侧列承载上下文、快捷问题和分析结果，避免连续对话把其他模块推远。
- 社区推荐降级提示已改用共享来源状态样式，后续每日 / 本周、诊断、light.gg、DIM Wishlist、本地推荐表的提示应继续复用这套“来源 / 状态 / 原因”视觉语言。
- 第二阶段已开始落地：每日 / 本周来源卡、每日面板加载中提示、轮换细节 pending 提示、状态诊断行、装备详情来源、详情加载、perk 加载 / 空状态、社区推荐加载 / 空状态、社区推荐降级提示、AI 原始分析、AI 助手跳过原因和单件装备 AI 跳过原因已接入共享 `source-status-*` 卡片和胶囊语义，后续新增状态提示不要再单独发明一套 warning / notice 样式。
- 装备详情弹窗已固定第一版信息顺序：基础信息和来源、护甲属性、实际 Roll / perk 池、社区推荐和 AI 原始分析、同名对比、本地备注、写操作。真实 GUI 验收时重点确认这个顺序是否符合玩家打开详情时的阅读习惯。

目标：

- 把桌面端从“长页面信息流”整理成“工作台”：左侧稳定导航，中间主工作区，右侧或折叠区域承载 AI、诊断、来源和操作反馈。
- 提高大屏空间利用率，减少无意义留白，让常用信息能并行查看。
- 统一加载、空状态、失败原因、降级提示、来源可信度、操作确认这些交互反馈，降低用户理解成本。

实施顺序：

1. 先改壳层和首页布局：调整 `ShellLayout` / `shell-content` 的宽度策略，把首页整理成主内容区 + 辅助区，不再只靠纵向堆叠。
2. 再处理 AI 助手：优先做成桌面端右侧栏或独立工作区固定聊天区域，避免长期对话把页面拉长。
3. 再统一状态反馈：把每日 / 本周、社区推荐、light.gg 降级、DIM Wishlist、本地推荐表、诊断信息统一成一套“来源 / 状态 / 原因 / 下一步”的展示模式。
4. 最后补装备详情弹窗层级：护甲属性、perk、社区推荐、AI 原始分析、备注和写操作按固定优先级排列，避免用户每次打开都要重新找重点。

验收：

- 1366px、1920px 和更宽桌面尺寸下，主内容区不再显得窄，左右空白明显减少。
- 首页首屏能同时看到今日 / 本周核心信息、账号或仓库摘要、AI 或诊断入口，不再只有一条长信息流。
- AI 连续对话 10 轮后，不会把首页其他核心模块挤到很远的位置。
- 社区推荐、light.gg 失败、本地推荐表、DIM Wishlist 的来源和降级提示用同一套视觉语言。
- 真实账号 GUI 验收时，可以按“首页工作台、AI 助手、资料库 / 仓库、装备详情弹窗”四条链路检查，而不是逐个零散 bug 检查。

### P1 · 护甲属性与同名对比优化

来源：

- 原 `Bug #11`、`Bug #15` 已转入需求优化，不再单独作为 bug 清单维护。

目标：

- 仓库整理时不再只看一个总分，而是能看懂属性价值、同名差异和评分依据。

当前重点：

- 继续打磨仓库护甲属性筛选体验，确认真实账号下六维属性均来自 Bungie API 可确认数据。
- 同名对比继续区分武器与护甲：武器看 perk 差异、社区推荐命中、wishlist 命中；护甲看属性差异、总值和关键属性组合。
- 继续打磨评分解释：当前已展示基础分、加分项、扣分项和风险提示；后续补“为什么建议保留 / 复查 / 清理”的更明确说明。

验收：

- 单件装备详情能显示已拥有护甲属性数值。
- 仓库能按关键属性筛选护甲。
- 同名对比不再只依赖总分。

### P1 · 真实账号 GUI 验收补齐

目标：

- 把当前已实现但还没完全人工确认的链路补完一次真实账号桌面验收。

当前问题：

- 现在很多功能已经在测试和本地夹具层面通过，但“真实账号 + 真实桌面界面 + 连续使用路径”的最后一公里还没有全部补齐。
- 这会导致逻辑上可用和用户体感可用之间还有一层不确定性。

待验收：

- 资料库搜索和筛选。
- 每日 / 每周面板的真实显示与空状态反馈。
- 装备详情弹窗。
- 社区推荐降级提示和 AI 原始分析展示。
- 本地社区推荐表导入后的资料库 / 仓库 / 详情联动。

验收目标：

- 把“功能存在”提升到“真实账号下能稳定使用、界面反馈清楚、异常路径也可理解”。

### P2 · 社区推荐数据可信度增强

目标：

- 让社区推荐从“功能可用”变成“用户能信任、能理解、能排错”。

当前问题：

- 现在社区推荐已经具备基础展示和降级能力，但来源解释、失败反馈、真实样本覆盖和导入链路成熟度还不够完整。
- 用户能看到推荐，不等于用户知道这条推荐来自哪里、为什么命中、失败时还能相信什么。

当前重点：

- 增加真实武器样本验证。
- 补真实账号 GUI 验收：导入本地社区推荐表后，确认装备详情、仓库命中和资料库标记显示符合预期。
- 评估“本地中文社区推荐表”作为第三数据源，默认不内置未授权数据。
- 优化资料库入口，避免只显示“社区推荐 N 个组合”的弱提示。
- 评估并优先推进“方案 3”：程序直接抓取并解析 `light.gg` 页面为结构化数据，把“网页访问 / 数据解析”与“AI 文本总结”解耦，避免实时分析能力继续绑定 OpenAI Responses API。

验收：

- 每条推荐都能看出来源。
- AI 失败不会静默，只降级但提示原因。
- 本地导入格式、桌面导入入口和离线夹具链路都可实际使用。

### P3 · 活动复盘增强

目标：

- 补齐 Raid Report / Destiny Tracker 方向的基础可读复盘。

当前问题：

- 当前活动能力更偏“能拉到记录”，离玩家真正想看的“最近打了什么、和谁打的、打得怎么样”还有距离。
- 缺少把原始活动记录整理成可回顾、可比较、可扫描摘要的展示层。

任务：

- 按 Raid / Dungeon / 常规活动分组最近记录。
- 增加队伍成员、完成时间、失败 / 完成状态、关键统计的可读摘要。
- 提供“最近 10 场活动”快速复盘视图。

预期结果：

- 用户打开后，不用逐条读原始记录，也能快速回忆最近活动情况。

### P4 · 桌面发布体验闭环

目标：

- 从绿色包可用，逐步走向正式桌面软件体验。

当前问题：

- 目前更偏“能运行的绿色包”，但从普通用户角度看，安装、更新、备份、恢复和问题诊断还没有形成完整闭环。
- 功能本身和“可长期使用的软件体验”之间还有一段产品化收尾工作。

任务：

- 评估自动更新方案。
- 评估安装包方案。
- 增加本地备份 / 恢复向导。
- 评估托盘图标是否真的符合使用场景。
- 梳理 `%APPDATA%/d2-tools` 数据目录迁移、备份和诊断导出的用户说明。

预期结果：

- 让用户即使不懂开发环境，也能更安心地安装、升级、迁移和排查问题。

## 代码重构

> 当前代码集中在少数几个巨型文件上，阻碍并行开发和独立测试。已确认方向：按菜单拆成互相独立的 feature，避免改一个菜单里的功能影响另一个菜单。

详细设计见 `docs/work/backlog/2026-06-22-desktop-feature-split-design.md`。

### 现状

| 文件 | 行数 | 承载内容 |
|------|------|---------|
| `HomePage.tsx` | 368 | 壳层、导航、页面切换和 feature hook / 页面接线 |
| `VaultPanel.tsx` | 905 | 仓库主 UI、筛选结果展示和仍待继续拆出的子组件 |
| `ipc.ts` | 35 | IPC 聚合注册入口，具体 handler 已按领域拆入 `ipc/` 子模块 |
| `client.ts` | 586 | 全局类型定义 + `window.d2` 声明 |

### 核心目标

- `HomePage.tsx` 只保留壳层、导航和菜单注册。
- 每个菜单一个独立 `features/<menu>/` 目录。
- 每个菜单自己管理状态、副作用、组件、domain 逻辑和测试。
- 菜单之间禁止直接 import；共用能力必须进入 `shared/`。
- 不创建统一的 `useAppState.ts` 巨型状态中心。

### 目标结构

```text
packages/desktop/src/renderer/
  pages/
    HomePage.tsx
  features/
    home/
    account/
    vault/
    library/
    ai/
    settings/
    daily/
    diagnostics/
    loadouts/
  shared/
    components/
    hooks/
    domain/
    api/
```

### 阶段 1：建立 feature 架构和菜单边界

目标：

- 创建 `features/` 和 `shared/` 目录。
- 将首页、每日、AI、设置等相对独立页面先迁入 feature。
- `HomePage.tsx` 改为菜单注册和路由壳。
- 迁移相关字符串测试，不再让所有测试都盯着 `HomePage.tsx`。

当前进展：

- 已创建 `features/home/`、`features/daily/`、`features/ai/`。
- 首页工作台已迁入 `features/home/HomeDashboard.tsx`。
- 今日 / 本周面板已迁入 `features/daily/DailyPage.tsx`。
- AI 菜单入口已迁入 `features/ai/AiPage.tsx`。
- 设置页已迁入 `features/settings/SettingsPage.tsx`。
- 资料库搜索页已迁入 `features/library/LibraryPage.tsx`。
- 仓库菜单接线已迁入 `features/vault/VaultPage.tsx`，后续仓库内部 DIM 化改造集中在 vault feature 推进。
- `HomePage.tsx` 已改为挂载这些 feature 入口，不再直接包含首页工作台、每日面板、AI 空状态、设置页、资料库搜索和仓库菜单 JSX。

验收：

- 改每日页、AI 页或设置页时，不需要改账号页和仓库页文件。
- `HomePage.tsx` 不再直接包含这些菜单的大段 JSX。

### 阶段 2：拆账号页并落地账号页 UI 重构

目标：

- 将账号页迁入 `features/account/`。
- 按已确认设计实现“上方一体化主区 + 下方低频区”。
- 账号页保留当前装备、当前角色背包、材料、货币、邮政官。
- 账号页状态和动作留在 account feature 内。

当前进展：

- 账号页已迁入 `features/account/AccountPage.tsx`。
- 账号页已调整为上方 `account-primary-workbench`、下方 `account-secondary-workbench`：上方集中当前角色装备和背包，下方放材料 / 货币 / 消耗品、游戏内配装栏和邮政官。
- 账号读取、登录、Manifest 初始化、活动摘要、DIM Wishlist 读取和仓库社区命中已迁入 `features/account/useAccountWorkspace.ts`。
- `HomePage.tsx` 不再直接包含账号页 JSX，也不再维护账号域状态；只把账号 feature hook 的结果传给页面和其他共享能力。
- 原账号页里的 DIM Wishlist / 本地社区推荐导入入口已迁到 `features/vault/VaultPage.tsx`，让仓库页继续承担装备整理和推荐数据维护。

验收：

- 当前装备和当前角色背包同属上方主工作区。
- 材料、货币、邮政官位于下方低频区。
- 账号页测试只读 `features/account/` 或渲染 `AccountPage`。

### 阶段 3：拆资料库、loadout 和共享装备详情

目标：

- 将资料库页迁入 `features/library/`。
- 将 loadout 页面和纯逻辑迁入 `features/loadouts/` 或 `shared/domain/loadouts/`。
- 抽出 `shared/components/ItemDetailModal.tsx` 和 `shared/hooks/useItemDetail.ts`。

验收：

- 账号、仓库、资料库各自控制自己的详情打开状态。
- 修改资料库搜索或收藏，不影响账号页和仓库页。
- loadout 对比和命中逻辑有独立纯函数测试。

当前进展：

- 本地方案库已迁入 `features/loadouts/LoadoutsPage.tsx`，导航新增独立“配装”页。
- 配装对比、缺失清单、来源匹配和缺失件就位判断已迁入 `features/loadouts/loadoutViewModel.ts`，`HomePage.tsx` 不再直接包含本地方案库 JSX。
- 本地方案列表、当前选择、对比选择、重命名草稿、差异开关、加载、重命名和删除已迁入 `features/loadouts/useLoadoutTemplates.ts`。
- 单件补齐 / 单件装备的按钮反馈状态和定时清理已迁入 `features/loadouts/useLoadoutActionFeedback.ts`。
- 生成转移计划和复制缺失清单这两个低风险模板动作已迁入 `features/loadouts/useLoadoutTemplateActions.ts`，`HomePage.tsx` 不再直接维护这两段剪贴板 / 计划生成逻辑。
- 保存当前角色配装、最高光等装备、游戏内配装栏应用 / 覆盖、缺失件补齐、单件补齐和单件装备已迁入 `features/loadouts/useLoadoutWriteActions.ts`。
- 方案命中 lookup / 匹配逻辑已迁入 `shared/domain/loadouts/loadoutLookup.ts`，账号页和仓库页不再各自维护重复匹配实现。
- 装备详情弹窗已迁入 `shared/components/ItemDetailModal.tsx`，详情打开、缓存、加载和关闭状态已迁入 `shared/hooks/useItemDetail.ts`；`HomePage.tsx` 不再直接包含装备详情弹窗 JSX。
- 装备详情的社区推荐加载、AI 解读、备注 / 标记、复制文本、同名批量标记和单件写操作已迁入 `shared/hooks/useItemDetailWorkspace.ts`。
- `HomePage.tsx` 现在只负责把详情 hook 接到账号、仓库、资料库和弹窗，不再直接维护详情弹窗内部动作。

### 阶段 4：拆仓库页和 VaultPanel 内部

目标：

- 将仓库菜单迁入 `features/vault/`。
- 逐步把 `VaultPanel.tsx` 内部拆为筛选、排序、批量选择、同名对比等 hook / domain。
- 后续仓库页 DIM 化改造在 `features/vault/` 内推进。

验收：

- 仓库页改造不需要修改账号页文件。
- 仓库筛选、排序、批量选择、同名对比各自有独立测试。

当前进展：

- 仓库菜单已由 `features/vault/VaultPage.tsx` 接线，`VaultPanel.tsx` 继续作为仓库主 UI。
- 批量选择、可见结果追加 / 移除、选择摘要和清理候选选择已迁入 `features/vault/vaultSelection.ts`。
- 仓库筛选、排序、分组、位置 / 框架选项和筛选标签常量已迁入 `features/vault/vaultFilters.ts`。
- 清理清单、游戏内定位文本、同名对比摘要、DIM Wishlist 命中计数和同名批量标记计划已迁入 `features/vault/vaultCleanup.ts`。
- 批量标记、批量移动、同名组标记和清理候选动作状态已迁入 `features/vault/useVaultBatchActions.ts`。
- 仓库本地标记保存、批量标记保存、批量解锁和批量转移写操作已迁入 `features/vault/useVaultWriteActions.ts`。
- `VaultPanel.tsx` 当前仍保留仓库主 UI 和部分重复组交互展示，后续 DIM 化改造时继续拆子组件，不再把新写操作塞回 `HomePage.tsx`。

### 阶段 5：拆主进程 IPC

目标：

- 将 `ipc.ts` 按功能域拆为独立注册模块。
- `register.ts` 只做聚合注册。

验收：

- 新增某个菜单 API 时，只改对应 IPC 子模块。
- preload 和 renderer API 类型保持稳定。

当前进展：

- `packages/desktop/src/main/ipc.ts` 已收敛为聚合注册入口。
- 已按领域拆出 account、actions、activities、analysis、auth、authSession、community、config、daily、diagnostics、library、loadouts、manifest、startup、vault、wishlist 等 IPC 子模块。
- 后续新增 IPC handler 应优先落到对应领域子模块，不再扩张 `ipc.ts`。

### 暂不改的部分

- core 包：当前依赖边界清晰，本轮不做 core 重构。
- http 包：规模很小，本轮不做结构调整。

---

## 暂不优先

### 装备模板 / 本地配装模板使用引导（原 `Bug #3`）

- 当前问题：已有配装相关能力，但用户不容易理解“模板”“当前装备”“本地保存方案”之间的区别，也不知道入口和推荐使用顺序。
- 后续目标：补清晰的引导、空状态说明和典型使用场景，降低第一次使用门槛。

### 完整 TWID 级日报 / 周报聚合（原 `Bug #7`）

- 当前问题：当前“今日 / 本周”更偏结构化接口数据展示，还不是接近 TWID / 中文日报机器人的完整聚合视图。
- 后续目标：补更多轮换、活动提示、重点摘要和分享文本，让它更像玩家每天愿意看的摘要页。

### DIM 整理工具文案与交互边界重命名（原 `Bug #9`）

- 当前问题：当前“整理”“清理”“候选”“建议保留”这些词在语义边界上还不够稳，容易让用户误解为工具会直接执行高风险写操作。
- 后续目标：把文案改得更接近“分析建议 / 候选清单 / 人工确认执行”，减少误解和心理负担。

### 完整 DIM 级拖拽式装备移动

- 当前问题：当前已有写操作和装备转移基础能力，但还没有 DIM 那种高自由度、低摩擦的拖拽移动体验。
- 后续目标：支持更直观的角色/仓库间拖拽、批量移动和冲突反馈。

### 完整护甲属性优化器

- 当前问题：目前能看单件护甲属性、基础评分和筛选条件，但还没有真正的“整套护甲搭配求解器”。
- 后续目标：支持按职业、属性目标、模组约束和异域装备组合计算更优配装方案。

### Destiny Tracker 级排行榜

- 当前问题：当前活动和账号数据还没有形成可比较、可排序的战绩排行榜视图。
- 后续目标：补玩家常见的 PVP / PVE 统计排行、关键指标对比和阶段性表现趋势。

### Raid Report 级完整副本复盘

- 当前问题：当前活动复盘仍偏基础记录展示，离 Raid Report 那种副本履历、通关细节和队伍表现分析还有明显差距。
- 后续目标：补齐副本级记录、历史趋势、关键节点和队伍协作视图。

### 默认内置未授权社区表格数据

- 当前问题：社区推荐能力已经在扩展，但默认内置第三方社区表格会带来授权、维护和过期风险。
- 当前结论：在明确授权前，不内置任何未授权社区数据，只保留导入能力和本地使用链路。

### 公网服务、多用户共享链接、图片化报告发布

- 当前问题：这些能力会把本地单机工具扩展成带发布、分享、托管属性的产品，复杂度和安全边界会陡增。
- 当前结论：现阶段继续坚持“本地优先、单用户、自主授权”，暂不扩展到公网服务和分享分发场景。

## 验证命令

```powershell
npx pnpm@9.15.0 test
npx pnpm@9.15.0 typecheck
```
