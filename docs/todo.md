# 当前待办

> 更新时间：2026-08-26
> 本文件只保留当前任务、状态和下一步。详细范围与验收标准由对应 backlog 维护，阶段过程使用 Git 历史追溯。

## 健康度

| 检查项 | 状态 | 备注 |
|---|---|---|
| GitHub CI | ⏳ push 后异步执行 | 执行 frozen install、`pnpm test`、共享 Shell 视觉契约、UI 合同静态门禁和构建后的 `pnpm typecheck:ci`；普通 push 不等待结果 |
| Release 门禁 | ⏳ 发版时执行 | 必须通过对应平台的 Git Release 入口完成本地门禁、GitHub Actions、安装包和 GitHub Release |
| Agent 自动验证 | ⛔ 默认禁用 | 只有用户明确要求本地测试、构建或打包时才执行 |

## 当前任务

| 编号 | 优先级 | 状态 | 任务 | Backlog | 下一步 |
|---|---|---|---|---|---|
| T7 | P1 | 🟡 待验收 | AI 工作台、原生能力与攻略配装工作流 | [任务说明](work/backlog/T7-guides-library-and-derived-targets.md) | 攻略链接、正文和 AI 整理文本已收敛到攻略页唯一入口；确认后优先按当前账号生成方案，配装页只审阅类型化成果。DIM Wishlist 已迁入仓库“目标与匹配”的低频操作，设置不再承载业务导入，自定义 Hash / JSON / CSV 规则仅保留遗留兼容读取。下一步使用真实账号和 Desktop 实窗验收攻略导入、要求确认、账号适配、配装交接、Wishlist 导入/替换/移除、遗留规则提示、目标追溯、Armor 重算、方案保存及安全执行链路；本轮产品入口调整尚未运行本地自动化验证 |
| T8 | P1 | 🟡 待验收 | 删除应用级写操作总开关，账号、仓库、配装和装备详情在 Bungie 权限满足时直接进入确认流程 | — | 使用真实账号在 Desktop 实窗检查单件锁定/装备/转移、仓库批量操作、一键最高光等和配装计划；确认 OAuth 权限不足时保留 Bungie 错误，确认旧配置可兼容读取 |
| T9 | P2 | 🟡 待验收 | 账号页增加 DIM 式八槽最高光等与账号掉落光等明细 | — | 使用三职业真实账号检查 `/8` 分数、八槽差值、职业兼容、异域限制、仓库和其他角色来源、完整八槽的数据不完整状态，以及账号最高与当前一键装备范围不一致时的提示；Desktop 实窗检查宽屏浮层和窄屏文档流布局 |
| T10 | P1 | 🟡 待验收 | 本地快速启动与 CI 门禁分层 | — | 统一 Windows / macOS Desktop 增量启动器；确认端口冲突、独立视觉 profile、workspace exports 检查和 `ci:local` 在干净环境下的行为；本轮未运行本地自动化验证 |

## Bug

| 编号 | 优先级 | 状态 | 问题 | 修复范围 | 下一步 |
|---|---|---|---|---|---|
| Bug #6 | P1 | 🟡 待验收 | 未配置 Bungie 或未登录时，账号、仓库、商人空态的卡片高度、对齐和恢复按钮语义不一致 | 统一页面级空态布局；仓库和商人按配置 / 登录状态显示正确入口；局部空态不再被页面级高度影响 | Desktop 实窗检查 `light / dark × 1280 / 980 / 760`，确认无按钮拉伸和横向溢出 |
| Bug #7 | P1 | 🟡 待验收 | Bungie 配置、资料库和登录步骤分散在不同菜单，未完成前仍可进入半可用首页 | 保留唯一设置页；应用级启动门禁按“Bungie 配置 → 登录 Bungie → 首页”推进，资料库后台准备与登录并行，不阻塞 OAuth 和账号原始读取；失败时保留原因与重试，名称和图标在资料库完成后补齐 | Desktop 实窗检查首次启动、配置保存、资料库后台成功/失败、登录成功/取消和菜单锁定，确认登录不等待资料库 |
| Bug #8 | P2 | 🟡 待验收 | 仄未开放或没有有效轮换时：首页仍显示“打开仄的完整库存”按钮 | 首页库存入口只在已确认有效的仄轮换存在时显示；未开放、读取失败或无有效库存时保留状态说明但不显示不可执行入口 | Desktop 实窗检查仄开放、未开放、无库存和部分库存四种状态 |
| Bug #9 | P2 | 🟡 待验收 | 铁旗已开放时偶尔只显示“结束时间待确认”，没有利用铁旗商人刷新时间作为可用的时间提示 | 公共里程碑 `endDate` 只映射为“距结束”；缺失时将已确认铁旗主商人的未来 `nextRefreshDate` 映射为独立的 `next_refresh_at`，界面显示“距库存刷新”，不冒充活动结束时间 | Desktop 实窗检查公共里程碑有无 `endDate`、仅角色活动、商人刷新时间过期和正常后备四种状态 |
| Bug #10 | P1 | 🟡 待验收 | 一键装备最高光等使用乐观 patch 后，Bungie 真实快照未生效时应用仍可能长期显示已装备 | 执行前权威刷新并重建方案；仓库转移和最终装备在后台按实例 ID 持续读取 Bungie Profile，不切换账号页前台加载状态；明确失败立即收口，已受理实例自动退避刷新直到真实进入角色并最终装备，只报告权威快照确认结果 | 使用真实账号检查背包装备、仓库转移后装备、活动中禁止换装、Bungie 长延迟同步、明确失败和部分失败；确认账号页不闪烁刷新且游戏与应用最终状态一致 |
| Bug #11 | P1 | 🟡 待验收 | 武器详情的“获取方式”在长价格和刷新信息下会将来源说明压缩为逐字换行 | 来源名称保持左侧固定列；来源说明和状态、价格、刷新时间改为右侧上下两层，保留全部真实字段并允许辅助信息自然换行 | Desktop 实窗检查多货币商人来源、历史来源和公共活动来源，确认说明不再逐字换行且价格、刷新时间完整可读 |
| Bug #12 | P1 | 🟡 待验收 | 萨拉丁等商人返回不可装备的售卖代理 Hash 时，武器被误分类为其他物品，详情只显示基础属性而缺失 Perk 和配置 | 通过 `weaponPatternHash` 解析正式武器定义，Offer 仍保留代理 Hash、当前 Roll、价格和购买状态；社区匹配与推荐改用正式 Hash；商人详情不再直出角色 ID，并恢复字段间距 | Desktop 实窗检查煅炉誓言 `2413746134 → 1076810832`、其他聚焦武器和普通可装备 Offer，确认 Perk 池、当前售卖 Roll、价格、购买状态和角色范围均正确 |

## 说明

- UI 只维护 `packages/ui` 的共享产品实现；Markdown 合同记录稳定约束，Web 用于快速预览，Desktop 用于真实功能验收。
- UI 合同静态门禁覆盖稳定标记、表面枚举、排版、层级、菜单主题色和选中态方向线，避免菜单实现重新取得共享视觉职责。
- 新增 Bug 必须使用全局唯一编号并在本文件中登记。
