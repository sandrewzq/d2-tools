# 应用工作区合同

> 本文件合并共享 Shell、设置、首页、账号、攻略、资料库、仓库和商人的结构、真实数据与状态边界。视觉细节统一遵守 [全局视觉合同](global-visual-contract.md)。

## 共享 Shell

稳定契约：`shell.topbar`、`shell.status-strip`、`shell.sidebar`、`shell.page-header`、`shell.page-content`、`shell.assistant`。

| 区域 | 真实绑定 | 约束 |
|---|---|---|
| 顶部栏 | 品牌、主题、GitHub、设置、AI、窗口控制 | 单一连续 Shell chrome；无平台窗口能力时只隐藏窗口控制 |
| 状态组 | `ShellStatusItem[]` 与对应 action | 状态项组成连续边界；状态 key 负责逻辑，label 只负责文案，不使用独立胶囊卡片 |
| 侧栏 | 当前账号摘要、主导航、底部动作 | 一级导航使用稳定图标和 `aria-current`；窄栏保留可访问名称 |
| 页面标题 | `ProductShellHost` 页面元数据与真实页面 action | 内容页不得复制第二个页头 |
| 主内容 | 当前菜单共享页面 | 普通页面唯一纵向滚动所有者，不建立同轴嵌套滚动 |
| AI 助手 | 会话、页面上下文、只读能力结果与数据引用 | 宽屏停靠，`980px` 及以下转 Drawer；只保留单一 AI 工作台，不建立固定任务 Tab |

Shell、页头、页面 gutter、首层分栏和全局 token 由共享层持有；菜单 CSS 不得覆盖 `.shell-*` 或无菜单前缀的工作区规则。

页面可以向 `ProductShellHost` 注册单一离开保护，只用于确实会因菜单卸载而丢失的未提交用户输入。Shell 侧栏、页内跨菜单动作和自绘窗口关闭命中保护时必须停留在当前页面并显示共享结构化确认弹窗；刷新、系统关闭或其他原生卸载路径使用平台 `beforeunload` 保护。取消后焦点回到来源操作，确认后才执行原导航或关闭动作。确认弹窗通过顶层 Portal 脱离可能被抽屉设为 `inert` 的页面内容，显示期间隔离应用背景，并始终位于 AI 抽屉、详情或菜单内容之上。弹窗打开时初始焦点落在安全的取消操作，`Escape` 等同取消，`Tab / Shift+Tab` 不得离开弹窗。新增或改造的共享页面确认流程不得调用 `window.confirm`，也不得把已持久化状态或普通筛选条件登记为离开保护。

顶部状态按行动紧迫度分为 `quiet / standard / attention / critical`，不按状态项数量平均分配视觉权重。正常配置、健康确认和当前版本属于低权重事实；发现新版、下载进度、待重启安装、资料库修复和真实失败属于可操作事件。状态项位置必须稳定，不得因状态或权重变化重新排序；高权重事件只通过颜色、背景、图标和文字增强，并保持连续边界，不建立独立胶囊卡片。窄窗口的“状态”入口显示待处理数量，展开后继续保持相同顺序。

应用更新状态必须区分 `idle / checking / not_available / available / downloading / downloaded / error`：未检查不得显示“最新”；已是最新只显示中性版本号；发现新版显示目标版本并使用信息色；下载中显示目标版本和进度；下载完成显示“待重启安装”并升为最高操作权重；失败使用错误色。顶部更新项只负责进入设置页更新区，不直接触发下载或重启安装。

AI 工作台发送前可以按用户问题自动运行受控只读能力，每轮最多三项。当前覆盖装备定义、Perk、账号实例、当前商人 Offer、本地配装检查和 `armor.plan`；宽泛整理或推荐问题不得伪造实体查询。Armor 调用必须同时识别当前六维数值目标和明确职业，旧六维、缺职业、缺目标或升级基线不明确时不得猜测。确定性结果随完整 `result_id` 进入模型上下文，回答底部显示缩短后的数据引用和状态；失败、无结果、未登录或缓存不可用必须保留真实限制。AI 工作台不得调用写操作、后台刷新或把聊天文本当作确认。

每次成功问答必须创建轻量上下文快照，消息保存快照 ID，快照保存页面和账号范围、Manifest 版本、基础上下文指纹、实际提示指纹及能力结果引用，不保存完整提示、装备正文或凭证。恢复会话后，当前上下文与最近快照不一致时顶部状态显示“已恢复历史快照”，上下文抽屉显示快照时间；继续提问只携带有限前文并创建新的当前快照。请求失败时恢复发送前消息，不写入无回答的历史条目。

只读能力调用审计保存在 renderer 本地存储，最多 100 条，只记录能力名、脱敏输入摘要、结果 ID、状态、耗时、警告码、结果数量和证据 ID。设置页“复制脱敏诊断导出”在主进程诊断后附加最近能力审计；不得写入完整提示、能力完整结果、账号装备正文、Token、Cookie 或 API Key。

明确的配装、构筑或攻略整理回答可以在消息底部显示单一 `guide_capture` 成果行，包含草稿标题、确定性结果引用数量和“送到攻略页”命令。成果行是消息内连续区域，不嵌套对象卡。交接后关闭 AI Drawer、打开攻略页并预填未保存正文草稿；必须继续经过保存、要求识别、人工确认、账号匹配和显式保存，不得直接创建方案或触发写操作。

`armor.plan` 返回候选时，消息成果行优先显示 `armor_solution_comparison`，标明规划模式和达标候选数量。交接文本只包含原始职业、六维目标、候选 ID 和结果引用，不复制候选最终六维作为约束；配装页显示“复核 AI 护甲方案”，重新解析并要求复核模组预算、套装、位置范围和真实实例。

页面动作权重由当前页面职责、人工使用频率、自动刷新覆盖和阻断状态共同决定，不按“刷新”文案统一映射颜色：

| 页面与状态 | 主操作 | 次操作 |
|---|---|---|
| 账号正常 | 页头 `刷新账号`；承担账号页核心数据更新 | 页头 `重新授权`；低频身份维护 |
| 账号未配置 Bungie | 内容空态中的 `去设置 Bungie` | 不显示无法执行的刷新或登录动作 |
| 账号未登录 | 内容空态中的 `登录 Bungie` | 不显示无法执行的刷新动作 |
| 账号无数据 | 内容空态中的授权或刷新入口；直接恢复当前工作区 | 页头不重复显示相同操作 |
| 仓库正常 | 页头 `刷新账号装备`；同步当前工作区 | — |
| 仓库未配置 Bungie | 内容空态中的 `去设置 Bungie` | 不显示 `刷新账号` |
| 仓库未登录 | 内容空态中的 `登录 Bungie` | 不显示 `刷新账号` |
| 仓库无数据 | 内容空态中的 `刷新账号`；直接恢复当前工作区 | 页头不重复显示相同操作 |
| 首页正常 | — | `刷新公开情报`；启动、重置边界和失败重试已有自动机制 |
| 首页读取失败 | 错误 Callout 内的 `重新读取公开情报` | 页头不重复显示同一刷新动作 |
| 商人正常 | — | `刷新商人库存`；进入页面、切换商人或收到明确数据事件时按需刷新 |
| 商人后台同步 | — | 页头刷新保持可点击；后台读取状态只在商人状态区反馈 |
| 商人读取失败且仍有缓存 | 页头 `刷新商人库存` 临时升权 | — |
| 商人完全无数据 | 内容空态中的 `重新加载商人库存` | 页头不重复显示同一操作 |
| 商人未配置 Bungie | 内容空态中的 `去设置 Bungie` | 不显示无法执行的刷新动作 |
| 商人未登录 | 内容空态中的 `登录 Bungie` | 不显示无法执行的刷新动作 |
| 设置账号分区 | `刷新账号`；该行的主要数据操作 | `重新授权`；低频维护入口 |

首次启动时仍挂载完整 `ProductShellHost`，但由共享启动门禁统一推进“Bungie 配置 → 登录 Bungie → 首页”；资料库与登录并行，未完成前只允许进入设置和当前步骤，不显示半可用首页。配置区必须提供 Bungie 官方 Application 页面入口、字段对应关系和固定 OAuth 回调地址。配置保存后自动在后台准备资料库，不创建独立向导页或第二套配置表单；资料库准备失败时保留明确原因和重试动作，不阻塞 OAuth 登录和账号原始读取，名称、图标及高级解析在资料库完成后补齐。

同一业务动作可以因页面职责不同使用不同权重。每个页面或局部失败状态最多突出一个主操作；空态或错误区已持有主恢复入口时，页头不得重复同一操作。进行中保持原标签和稳定宽度，通过 `aria-busy`、禁用状态和邻近反馈表达进度。

商人刷新必须区分后台同步和手动请求：进入页面、切换商人、作用域变化和明确数据事件属于按需后台同步，不使用固定间隔定时器或轮询；Xur 到访 / 离开边界属于低频明确事件，可以保留一次性边界定时器。后台同步不得清空当前库存，也不得禁用页头刷新。只有用户已经发起的手动刷新可以临时禁用该按钮。切换商人使用短防抖，快速浏览目录时只请求最后停留的商人；手动请求使用后发请求优先的现有序号机制接管后台请求。

## 设置

稳定契约：`settings.workspace`。

固定八个分区：概览、语言与外观、账号、资料库、Bungie 接口、AI 助手、数据备份与迁移、诊断与操作日志。设置只承载应用配置，不承载攻略导入、推荐数据导入或其他业务工作流。

| 区域 | 真实字段与 action | 状态边界 |
|---|---|---|
| 概览与更新 | `appUpdateSnapshot`、账号/资料库/Bungie/AI/后台任务摘要、检查/下载/安装/诊断 | 未检查保持中性；只有发现版本后允许下载，下载完成后允许安装 |
| 语言与外观 | `languagePreferences`、`colorMode`、`density` | 界面语言与资料库语言分开建模；跟随开启时禁用独立资料库语言 |
| 账号 | `accountSummary`、读取状态、刷新时间 | 加载中禁用刷新；未授权保留重新授权入口；写操作由对应页面确认，不设置应用级总开关 |
| 资料库 | Manifest 状态、检查、更新、修复 | 失败时保留上一份可用资料；进行中禁用冲突操作 |
| Bungie | 官方 Application 入口、API Key、Client ID、Client Secret、回调地址和数据目录；保存后自动准备资料库 | 三个配置值均明文展示；三者都不进入仓库示例或诊断快照，诊断导出仍须脱敏；字段不完整时阻止保存；资料库准备失败不回滚已保存配置 |
| AI | 协议、Key、Base URL、模型、light.gg 能力 | 无配置、加载、测试中、失败和不支持能力分别呈现 |
| 备份与迁移 | 创建/恢复备份、清缓存、迁移说明、打开目录 | 调用真实平台能力，不伪造完成结果 |
| 诊断与日志 | `backgroundTasks`、`actionLog`、AI 只读能力审计、筛选、刷新、复制诊断 | 空日志保留筛选和诊断入口；失败项提供脱敏诊断；能力审计通过统一诊断导出附带 |

布局合同：

- `>1280px` 目录 `240px`，`981-1280px` 为 `210px`，`761-980px` 为 `190px`；`760px` 及以下目录置顶并限制高度。
- 状态摘要使用单一 `4px` `SurfaceFrame`，内部连续单元直角；三列，`1280px` 以下两列，`980px` 以下单列。
- 设置行使用弹性说明列和右侧控制列；宽屏控制列 `282px`，中屏 `260px`，`980px` 以下单列。
- 重复操作按钮使用 `34px × 144px`；Select 和 Field 填满控制列。
- 账号、资料库、更新和日志的精确时间使用本地时区 `YYYY/MM/DD HH:mm:ss`。
- OAuth token、Bungie API Key、Client Secret 和 AI Key 不得写入仓库快照。

## 首页

稳定契约：`home.workspace`、`home.vendor-stock`。

| 区域 | 真实绑定 | 关键状态 |
|---|---|---|
| 刷新节奏 | `dailySummary.daily_reset`、`weeklySummary.weekly_reset` | 使用本地系统时区；顶部刷新矩阵只展示每日和每周重置，绝对更新时间靠右，动态倒计时使用全应用统一时间强调色 |
| 核心活动 | `weeklySummary.priorities.*.entries` | 日落可独立展示；轮换突袭和轮换地牢只在 Bungie 账号已登录且角色活动数据成功读取时展示。未登录、读取失败、刷新中或数据过期时直接隐藏，不显示 pending、候选名称或旧缓存 |
| 铁旗信号 | `weeklySummary.iron_banner`、角色 Activity Challenge、VisibleRewards | 与“本周活动焦点”按约 `60% / 40%` 同行；只展示玩法、开放状态、倒计时、当前角色挑战和明确的直接奖励。当前角色进度使用“当前角色挑战”，账号范围使用“账号内 N 个角色可参与”，不得将两个维度写成同一句角色状态。萨拉丁库存、聚焦数量、具体装备、价格和购买条件只在商人页展示；条件可见的通用熔炉掉落不进入首页奖励摘要 |
| 周信号 | `special_event`、`weekly_bonus` | 本周活动焦点固定展示；只读取活动修饰词明确标记为“焦点活动”的内容，其他限时活动确认后独立展示，不覆盖铁旗 |
| 仄库存 | `dailySummary.sources.vendors`、`HomeBriefing.fetched_at`、`isXurActiveAt`、`nextXurBoundaryAt` | 整体使用单一 `summary-frame`，标题、到访/离开时间、库存状态和来源同框；内部摘要不重复外框；不使用 Vendor `nextRefreshDate` 替代；加载、不可用或失败时不回退旧库存 |
| 操作 | `onRefreshDaily`、`onNavigate("vendors")`、`onOpenXurOffer`、`onOpenWeeklyActivityReward` | 只有可确认且含 Hash 的奖励或武器/护甲 Offer 可打开统一详情 |

首页保持“刷新矩阵、周信号、活动摘要、商人库存”四层信息结构。周信号层优先展示正在开放的铁旗和本周活动焦点，但不承担配置、登录或“下一步行动”引导。日落卡可在公开数据可用时展示；轮换突袭和轮换地牢仅在登录 Bungie 且角色活动读取成功、数据未过期时展示，未登录或失败时不保留空卡和旧名称。已显示的轮换卡需要能追溯到 Bungie 角色活动和当前 Manifest，不能由公共里程碑或第三方站点单独推断。核心活动宽屏同排展示；中等工作区宽度改为日落独占首行，已显示的突袭与地牢并排，`700px` 以下再恢复单列。桌面活动条目最小行高为 `98px`，`1280px` 以下允许内容撑开。突袭和地牢按真实条目数量决定内容高度，单条目的日落卡只拉伸外框与同排双条目卡片底边对齐，日落内容在拉高区域内垂直居中，不拉伸标题或奖励条目。描述最多展示两行，奖励按可用宽度优先并排；`760px` 以下恢复内容自适应高度。铁旗与本周活动焦点在周信号层同行且等高，其他已确认限时活动另起一行；没有挑战时不显示 `0/0`，不在首页显示任何 Vendor 零值计数或具体库存。仄库存模块使用唯一首层 `summary-frame`，内部状态和摘要不再叠加框线；Offer 仍是对象卡。宽屏 Offer 四列，`1280px` 以下两列，`980px` 以下单列。

## 账号

稳定契约：`account.slot-comparison`。

- 账号目录使用完整 `tablist / tabpanel`，按“当前角色 / 整个账号”分组：当前角色固定包含战斗装备、角色物品与配置、任务与赏金、邮政官；整个账号固定包含材料与货币、账号战绩。目录顺序不得按运行状态动态重排。角色使用 `ContextSwitcher` 和 `aria-pressed`，不是页面导航。
- 目录侧栏不使用任务或普通物品总量制造虚假优先级；只有邮政官等需要快速识别的清单显示真实总数。账号工作区内容宽度大于 `700px` 时竖向目录使用上下方向键，进入 `700px` 以下横向目录后使用左右方向键；`Home / End` 跳到首尾，朝内容区方向的方向键将焦点送入当前面板。
- 账号首屏优先回答“当前角色、当前装备光等、账号最高光等、当前角色与仓库可组成的最高光等方案、装备与背包差异”。摘要只显示账号名、角色数、仓库容量、快照时间以及“缓存 / 刷新 / 已读取”状态，不突出 Membership 长 ID。角色切换使用真实角色徽标，并支持方向键、`Home`、`End`；账号工作区进入 `700px` 以下布局时，目录和角色切换均保持单行横向滚动，不堆叠成多行按钮墙。
- 角色切换项同时显示当前装备光等和遵守职业、异域同时装备限制的账号最高光等；最高光等使用整数加 `/8` 余数展示。摘要操作区使用“最高光等方案”作为只读预览入口，点击后打开账号菜单私有浮层；真正执行写操作的按钮独立使用“一键装备最高光等”。桌面浮层覆盖内容区、窄屏回到文档流，不改变共享 Shell chrome；点击触发按钮与浮层之外的区域自动关闭，`Escape` 关闭后焦点返回触发按钮。
- 窄屏横向目录保留“当前角色 / 整个账号”可见分组标记；每个目录 Tab 的无障碍名称同时包含作用域和功能，横向滚动不得改变目录顺序或隐藏作用域含义。光等浮层在短视口中限制最大高度并在浮层内部滚动，不能把底部说明裁出可视区域。
- 分类切换必须自动关闭光等浮层；横向目录存在实际溢出时显示边缘渐隐并让当前选中项滚入视口，内容未溢出时不显示误导性的滚动提示。
- 光等浮层只展示真实的最高光等装备方案，不再提供不满足同时装备条件的“八槽最高”理论视图。顶部以“当前装备光等 → 装备后光等”表达结果变化，八个槽位按三武器、五护甲排序，使用明确列头显示位置、装备与来源、光等及相对方案整数均值的正负差值；缺少任一槽位时仍保留完整八槽并明确显示方案数据不完整。
- 账号最高光等允许读取其他角色持有的可用装备，邮政官不参与计算；当前一键装备只使用目标角色与仓库。两者一致时明确说明当前方案已达到账号最高光等；两者不一致时只显示账号最高光等和跨角色来源限制，不再通过第二套槽位列表制造可直接执行的错觉。
- 装备按武器、护甲、装备、其他分类，再按稳定位置组织“当前装备 / 当前角色背包候选”两列对照；“其他”只显示记忆水晶位置。
- 位置来源使用 Manifest bucket 与稳定映射，不按名称猜测；仓库物品不得混入当前角色背包。
- 每个位置显示名称、当前装备数、候选总数、对象事实、配装命中和写操作状态；背包候选较多时完整换行展示，不折叠或隐藏。
- 角色物品与配置合并当前使用的职业分支、机灵、飞船、载具、徽标等配置，以及角色携带、外观、可选配置和未分类只读数据；账号材料与货币独立为账号级页面，不随顶部角色切换改变作用域。任务与赏金在 Bungie 返回可见目标时显示真实目标进度、完成状态和进度条，不用条目总数冒充待处理数量。
- 材料清单使用专用连续行：`44px` 图标、名称与类型说明、右侧数量三列；不得复用活动记录的三列表格布局，也不得让材料元信息自动掉到下一行。
- 邮政官显示当前角色完整清单和真实总数，不得以最多 12 件的预览数量冒充总数；未接入取回能力时保持只读，不显示假操作。账号战绩明确聚合全部角色，不得在没有角色筛选能力时暗示只属于当前角色。
- 装备区标题在存在活动配装时显示配装名称和当前角色命中数。账号刷新必须保留已有装备卡片，仅首次没有位置数据时显示空态；当前装备图片优先读取，背包候选图片延迟读取，长槽位区允许浏览器跳过屏外绘制但不能改变数据完整性。
- 装备对照使用“位置 `112px` / 当前装备 `236px` / 背包候选占满剩余空间”的连续行布局；背包候选始终完整展开，宽屏五列自动换行，`1280px` 三列，`980px` 两列，`480px` 以下单列。
- 当前装备列使用稳定的选中背景和强调边界，背包候选列保持中性表面；两种来源不能只依赖重复文案区分。
- 武器与护甲对象卡统一使用稳定的三行结构和 `72px` 高度：名称、核心事实、对象状态各占一行。来源由桌面列标题或窄屏面板标题表达，槽位由左侧行标题表达，卡片内部不重复“当前角色装备 / 当前角色背包候选”和槽位名称。武器核心事实显示武器类型、稀有度与光等；护甲核心事实显示稀有度与光等，末行显示总值、锁定及“配装 / 打开中”等状态。状态不得新增第四行或通过整行拉伸改变同槽位卡片高度，完整状态说明通过 tooltip 和无障碍名称保留。
- 装备详情允许立即显示账号快照中的身份、当前属性和当前配置，但关键几何结构必须从首帧稳定：账号实例的属性表固定预留资料库标准值与差异列，比较区常态固定为“标准值 / 当前差异与修正”两行，待应用变化并入同一列，缺失数据以“读取中 / 未返回”占位，不得因定义或配置状态变化增减列或为隐藏占位增加额外行高。加载状态按定义、实例和辅助数据分区记录，`is_detail_loading` 只作为两条关键请求的总忙碌状态；版本静态值与多版本下拉必须共用同一字段外壳，实时来源和推荐等辅助数据只能在已存在区域内更新。
- 只有武器和护甲对象可打开统一详情；装备、材料、任务、货币和其他对象保持只读。
- 刷新、失败和空态保留当前 Tab、角色和位置骨架，不使用整页遮罩抹掉结构。
- 一键最高光等使用类型化结果反馈：只有 `submitting` 阶段锁定重复提交；Bungie 写接口逐件返回后，成功受理项立即提交局部状态并进入 `syncing`，明确失败项进入失败或部分提交。只有轻量账号组件确认目标实例已真实装备后才显示成功；对账不触发完整账号工作区 Loading，也不通过中文文案关键字推断状态颜色。
- 单件装备、转移、邮政官取回和锁定与一键最高光等共用账号写结果提交语义：Bungie 单件接口明确成功，或批量接口返回单项成功状态后，立即把对应 patch 提交到中央账号 Store、AccountSession 和磁盘快照，所有消费页面同步更新当前装备、位置、锁定与光等，并在确认前保留 `syncing` 标记；明确失败项保持原状态并显示原因。后台任务只按事件读取受影响角色或实例的轻量组件，不触发完整账号工作区 Loading；轻量 Profile 命中后把提交反馈升级为已确认并解除 committed patch 保护。Profile 暂时返回旧状态时继续低优先级退避对账，旧数据不得回退页面，新操作替代旧目标时不得重新应用旧 patch。

核心字段：`characters[]`、`slotComparisonRows`、Activity History、材料集合和邮政官只读物品。写操作中只锁定目标对象，不阻断其他位置。

## 资料库

稳定契约：`library.workspace`。

- 上方使用资料库状态摘要，主体使用“查询侧栏 + 连续结果列表”的工作区。
- 装备与“Perk 与框架”是互斥模式，使用完整 Tab 语义；搜索、筛选和清空是普通 Control。
- 查询侧栏首屏只承载模式、主搜索、基础筛选和默认折叠的高级筛选；最近查询、收藏计数和别名管理统一收进默认折叠的“搜索辅助”，不得与主搜索并列抢占首屏高度。
- 摘要显示激活版本、最新版本、激活时间、检查时间、语言、定义数量和缺失组件。
- 装备、Perk、框架、历史和收藏来自当前 Manifest 与本地历史，不使用固定名称、别名或数量。
- 同名装备存在多个可确认发布版本时，结果行名称旁直接显示玩家可识别的年份、赛季、资料片或内容包摘要；不要求展开详情，不显示 Hash 或内部 release trait。
- 装备结果首层按“名称与版本 / 类型、槽位、框架、弹药和伤害属性 / 当前获取、账号持有和来源可信度 / 操作”排列；完整定义描述、起源特性、历史来源和实时渠道进入同一来源详情，不在首层重复铺开。收藏是低权重操作，武器和护甲的“查看详情”仍是唯一主操作。
- 定义对象与账号实例必须区分。只有匹配到真实实例时才显示位置、Roll 和账号详情入口。
- Perk 反查装备使用可追踪的具体 Hash / Manifest 关系，不按装备名称模糊扩展。搜索列表以 Logical Perk / Perk Family 为用户实体：家族身份由 Inventory Plug 的规范化名称、官方图标和 Plug / Item 类别共同确定，不把描述或 Sandbox Perk Hash 直接当作列表身份。普通、强化和其他官方变体在同一结果内保留各自 Sandbox Perk Hash、Plug Hash、描述和关联数量；例如“爆炸箭头”只显示一行，行内分别展示普通效果与强化差异。
- Perk 搜索结果只返回精确关联总数和分类；展开“关联装备”后按需分页读取完整装备摘要，并在 workspace 按 Perk key 缓存，折叠不清除，新搜索或 Manifest 版本变化时清理。展开区覆盖加载、失败重试、空结果和加载更多状态，不设置静默固定上限。
- 关联装备继续使用连续列表，每个具体发布版本独立成行。左侧显示约 `40px` 官方装备图标并叠加官方版本水印；名称旁始终显示玩家可识别的发布版本，缺失时显示“版本待确认”；概要显示稀有度、装备类型、框架、弹药、伤害属性、专家标记，以及该版本支持“普通 / 可强化”的哪种 Perk 变体，随后显示历史来源或当前获取状态待确认，并可直接打开统一装备详情。同一发布版本的技术重复定义通过 canonical Hash 合并，不同发布版本保留且相邻排列；家族关联总数按所有变体的 canonical 发布版本实体取并集，不把各变体数量直接相加。
- Renderer 与 Desktop main / preload 的资料库合同必须显式握手。合同版本不支持 Perk 家族、分页关系或变体匹配时，仍可显示基础 Perk 搜索结果，但关联装备区必须提示重新构建并重启，不得把旧版固定上限摘要渲染成完整结果，也不得用空图标和“版本待确认”伪装合同正常。
- 未初始化、部分组件缺失、更新中、检查失败、历史为空和搜索无结果分别呈现；更新失败保留上一份可用资料库。
- 搜索、实时来源、收藏和别名使用独立错误状态，只影响所属区域。搜索更新期间可以保留上一份可见结果以稳定几何结构，但必须明确标记为上一次内容并在结果区使用 `aria-busy` 和独立状态播报。
- 结果操作支持键盘方向导航：同一行左右切换操作，上下移动到相邻结果的对应操作；统一装备详情关闭后焦点返回来源按钮。`760px` 及以下查询区回到页面文档流，不设置独立高度和纵向滚动；Manifest 摘要保持两列，极窄宽度再转单列。

结果区使用连续 `SurfaceList`，每行只画 `RowLine`，不把普通结果改成对象卡。

## 攻略

稳定契约：`guides.workspace`。

- 攻略页使用“筛选目录 / 结果列表 / 阅读或编辑”三栏连续工作区。左栏负责全部、使用中、已归档、收藏和分类筛选；中栏负责全文搜索、结果选择和新建入口；右栏只显示当前文档或当前草稿，不建立第二套弹窗编辑器。
- 攻略文档保存标题、分类、标签、收藏、归档状态、来源和正文。来源类型分为粘贴文本、个人笔记和来源链接；链接读取只能由用户在编辑区手动触发，先显示最终地址、正文分段、读取大小、警告和正文预览，用户选择“采用为正文”后才填入草稿，仍需再次保存攻略。
- 搜索覆盖标题、分类、标签、来源名称、来源 URL 和当前正文。状态、收藏和分类筛选可以组合；搜索无结果、库为空和未选择文档必须分别呈现。
- 新建和编辑必须要求非空标题与正文；来源类型为链接时还必须提供有效的 HTTP(S) 地址。保存写入本机；标题、分类、标签、收藏、归档或来源元数据变化只更新文档，只有规范化后的正文变化才创建新的不可变快照。
- 阅读区显示当前正文、来源、最近更新时间和正文快照数量。存在多个快照时显示按时间倒序的历史摘要和内容指纹短码；基础切片不提供旧快照恢复，也不把指纹当作玩家需要输入的标识。
- Desktop 链接读取只允许公开 HTTP(S) 地址，逐次校验重定向目标，阻断本机、局域网、带凭证地址、不支持内容类型、超过五次重定向、超过 15 秒和超过 2 MB 的响应；不执行页面脚本，不绕过登录、验证码或付费墙。Web 预览明确显示远程读取不可用。
- 保存正文时为每个不可变快照生成确定性章节分段。阅读区可以对当前快照运行本地确定性提取，候选覆盖职业、子职业、异域护甲、武器、护甲属性、模组、星相和碎片，并显示置信度与原文行引用。所有候选默认进入复选审阅；只有点击确认后才保存轻量确认记录，未确认结果不得成为装备目标、Armor 约束或配装。
- 提取确认绑定攻略文档 ID、正文快照 ID、正文指纹和解析器版本。正文变化或解析器版本不兼容时，旧确认只保留历史关系，不显示为当前正文已确认；确认文件损坏时不得覆盖为新的空记录。
- “派生成果与追溯”位于确认成果之后、正文之前，连续列出正式装备目标、Armor 约束、账号配装候选，以及从 Armor 约束或账号候选显式保存的本地方案。列表消费版本化 `GuideDerivedRelation`，只保存稳定 ID、关系、标签和时间，不复制攻略正文、账号快照或完整成果；有现有页面落点时提供明确跳转，没有落点的成果保持只读记录。
- 正式装备目标跳转必须打开仓库“推荐数据”并定位具体目标行；本地方案跳转必须选择具体方案。反向返回攻略时重新读取关系索引确认来源仍有效，不从摘要、guidance 或显示文案解析 ID；攻略删除后派生业务对象继续可用，但不得显示假定位成功。
- 收藏、归档、恢复和删除都调用真实持久化 action。归档文档从“使用中”筛选移出并可在“已归档”恢复；删除后目录和当前选择必须同步更新，不显示假成功提示。
- 加载时保留三栏结构和筛选控件；读取失败、保存失败或删除失败显示可见错误，不能用空库覆盖错误语义。编辑中和保存中保持草稿内容，保存失败后允许继续修改并重试。
- Desktop 使用本地 `guide-library.json`、轻量 `guide-extractions.json` 和版本化 `guide-derived-relations.json`，Web 只提供同一共享页面的会话内预览 adapter。便携备份包含攻略正文、提取确认和派生关系，但不包含 AI 密钥、OAuth token、远程网页缓存或未采用的链接预览。

宽屏三栏比例由攻略菜单 CSS 持有；窄屏按筛选、目录、正文顺序纵向排列。普通目录行使用连续列表表面，正文阅读和编辑区使用页面工作区表面，不把三栏分别包装成对象卡。

## 仓库

稳定契约：`vault.workspace`。

仓库固定包含筛选列表、推荐与目标、同名整理三个 `tabpanel`，共同消费同一份账号实例集合，并按“浏览 → 查看推荐证据 → 对比同名副本”的玩家顺序排列。清理不再维护独立工作台：玩家在筛选列表或同名整理中写入“待处理”，后续转移和游戏内处理仍以该明确状态为边界。

| 工作区 | 数据与操作边界 |
|---|---|
| 筛选列表 | 左侧常驻筛选、右侧十列紧凑装备网格；Profile 实例、Manifest 定义、玩家整理状态、锁定、排序和保护信号共同参与筛选 |
| 推荐与目标 | 内部分为“武器推荐”和“个人目标”：武器推荐先根据数据状态显示准备、核对、部分可用、失败或完整实例结果；个人目标完整承接现有装备目标与跨页定位，不与推荐来源挤在同一双栏 |
| 同名整理 | 按规范武器名称和实例 ID 分组；左侧按整理进度选择同名组，右侧使用比较基准和单一推荐来源突出实例差异并暂存本组整理状态；护甲继续使用现有分组与比较逻辑 |

推荐数据入口位于“推荐与目标 → 武器推荐”。推荐数据不可用时，整个结果区首先显示普通玩家可执行的数据准备状态：主要操作是选择正式 `武器推荐.csv`，DIM 社区推荐作为可选补充。数据已经可用时，页面只保留紧凑来源状态与“管理推荐数据”入口，管理内容使用带焦点限制和返回焦点的覆盖层，不在结果列表中向下展开造成页面跳动。覆盖层内的中文 CSV、DIM 在线更新、本地 DIM 文件、粘贴文本、标准模板和移除数据入口全部直接可见，仅通过主次按钮、说明与危险操作样式区分；不得通过默认折叠隐藏真实功能。

武器推荐知识库 CSV 是主要中文推荐数据：选择文件后只展示文件名、武器数、来源记录数、来源数和错误摘要，完整校验成功后仍需明确确认才替换 SQLite 知识库。DIM Wishlist 是第三方辅助数据，不是攻略来源，也不转换成 CSV；普通玩家主入口是“检查社区更新”，界面显示当前短 commit、启用时间、规则数和武器数，检查后展示固定 SHA 对应文件的统计预览，明确确认后才替换 SQLite；原生 `.txt` / `.wishlist` 文件导入和粘贴文本继续作为离线、自定义回退。两种格式的导入或替换都必须先解析并展示预览，再由用户明确确认；失败时保持当前活动数据不变。保存、替换或移除后自动重新计算当前账号匹配，不要求刷新整个账号或重启应用。

仓库只显示与当前账号装备直接相关的结果：命中装备数、命中组合数、PVE / PVP / 通用模式覆盖和匹配依据。没有 Wishlist 时显示中性空态和低权重导入操作；不得把 Hash、JSON、SQLite 路径或 CSV 逐列规则编辑暴露给普通用户。历史版本已保存的自定义规则继续由 Core / Services 兼容读取，仅在确实存在旧数据时显示“遗留自定义规则”，不提供新建、替换或粘贴入口。AI / light.gg 实时分析不参与仓库批量扫描，只在用户明确打开单件装备分析时调用。

推荐数据中的独立装备目标按稳定目标 ID 接受跨页定位请求，并使用既有选中表面 token 聚焦目标行。攻略来源目标提供“返回攻略”，但必须通过派生关系索引确认文档仍存在；Armor Planner 缺口提供“查看 Planner 引用”，只携带稳定 `result_id / candidate_id`，不承诺恢复完整求解结果。

筛选范围只提供武器、护甲和装备，不提供“全部”与“其他”入口。武器槽位只显示动能、能量和威能；护甲部位显示头盔、臂铠、胸甲、腿甲和职业物品。武器类型使用真实 `item_type`，稀有度使用 `tier`，装备阶级使用 `instance.gear_tier` 并包含 T0，伤害属性使用实例伤害类型；护甲职业使用 `class_type`。护甲套装选项目录来自当前语言 Manifest 的完整 `DestinyEquipableItemSetDefinition`，账号装备摘要中的真实 `equipableItemSetHash` 只用于匹配物品和计算持有数量；未持有的套装仍保留在目录中并允许选择，命中为空属于正常筛选结果。目录加载中或不可用时必须显示明确状态，不得回退成账号持有套装列表。缺少稳定账号字段的制作状态或框架不得用固定选项补造。

筛选列表保留现有左侧条件，武器与护甲按实际结果容器宽度布局，不再固定宽屏八列。标准武器卡片最小宽度约 `220px`，默认使用 `52px` 游戏图标；结果区约 `1320px` 以上六列、`1080px` 以上五列、`860px` 以上四列、`640px` 以上三列、`440px` 以上两列，更窄时单列，AI 抽屉打开后按剩余宽度自动降列。纯护甲视图继续复用全局 `44px` 游戏图标和现有卡片内容；混合视图按能够容纳武器摘要的宽度布局，但不改变护甲字段、筛选或整理逻辑。装备阶级直接使用 `DestinyInventoryItemConstantsDefinition.gearTierOverlayImagePaths` 的官方覆盖图，并与 DIM 一样叠在图标左上区域；不得用 CSS 自绘竖条或从左下角堆点。T0 不显示覆盖图，缺失阶级在产品层按 T0 参与筛选和显示。武器对象卡分成身份、事实、来源与状态四层：身份层显示图标、名称、类型和槽位；事实层显示游戏化弹药图标、官方彩色伤害属性图标与光等；来源层最多显示两条短摘要，例如 `Aegis：5/6 栏符合`、`DIM：命中 1 组`、`DIM：未命中 · 最接近 2/5 Perk` 或 `LGpig：仅推荐武器`，其他来源使用“另有 N 个来源”进入详情，不把 `x/y` 表达为评分；状态层显示一份玩家整理状态、锁定与配装引用。人工推荐源按武器身份共享来源要求，同名历史、复刻和高阶版本不分开判断；正式知识库中保留的明确栏位全部进入分母，同栏多个候选为任选其一，实例实际拥有即符合、没有即不符，当前 Hash 的定义池不能删除要求。DIM 保持一行一个完整组合、多行任选其一，不跨组合累计 Perk，也不在卡片显示总组合分母。只有账号实例 Roll 本身未完整读取时才显示“Roll 数据异常”，不显示残缺的 `x/y`；全应用不使用拇指图标表达推荐结果。来源显示名固定使用 `Aegis推荐 / LGpig推荐 / YXCRALLXY推荐表 / Sayalarry推荐表 / DIM社区愿望单`。标准武器卡片保持统一高度；可选紧凑模式只缩减列表摘要，不删除详情中的证据。护甲沿用现有身份、事实和状态结构，不接入 T20 来源摘要。锁定使用绿色锁形状态标记，固定在卡片底部状态行右侧并使用主题状态底板，不再覆盖装备图标；配装引用使用应用状态文字和既有选中边框，不使用右上角单字方块。完整 Perk、框架、品质、护甲能量、逐栏推荐证据和长备注留在详情或同名整理，不进入普通网格。仓库页必须只生成一次实例来源摘要索引，卡片、推荐筛选、推荐结果和同名整理共同查表；不得在每张卡片渲染或每次筛选时重新遍历 Wishlist 和来源记录。

玩家整理状态统一为未标记、保留、待复查和待处理。`farm / loadout` 继续作为既有用途标签保存，但不进入整理状态控件。锁定、配装引用、愿望单、个人目标和社区推荐属于系统证据，只显示或筛选事实，不自动生成保留、待复查或待处理结论。普通十列装备单元只显示一份玩家整理状态，并保留锁定与配装引用角标；愿望单、个人目标和社区推荐继续参与筛选并在详情或同名整理中展开，不再同时显示推导决定。

“武器推荐”的结果区按武器实例逐行展示，不按 `itemHash` 合并同名副本，默认显示全部账号武器，并允许按有来源记录、有符合项、来源 `0/y`、来源冲突、Roll 数据异常、无来源记录和有人工标记查看。上述入口是可以重叠的查看条件，不是互斥评级；例如一个来源有符合项、另一个来源为 `0/y` 时，同一实例同时属于“有符合项”和“来源冲突”。只有完整核对状态才允许列出“无来源记录”；部分或失败状态继续保留上次结果。每行显示实例身份、用途、人工标记和最多两条来源摘要，并提供“查看详细证据”和“整理同名武器”两个明确去向；只有确实存在同名副本时才显示整理入口。人工推荐源正常情况下只表达 `x/y 栏符合` 或仅推荐这把武器；正式知识库中保留的明确栏位固定进入分母，当前 Hash 无法补出候选 Hash 时继续按官方名称对照，不能静默删除要求。只有实例 Roll 读取异常时才用独立状态替代整条 `x/y`。DIM 完整命中时只表达“命中 n 组”，未命中时只表达同一条最接近组合的 `x/y Perk`，不把组合数伪装成栏位数，也不把不同组合中的 Perk 相加。页面固定解释 `x/y` 是有效来源要求与本实例实际拥有栏位的对照，不是系统评分。界面不生成“完整命中、核心已齐、值得保留”等结论。无来源记录与来源已有记录但 `0/y` 必须区分。

账号武器推荐核对必须显示独立状态：未开始、核对中、部分可用、已完成或失败。只有已完成状态才能把空结果解释为当前账号没有来源记录；核对中、部分可用和失败不得把未显示的武器解释为未收录。账号刷新后旧结果标记为待重新核对，重新核对失败继续显示上次结果并说明其状态，不得清空已有结果，也不得把推荐核对错误混入最近活动错误。资料库未准备、资料库版本落后和推荐知识库不可用使用稳定结构化原因，不从错误字符串推断。推荐扫描只处理武器实例，护甲不进入 T20 扫描；“复制验收报告”只读取当前账号和扫描结果，不写标签或游戏数据。

护甲多条件为 AND，同一属性只保留一条；总属性只作为排序维度。保护与匹配支持多选并按 AND 收窄结果。重置筛选与结果摘要位于结果区顶部，不单独占用一行。

同名整理默认使用已标记保留的实例作为比较基准，否则使用本组第一件；基准只用于突出相同与差异，不属于系统推荐，不自动修改状态，也不限制一组只能保留一件。基准使用单选语义，切换后武器按真实 Socket 顺序和玩家可识别的插槽名称比较，护甲展示总值与完整六维；差异必须同时提供文字标记，不能只依赖颜色。武器首屏默认比较“当前启用”，完整 Roll 作为用户主动开启的只读增强视图；当前同名组存在实例级来源记录时，在同一标题区增加“比较内容”选择，默认仍为实例 Roll，并仅列出该组真实存在的来源。选择一个来源后，矩阵按枪管/瞄具、第二列、大师、Perk 1、Perk 2、起源特性的固定顺序，仅展开该来源有效且明确要求的栏位；每格同时显示来源要求、本件实际拥有、当前启用和符合/不符文字，实例 Roll 未读取时显示“数据未读取”，其他来源继续在实例证据区显示独立摘要。只推荐武器但未指定栏位的来源显示中性说明，不伪造比较列；护甲不显示来源选择。进入同名组后可以后台按需读取组内实例详情，但详情完成不得自动切换展示模式。完整 Roll 每列集合由当前启用项与 `reusable_plugs` 中来源包含 `instance` 的实际拥有项组成，排除只来自 `character / profile / manifest` 的解锁项和定义池候选；当前启用项只作为集合内状态，不参与完整 Roll 的相同判定。详情未齐时只能明确显示并比较“当前启用”，不得把账号紧凑快照中的 `socket_plugs` 冒充完整 Roll 或提前给出完整集合结论；读取失败必须提供重试，完整 Roll 控件在数据未就绪时保持可见但不可用。辅助“当前启用”模式只比较每列当前选择。集合关系显示为相同、部分重合或完全不同，同名整理只读比较，切换游戏内 Perk 仍在装备详情完成。比较矩阵采用“单元格差异状态 + 无框 Perk 行”两层结构：差异通过左侧状态条和短文字标签表达，不使用整格描边；Perk 行只使用分隔线，当前或可切换状态固定在右侧，仅此实例拥有的 Perk 在名称旁显示短标签。Roll 与来源要求列都应保证可读宽度并允许矩阵横向滚动，不得为了塞入视口压缩名称和状态。Roll 单元中的 Perk 图标必须复用资料库和武器详情的 `game-definition-icon` 原语，以主题适配底板、边框和 `contain` 保留透明官方图标的可读性，不得按普通装备缩略图使用 `cover` 裁切。

实例保护与匹配依据分开展示。锁定和精确实例 ID 配装引用属于实例保护；仅按 Hash 或部位匹配的配装需求明确标为“配装同款”，Wishlist、个人目标和历史社区数据保留在匹配依据中，不得让同 Hash 的每个实例都显示为精确配装引用。

同名组目录使用 roving focus：纵向布局可用上下方向键，横向布局可用左右方向键，`Home / End` 跳到首尾；选中项必须滚动到可见区域。单件整理状态先在当前组暂存，它是尚未写入本地标签的“待应用状态”，不是产品级草稿。左侧组目录和右侧行必须明确显示“待应用”；只有待应用状态与已应用状态不同时才开放“应用本组状态”和“撤销待应用”。切换仓库内部 `tabpanel` 时保留待应用状态；通过 Shell、返回攻略、查看 Planner 引用或窗口关闭离开仓库时必须提示待应用件数并允许继续整理或放弃离开，刷新与原生关闭使用平台卸载提示。存在待应用状态时不能直接进入下一未整理组。“填充：基准保留，其余待复查”只修改待应用状态。锁定、任一应用配装或游戏内配装精确实例引用、玩家手动保留、同名组独特 Roll、来源冲突、Roll/官方名称不完整及推荐未覆盖属于强制保护，不能被批量或同名整理新设为待处理。批量待处理必须用共享结构化确认弹窗预览所选数量和受保护数量，确认后只写未受保护实例的本地标签。推荐证据冲突继续复述冲突数量和真实写入边界。所选装备移动和待处理装备转移同样由共享仓库 UI 完成结构化确认，Desktop adapter 只执行已经确认的写入。应用整理状态只写本地标签，不转移、不解锁、不拆解。

写操作继续调用真实 action / IPC，失败前不得提前修改锁定、位置或标签显示。

## 商人

稳定契约：`vendors.workspace`。

- 商人工作区使用“地点目录 / 商人详情”两栏结构，不保留重复的右侧上下文栏。来源、读取时间、重置边界、可见条目和当前角色库存上下文合并到商人头部与状态条；普通产品界面不显示角色长 ID，也不使用全局核验总量占据当前商人的决策空间。
- 商人状态条提供“账号全部”和各真实角色的作用域切换；切换通过现有 `character_ids` 请求重新读取并按作用域过滤 Offer，账号级章节显示“账号”，角色级库存显示对应角色。作用域切换不得伪造数据、静默保留旧角色库存或把角色长 ID作为可见标签；角色按钮使用职业名（同职业追加序号），即使某个角色详情失败也必须保留可切换入口，并支持左右方向键与 `Home / End` 定位。
- 目录按真实地点和稳定 `vendorHash` / ID 分组，使用紧凑文字行，不显示头像或首字母占位。目录提供商人、物品和分类搜索，命中当前商人物品时详情同步收窄到匹配条目，命中商人名称时保留完整库存；搜索保留真实地点归属。正常商人行只显示条目数量，读取失败和部分可用才追加异常状态。窄屏目录转为单行横向滚动，方向键随布局切换为左右导航。
- 详情先显示声望与等级，其余章节保持商人结构定义的业务顺序；未配置的基础服务和任务位于已配置的主库存、周常奖励、聚焦等核心章节之后。不用分类摘要替代完整库存。声望面板必须明确显示等级上限、距离下一等级和“可领取下一等级奖励 / 已达等级上限”状态，不得把无法确认的奖励节点伪装成可领取按钮。商人状态条下方提供单行吸顶章节导航，滚动后固定在共享页面标题栏正下方，只定位现有章节，不切换或隐藏库存；当前章节必须有明确选中态，窄屏允许横向滚动。
- 每个商人章节标题必须显示真实作用域和业务动作（如“当前角色 · 游戏内购买”“账号 · 领取”“公会 · 获取”）；作用域来自 ViewModel 的 `scope`，动作来自 `action`，不得根据章节名称猜测或把账号级奖励写成当前角色库存。
- 商人状态条提供局部“只看可负担”和“只看推荐”筛选，使用 `aria-pressed` 表达当前状态。筛选只改变当前详情的呈现，不修改快照、目录数量或真实库存；结果为空时保留清除筛选入口，章节导航只包含筛选后仍有内容的章节。
- Offer 网格中的可打开装备卡支持左右切换、按当前列数上下跳转以及 `Home / End` 首尾定位，焦点移动时滚动到可见区域；只读材料、任务和服务行不进入装备卡导航。
- 没有应用内操作能力的非装备直接库存和任务继续保留真实 Bungie 事实，但使用紧凑信息行并明确标记“游戏内使用 / 领取”或条件状态，不得显示成可在应用内点击购买的装备卡。
- 每个 Offer 显示真实图标或缺图态、名称、类型、摘要、成本、数量、购买状态和失败原因。成本在余额可用时同时显示“持有 / 需要”；购买能力必须写成“游戏内可购买”或对应失败原因，装备卡的应用内动作明确为“查看详情”，不得暗示应用可以直接购买。
- Offer 存在真实推荐判断时，在身份信息下方显示短推荐标签；推荐标签是决策证据，不替代购买条件、成本或详情入口，也不得仅用颜色表达。
- 角色相关库存显示当前角色上下文，不跨角色合并库存。
- 武器和护甲 Offer 可打开统一详情；材料、任务、解码器和其他对象只显示库存事实。
- 读取时间和重置边界使用原始时间字段在渲染时格式化，不保存预格式化文案。
- 加载、未开放和失败时清除过期轮换；部分可用时展示已确认销售数据并标记缺失属性或插槽。

Offer 使用响应式对象卡网格：宽屏五列，`1280px` 三列，`980px` 两列，`760px` 单列。章节和目录保持连续直角结构，不建立水平商品轨道。

## 配装边界

稳定契约：`loadouts.workspace`。本文件记录共享配装页必须保持的页面与交互边界；游戏内配装与应用配装是两种独立业务对象，应用配装的方案库、对比、决策工作区、自动配甲和安全穿戴共享同一产品实现。

当前领域能力已落地应用配装页面栈、方案库只读详情、2–3 方案差异矩阵、固定三武器/五护甲槽位、逐槽位实例选择、自动配甲、穿戴核对与保存到游戏内槽位后续动作。应用配装编辑器使用“完整构筑画布 / 实时摘要”两部分工作区；自动配甲、候选比较和更多规划方式在护甲区域原位展开，不替换完整构筑上下文。Desktop 与 Web 通过同一共享页面消费应用层 ViewModel。账号 freshness、未保存离开保护、焦点恢复和 `1280px / 980px / 760px` 响应式边界属于该稳定合同。

- 全局侧栏只保留一个 AI 工作台，不显示固定“任务”Tab 或攻略任务树。攻略链接、正文和 AI 整理文本统一从攻略页进入；配装页只接收攻略页已经确认并完成账号匹配的成果。旧侧栏保存的攻略文本只做一次性恢复，成功生成草稿后清除。
- AI 工作台的 `guide_capture` 交接会打开攻略页并预填一份未保存的正文草稿，继续经过保存、要求识别、人工确认和账号匹配；不得直接打开配装页或创建方案。`armor_solution_comparison` 与 `equipment_target_candidates` 仍进入配装页对应的专用审阅。
- AI 工作台的 `equipment_target_candidates` 交接使用专用候选审阅，不经过攻略文本解析。候选列表必须区分账号真实实例和仅 Manifest 定义，允许逐项复选并显示目标角色；确认后只生成 `assistant-targets` 未保存草稿。真实实例绑定 `selected_instance_id`，仅定义候选只写 `item_hash` 并保持待获取、待选择，不能伪造实例或自动保存。
- 一级视图只保留 `游戏内配装` 和 `应用配装`。两者不是同一方案的来源筛选：游戏内配装是 Bungie 官方角色槽位，应用配装是本应用保存的完整构筑方案。自动配甲不是第三个一级页面、方案类型或来源。
- 游戏内配装显示 Bungie 官方槽位与官方应用能力；核心操作使用 `应用游戏内配装`、`复制到应用配装`、`用当前装备覆盖槽位` 和 `清空槽位`。复制只携带官方实际返回且可确认的字段，打开未保存的应用配装编辑器，不立即创建已保存方案。
- 应用配装默认进入已保存方案库，首屏提供搜索/筛选、只读详情、穿戴状态、`对比方案`、`新建配装`、`穿戴此方案` 和 `编辑方案`。目录只显示已经显式保存的应用配装；草稿不进入目录。
- 应用配装对比允许选择 2–3 个方案并默认突出差异。游戏内配装可以作为只读参照快照参与共同字段比较，但未知字段必须显示 `未返回`，不得因此转化为应用配装或与应用方案共用身份。
- 新建和编辑推入唯一的应用配装决策工作区。宽屏主区持续展示子职业紧凑摘要、三件武器和五件护甲，右侧持续显示最终六维、目标差值、缺口、浪费、套装、能量、缺失和可执行状态。六维、碎片、套装、实例限制和逐部位模组规则都在护甲区域展开；候选不会替换完整构筑画布。顶部保存状态、`保存` 和 `穿戴` 持续可见；点击槽位在当前画布上打开真实实例选择抽屉，关闭或选择后恢复来源焦点和编辑状态。
- 进入编辑、自动配甲、穿戴核对或发布等聚焦流程后，收起配装菜单外层的角色/视图/新建工具栏和常驻就绪状态，只保留当前任务需要的返回、方案身份、保存状态、保存与穿戴操作；错误和进行中状态仍在邻近区域显示。保存不得在顶部和底部重复出现。
- 装备槽位使用鼠标优先交互：空槽和已选装备的主体区域均为完整点击目标，悬停时明确显示选择方向；清空等破坏性较低的次操作保持独立按钮。点击槽位在当前构筑画布上打开右侧实例选择抽屉，抽屉提供槽位内搜索、候选数量、整行选择、遮罩关闭和 `Escape` 关闭；候选必须基于真实账号位置区分已装备、角色库存和仓库，当前已选实例置顶并显示“当前”，同时展示位置、光等、武器真实 Perk 或护甲最高属性等选择信息，搜索可以匹配 Perk 名称。选择后写入草稿、关闭抽屉并恢复原槽位焦点，不推入一张替代构筑画布的整页列表。
- 编辑头部的目标对象优先选择当前账号真实角色，并同步职业与目标角色 ID；账号不可用时才保留职业文本输入。方案名称、保存状态、保存和穿戴收敛在同一个吸顶编辑栏中。
- 新建空草稿不得只展示大面积空装备卡。中央先提供“从当前装备开始”“按属性目标自动配甲”“手动选择装备”三个真实起点；三武器使用紧凑横排，五护甲作为中央主要决策内容。子职业优先表达已确认的分支、星相、碎片和技能语义，Hash 只作为未解析诊断信息；没有真实编辑 action 时必须明确标记“当前仅记录”，保持静态对象样式，不伪装成可点击入口。
- 新建、当前装备复制、攻略成果交接和游戏内槽位复制都进入同一个未保存应用配装编辑器；DIM 先完成链接读取与预览，再进入该编辑器。创建入口不得维护平行编辑 DOM 或提供独立的攻略文本粘贴入口。
- 武器和护甲必须选择真实账号实例；同名多实例不静默代选。子职业、目标 Perk、星象、碎片、技能和模组只有在稳定 Hash 与可写能力可验证时才进入执行计划，其余保持“仅记录”。
- 护甲规划位于应用配装编辑器的护甲区域，玩家入口文案使用 `按属性目标自动配甲`。它是护甲构筑主命令和编辑器内部状态，不新增第三个一级页面。展开后只保留“配装设置 / 推荐方案”两部分，方案摘要进入推荐方案顶部，不再维持第三个固定侧栏；接受方案后原位填入五个护甲槽位并收起规划区，不立即保存或穿戴。默认直接使用“当前库存”；“理论上限 / 待刷目标 / 升级路径”与套装、固定/排除、碎片变化和位置范围一起放入折叠的更多条件，不要求普通玩家先选模式。
- 属性模组输入以头盔、臂铠、胸甲、腿甲和职业物品五条独立规则为真相，每条使用 `自动 / 不安装 / +5 / +10`；固定 `+5 / +10` 后允许继续锁定具体属性。全局 `+5 × N / +10 × N` 只从五条规则和候选结果派生，用于摘要、比较和旧方案兼容，不得继续作为新方案的唯一输入。
- 属性模组输入分别使用 `+5 属性模组` 与 `+10 属性模组` 的精确数量，总数不超过五件护甲；T5 护甲调整由计算器逐件自动选择，零能量且不计入属性模组数量。库存与升级候选必须按五个具体部位的 Socket、草稿耗能功能 Plug、候选未覆盖 Socket 的现有耗能 Plug 和剩余能量分配属性模组；未安装属性模组的部位必须规划真实零能量清空 Plug。页面逐件显示护甲调整、属性模组、其他模组占用和最终能量，不得只展示五件聚合预算。
- 护甲约束支持 Manifest 可确认的 `2件 / 4件 / 2+2`、位置范围、固定异域、锁定和排除。理论模式不要求账号且只输出理论身份；待刷模式区分已有、待升级、待核对和待获取；库存与升级模式才允许选择真实实例组合。
- 护甲计算统一消费 `@d2-tools/app/armor` 的 `ArmorPlannerWorkspaceState`，页面不得继续直接调用旧 `solveLoadoutArmorCandidates`。Desktop Adapter 在主进程补齐当前 Manifest 规则集、套装目录和账号护甲快照；页面只提交职业、目标、约束、位置和实例选择条件。
- 修改六维、碎片、模组预算、优先级、锁定、排除或位置范围后，必须立即废弃旧候选。计算中可以保留上一结果作为内部恢复数据，但不得继续显示为可选择候选；`stale / error / indeterminate / unreachable / invalid` 必须分别显示真实状态和领域警告。
- 四种推荐方案共用逐属性比较、必须满足的要求、距离目标、模组用量、超出目标的属性和套装效果；首层直接显示六维、目标差值、距离目标、超出目标、套装以及转移/替换/缺失数量，展开后逐件显示部位、护甲、调整、属性模组、其他模组、能量和状态。理论方案显示框架、第三属性和调整身份，待刷方案显示缺失与已有匹配，库存方案显示位置与转移数，升级方案显示保留件、替换数和步骤。允许选择 2–3 个方案进行比较；比较不修改草稿，选择库存或升级方案只填入应用配装的五个护甲槽位并返回编辑器，不立即保存，也不触发 Bungie 写操作。

- 护甲设置中的五个部位直接提供 `自动 / 不装 / +5 / +10`；固定数值后才出现属性选择。属性优先使用可见的有序列表，不使用无序复选框保存隐藏点击顺序。数据尚未读取时只显示一条中性说明，确认安装冲突后才显示对应部位和玩家可行动原因。
- 普通玩家路径统一使用 `模组插槽 / 模组 / 当前游戏数据 / 具体装备 / 配装要求 / 必须满足 / 推荐方案`。英文 `Socket / Plug / Manifest` 仅保留在领域合同、诊断复制内容或显式技术详情中，不进入标题、表单标签、常驻状态或普通错误恢复文案。
- `保存应用配装` 只持久化应用方案；保存后才能稳定生成穿戴计划。`穿戴此方案` 使用 `plan / execute / refresh` 边界，包含真实转移、来源角色释放、当前装备替换和可验证 Plug 写入，失败时停止后续步骤并展示真实结果。
- 攻略来源方案的摘要区显示结构化来源并提供“返回原攻略”；入口通过 `source.source_id` 查询派生关系，不解析 guidance 文案。关系失效时方案仍可继续使用并显示提示。来自装备目标的 Armor 引用只展示缩短后的结果与候选标识，并明确要求按当前账号和规则重新计算，不伪恢复未持久化候选。
- DIM 导出位于方案应用区，只对当前账号已核对的完整真实实例方案开放。导出使用当前 DIM `equipped / unequipped` 内联格式并保留可定位的 `socketOverrides`；理论、待刷、实例缺失、Hash 不一致或 Plug 无法映射时显示结构化阻断原因，不生成伪链接。
- 只有应用配装穿戴步骤已由轻量 Profile 确认在游戏内生效后，才开放 `保存到游戏内槽位`；覆盖已有 Bungie 槽位必须明确确认，应用配装不会因发布而自动删除或同步。发布必须生成独立不可变计划，绑定来源执行计划、账号、角色、已确认实例和目标槽位内容指纹；确认前再次核对当前已确认装备事实，装备状态或槽位变化时保持零写入。写入后按目标范围重新读取并核对槽位实例，使用发布计划、确认、执行和步骤 ID 写入验证记录。草稿变化必须立即废弃旧执行与发布报告。

状态矩阵必须覆盖：首次读取、空目录、链接读取失败、DIM 部分解析、候选计算中、无满足约束候选、方案未保存、方案有未保存修改、穿戴计划不可执行、穿戴部分失败、穿戴成功待发布、槽位覆盖确认和发布失败。已确认字段继续显示，缺失字段不得按名称或相似配置补造。

### 配装绑定表

| 页面区域 | 运行时字段或 action | 数据和状态约束 |
|---|---|---|
| 应用配装库 | `listLocalLoadoutPlans`、`LocalLoadoutPlan[]`、`selectedPlanId` | 只列出已显式保存的应用配装；草稿不进入目录。方案详情与编辑器是两个页面状态。 |
| 方案对比 | 已选择方案 ID、游戏内参照快照、差异 ViewModel、`showDiffOnly` | 支持 2–3 个方案；游戏内参照不改变身份，未返回字段不补造。退出后恢复原选择和焦点。 |
| 基础信息 | `name`、`class_name`、`target_character_id`、`source`、`notes` | 新建和当前装备复制只创建草稿；`saveLocalLoadoutPlan` 成功前不写入本地文件。 |
| 武器与实例 | `item_targets`、`selected_instance_id`、`item_hash`、`plug_hashes`、`matchLocalLoadoutPlan` | 按实例 ID 核对；同一目标有多个真实实例时保持“需要选择实例”，不自动代选。 |
| 子职业与构筑 | `subclass_target`、`guidance` | 仅保存稳定 Hash 和已解析条件；不能证明可写的配置在产品中保持“仅记录”。 |
| 护甲规划 | `armor_constraints`、`ArmorPlannerWorkspaceState`、`armor_plan` | 模式、套装、锁定、排除、异域、位置范围、六维和五部位属性模组规则随草稿保存；全局数量只是派生摘要，临时候选不持久化。选择库存或升级候选后保存稳定结果 ID、可选缓存键、候选 ID、规则/来源 revision、真实实例 ID，以及逐件 slot、Socket、属性、数值、Plug、模组能量和最终能量账本；重新打开时从这些稳定字段恢复摘要，执行前按刷新后的实例、Socket 和能量重新预检。 |
| 应用配装详情与摘要 | `LocalLoadoutPlanMatch`、账号当前快照 | 匹配结果是即时派生值，不写入 `loadout-plans.json`；未读账号只显示等待核对。`穿戴此方案` 持续可见但按真实缺口禁用或降级。 |
| DIM / 攻略 / AI 成果入口 | `previewDimLoadoutImport`、`createDimLoadoutExport`、`loadout_candidates`、`armor_constraint_draft`、`equipment_target_candidates` | DIM 导入必须先显示预览；攻略正文只在攻略页导入和确认。配装页只审阅类型化成果，并区分真实实例、定义候选和缺口；所有导入与成果交接都不直接写入方案。 |
| 穿戴与发布 | `createLocalLoadoutPlanExecutionPlan`、`createLocalLoadoutPlanPublishPlan`、实例级 write action、刷新核对与 Bungie 槽位 action | 保存应用配装、穿戴和保存到游戏内槽位是三个独立动作；步骤失败停止后续执行，只有真实刷新核对成功后才允许发布。发布前复核账号、已装备实例和槽位指纹，发布后核对目标槽位实例并记录验证。 |

- 攻略中的护甲属性是五件护甲聚合目标，必须预填到 `armor_constraints.stat_minimums`，不得按单件护甲属性筛选账号实例。
- 当前 Armor 3.0 六维可直接预填；旧六维名称必须显示映射说明并进入待确认，用户复核前不得伪装成已确认事实。
- 只有唯一且带实例 ID 的异域命中可以预填固定异域；同名多实例必须由用户选择。攻略没有明确给出逐部位属性模组安排时，五个部位保持 `自动` 并显示复核提示，不从聚合数量猜测具体位置。
- 接受库存或升级候选后必须保存稳定 `result_id`、候选 ID、规则版本、来源 revision 和真实实例 ID；`cache_key` 只用于短期复用。方案摘要显示缩短后的结果引用，缓存过期只提示重新计算，不得让已保存真实实例伪装成失效或自动替换。
- 应用配装穿戴计划必须显示计划引用和完整步骤。用户确认后、第一步写操作前强制刷新账号并重建计划；账号、目标角色、实例、位置、来源角色替换、可写 Plug 或缺口变化时显示“计划已失效”，不得执行部分旧步骤。
- 执行前复核失败与执行后刷新验证失败是两个独立状态；前者必须是零写入，后者显示逐项完成数、失败步骤和账号刷新核对结果。
- 用户确认后生成独立确认 ID 和执行 ID；应用配装的每个转移、装备与 Plug 步骤必须把计划、确认、执行和步骤 ID 传入操作日志。穿戴确认页显示缩短后的执行引用，设置页日志保留完整 ID 的悬停信息和复制诊断。
- 执行后必须写入同一执行 ID 下的验证记录，状态为 `verified / partial / mismatch / unavailable`。设置页提供“执行验证”类型筛选；部分完成和验证不可用使用警告状态，不得统一显示成普通成功或失败。

### 配装状态矩阵

| 状态 | 可见区域 | 允许操作 | 不允许伪造 |
|---|---|---|---|
| 首次读取 / 空目录 | 目录和创建入口 | 新建、当前装备复制 | 不展示旧模板或虚构方案。 |
| 未保存草稿 | 槽位式应用配装编辑器和实时摘要 | 编辑、选择实例、显式保存 | 不显示为目录中的已保存方案。 |
| 已保存但账号未读 | 方案详情和来源 | 编辑、删除 | 不声明装备已拥有或可执行。 |
| 多实例待选择 | 对应装备行和摘要 | 选择具体实例 | 不按名称、Hash 或最近位置静默选定。 |
| 目标 Plug 不可用 / 装备缺失 | 对应装备行 | 编辑目标、查看现有候选 | 不将不可验证 Plug 加入执行计划。 |
| DIM 链接读取失败 / 攻略解析失败 | 创建入口和操作状态 | 修正链接或文本并重试 | 不创建半成品方案，不显示模拟预览或成功状态。 |
| DIM 导出阻断 | 方案应用区 | 选择真实实例、修正 Hash 或 Plug 后重试 | 不为理论、待刷、缺失实例或无法定位插槽的方案生成伪完整链接。 |
| 护甲计算中 / 过期 / 无候选 | 护甲章节和摘要 | 调整约束、重新计算 | 不显示旧模式候选，不把理论身份或待刷要求伪装为真实实例，不把无候选表示为已优化。 |
| 穿戴计划不可执行 / 部分失败 | 执行核对和操作状态 | 查看已完成、失败和未执行步骤 | 不回滚成功步骤或报告虚构事务成功。 |
| 穿戴成功待发布 / 槽位覆盖 | 发布区域 | 选择真实 Bungie 槽位并明确确认；确认后再次刷新并执行失效校验 | 不自动覆盖槽位，不使用已变化的装备状态或槽位内容，不同步或删除应用配装。 |

## 通用状态要求

所有菜单必须保留正常、首次加载、刷新中、空、失败、部分失败、无权限、禁用和进行中状态。状态只影响所属区域；已确认事实继续显示，缺失字段不补造，写操作成功前不更新事实值。
