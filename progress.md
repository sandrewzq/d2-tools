Original prompt: [d2-armor-solver](file:///Users/longhuadmin/Desktop/sandrew/d2-tools/d2-armor-solver) 结合这个项目，整体分析下配装页 $develop-web-game

- 已阅读仓库规则、docs/todo.md、docs/development.md 与 develop-web-game 技能。
- 当前目标是只读分析 d2-tools 配装页与 d2-armor-solver 的能力映射，不直接改业务代码。
- 待完成：检查 Web 预览可运行性/截图与交互状态；整理结构、数据流、能力差异、风险和建议。
- 已完成 Web fixture 只读检查：`1280px` 与 `760px` 配装页截图；确认页面无 `render_game_to_text`，改用 DOM 文本、截图和控制台错误观察。Web fixture 的账号/槽位/Armor provider 是 mock，不能作为 Desktop 真实能力验收。
- 已完成 `d2-armor-solver` 对比：其优势是目标规则、理论/库存/升级求解、候选排序和结果账本；d2-tools 优势是版本化 Armor Planner、真实账号实例、方案草稿/保存、DIM、Bungie 执行前后验证及攻略/AI 追溯。

## 当前会话：仓库页复核

Current request: `$develop-web-game` 你再分析下仓库页目前是否还有问题

- 目标：只读检查共享 Web 仓库页的视觉、响应式、键盘焦点、弹层和状态流，不直接修改产品代码。
- 方法：使用仓库 Web 预览与 develop-web-game 的 Playwright 客户端，在 1280 / 980 / 760 宽度执行输入、截图和控制台检查；结合 game-ui-ux 合同判断问题优先级。
- 说明：当前产品是 React 工作台，不是 canvas 游戏，因此不新增 `render_game_to_text` 或 `advanceTime` 游戏接口。
- 1280×720 基线：仓库筛选页可打开，但控制台出现 `ERR_CONNECTION_REFUSED` 与 404；Web fixture 仅有 3 件互不重复装备，无法覆盖同名整理的待应用、风险确认和离开保护主链路。
- 初步视觉观察：筛选栏纵向信息密度较高，右侧结果仅显示 3 张很窄的紧凑卡片并留下大面积空白，需要继续检查 980 / 760 响应式与滚动行为。
- 已完成 1280×900、980×900、760×900 三档复核，并实际检查筛选列表、同名整理、目标与匹配、装备详情、AI 抽屉、仓库 Tab 键盘导航、Wishlist 管理器和目标编辑器。
- 已确认正常：仓库 Tab 支持左右键、Home、End；装备详情打开后焦点进入关闭按钮，Escape 关闭并返回原装备卡；980 / 760 的 AI 抽屉具备 dialog、inert、初始焦点与 Escape 焦点恢复；页面没有 body 级横向溢出。
- P1（本轮已处理）：Desktop 传入的 `cleanupActions` 现在由 `VaultPageContentView` 接入轻量候选选择栏，`VaultOrganizePanel` 和 `useVaultBatchActions` 已挂载。主仓库页恢复批量选择、批量标签和批量转移入口；批量解锁不再作为仓库主流程暴露。
- P1/P2：装备网格被固定为十列，实测卡片宽度约为 1280 下 76px、980 下 58px、760 下 108px；1280 / 980 的装备名称、类型、槽位和属性严重逐字裁切。当前“十列”合同与 44px 图标及三层信息结构在中等桌面宽度下物理冲突。
- P1/P2：980 下筛选栏 `clientWidth=260`、`scrollWidth=340`，共同条件的两个字段被右侧裁掉约 80px；横向滚动条位于长面板底部，难以发现。
- P2：装备目标编辑器在三档宽度均有字段挤压，“属性”“最低值”标签出现逐字竖排，属性选择、数值和删除按钮在窄栏中仍强制三列。
- P2：Web fixture 只有 3 件不同 Hash 的装备，同名整理始终为 0 组 / 0 件，无法验证待应用、撤销、风险确认、离开保护、弹层叠加和批量结构化确认；fixture 也缺少真实装备图标与 tier overlay，因此不能代替 Desktop 真实数据验收。
- P2：控制台持续出现 `127.0.0.1:7777/event` 连接拒绝及 `/api/home-snapshot` 404。fixture fallback 最终可工作，但预览环境不是干净状态。
- P3：1280 的 AI 为停靠态，Escape 不关闭；980 / 760 的 overlay 抽屉可关闭。可能属于设计选择，但键盘关闭语义需要在合同中明确。
- 样式风险：`packages/ui/src/styles/menus/vault/02-workspace.css` 已超过 3400 行，包含多轮重复响应式和后置覆盖规则；980 布局与列数异常很可能由规则叠加造成。下一轮应收敛网格断点、筛选栏和目标编辑器响应式，最后补强 Web fixture 覆盖。
- 本轮已将用户可见的“可清理”统一改为“待处理”，并补充“本地状态只保存在应用内、转移不会自动拆解”的边界文案；同时在筛选列表接回候选选择和真实 Bungie 转移确认入口。网格、筛选栏、目标编辑器和 Web fixture 覆盖问题仍待后续处理。
- 本轮继续收敛仓库响应式：装备网格调整为宽屏 8 列、1020 以下 6 列、850 以下 4 列、560 以下 2 列；980 筛选栏共同条件改为单列；目标编辑器属性条件在中窄宽度改为两列加独立移除按钮，避免标签逐字挤压。尚未进行实机截图复验。
- 本轮补充 Web fixture：新增一件与“精准手炮”同 Hash 的锁定重复实例，并支持不同光等与 Perk，用于覆盖同名整理的比较、待应用状态、实例保护和 Wishlist 命中；fixture 基础工厂新增可选锁定字段，武器与护甲预览也补上可识别图标。尚未进行实机截图复验。

## 首页分析（2026-08-26）

- 已按 develop-web-game 的“启动、操作、截图、观察”流程检查 Web 首页。
- 在 1280x720、980x720、760x720 下检查了布局尺寸、横向溢出和键盘 Tab 顺序。
- 当前首页主要问题是信息密度和数据状态表达，不是页面结构崩溃：980px 以下内容变成长页面，仄模块在无有效轮换时占据较大空白；Web fixture 有外部请求拒绝和 404，需要与首页 UI 问题区分。
- 首页 Tab 顺序可从全局工具到首页、AI、刷新公开情报；无可操作奖励时活动卡不进入焦点流，符合只读摘要定位。
- 本轮只读分析，未修改首页代码；后续如要改动，应优先处理 980px 的垂直密度、失败/待确认状态文案和 Web fixture 请求错误。

## 首页继续优化（2026-08-27）

- 继续按“公开情报总览 + 账号相关信息 + 快速进入详情”的定位收敛首页，不加入“下一步行动”或配置 / 登录引导。
- 中等工作区宽度不再过早把所有内容改成单列：周信号保持铁旗与本周活动焦点两列；核心活动改为日落独占一行、突袭与地牢并排；`700px` 以下才恢复单列，减少 `980px` 长页面。
- 周信号最小高度由 `108px` 收紧至 `96px`，活动空态由 `54px` 收紧至 `44px`；仄读取失败、加载中和无轮换状态改为短说明，避免与顶部错误带重复叙述；仄加载骨架高度降为 `56px`，仍保留 8 件库存数量映射。
- 活动卡和周信号中的奖励、待确认、公开接口状态等文案改为读取首页共享 `HomeCopy`；中文回退不变，英文预览不再混入这些中文硬编码。
- 本轮未运行本地自动化验证；后续使用 Web 快速预览与 Desktop 实窗检查 `1280 / 980 / 760` 三档实际容器宽度和明暗主题。

## 待办清理（2026-08-27）

- 用户确认 T7～T13 与 Bug #6～#12 已验收完成，已从 `docs/todo.md` 当前待办中移除；相关实现与过程记录保留在 Git 历史和本文件中。

## 资料库复核（2026-08-27）

Current request: `$develop-web-game` 你再整体分析下资料库，是否还有优化的地方

- 目标：在上一轮资料库查询层级、状态归属、响应式和方向键导航改动后，只读复核共享 Web 页面，不修改产品代码。
- 方法：复用现有 Web fixture 与 Playwright 操作循环，检查 1280 / 980 / 760 宽度、装备与 Perk 模式、搜索辅助、来源详情、统一装备详情、键盘焦点、页面溢出和控制台错误。
- 说明：当前产品是 React 工作台而不是 canvas 游戏，因此不新增 `render_game_to_text`、`advanceTime` 或游戏全屏接口；DOM 文本、截图和页面状态作为观察依据。
- 已完成 1280×900、980×900、760×900 三档检查，并覆盖装备 / Perk Tab、搜索辅助、高级筛选、来源详情、Perk 关联装备、统一装备详情打开与 Escape 关闭。
- 已确认正常：三档宽度均无 body 级横向溢出；搜索框获得初始焦点；模式 Tab 支持方向键切换；关联装备可打开统一详情；详情关闭按钮获得初始焦点，Escape 关闭后焦点恢复到来源按钮。`SharedItemDetailDialog` 通过 `aria-labelledby` 指向标题，审查脚本中的 `dialogLabel: null` 不是无障碍缺陷。
- P1：760px 单列合同没有生效。实测工作区仍为 `240px + 445px` 两栏；基础规则 `.app-shell .library-workbench` 的 specificity 高于 700px 断点中的 `:where(.app-shell) .library-workbench`，导致 `grid-template-columns: 1fr` 被覆盖。高级筛选、来源详情或关联装备展开后，查询栏成为长竖条，结果行最高约 487px。
- P2：资料库页面仍直接展示完整 `DestinyInventoryItemDefinition.26.06.16.0000`，并在页面说明、筛选和结果状态中暴露 `Manifest`。这与 `docs/development.md` 的普通 UI 统一使用“资料库”、完整技术版本只进入设置或诊断的边界冲突；760px 下长技术版本还会拆成多行。
- P2：Web fixture 只有 1 件武器、1 个 Perk、1 条最近查询、0 收藏，缺少多版本、护甲、长列表、分页、收藏成功/失败、搜索空/失败、资料库缺组件/有更新等场景，无法验证方向键跨多行、多版本 canonical 合并和完整状态矩阵。
- P2：点击“最近查询”只把名称填入装备查询条件，不执行搜索，也不把焦点移回搜索框；当前名称和行为语义不一致。若定位为恢复查询，应直接执行；若定位为快速填充，应改名并恢复输入焦点。
- P3：1280 / 980 下查询栏固定约 240px，搜索输入实际约 122px，中文 placeholder 明显截断。可在中宽度适度增加栏宽、让搜索按钮换行，或缩短提示文案。
- P2：Web fixture 持续产生本地事件服务 `ERR_CONNECTION_REFUSED` 和 `/api/home-snapshot` 404。fallback 可工作，但预览控制台不是干净状态，应与产品 UI 缺陷分开处理。
- 建议顺序：先修复 700px 断点 specificity；再收敛玩家文案和版本摘要；然后明确最近查询交互；最后补齐 Web fixture 状态矩阵后继续做 Desktop 真实数据验收。
- 本轮仅追加 `progress.md` 并生成 `.local-data/tmp/library-review/` 审查资产，未修改资料库产品代码。
- 已按复核结论开始实现：将 `.library-workbench` 基础规则统一为低 specificity，确保 940px / 700px 容器断点按顺序覆盖；搜索控件改为纵向布局，避免输入框被固定按钮列压缩。
- 资料库摘要使用 `formatLibraryVersion` 只显示 `YYYY/MM/DD`，解析失败回退“可用”，更新提示也不再泄漏完整技术版本；资料库页面普通文案中的 `Manifest` 已替换为玩家可理解的“资料库”，并补齐英文对应文案。
- “最近查询”新增显式 action：Desktop 同步切回装备模式、恢复查询词并使用显式 query 发起搜索，避免 React 状态提交时序导致旧查询；Web fixture 同步恢复模式与输入值；点击后焦点回到主搜索框。
- 资料库页面标题副文案位于共享 `ProductShellHost`，已做单行最小修改，将“本地 Manifest”改为“本地资料库”；未调整共享外壳结构或其他菜单。
- 本轮按仓库规则未自动运行测试、类型检查、构建或视觉脚本；Web fixture 状态矩阵与预览控制台请求错误仍作为后续独立优化项。
- 已继续扩充资料库 Web fixture：装备结果包含同名不同发布版本、仓库持有武器、猎人护甲和其他只读定义；Perk 结果包含多条记录、普通 / 强化变体、多个关联装备和零关联空态；补充本地 data URL 图标、实时公共活动 / 角色商人来源、三条历史和初始收藏。
- Web 资料库收藏已改为会话内真实状态，添加与取消会立即更新共享页面；页面模型接收当前 `LibraryHistory`，不再固定返回静态收藏。
- 删除 `ProductShellHost` 每次渲染向 `127.0.0.1:7777/event` 发送的遗留调试请求；Vite fixture 预览跳过不存在的 `/api/home-snapshot`，正式 Web 环境继续保留真实 API 请求。
- 当前剩余验收集中在 Desktop 真实资料、错误状态矩阵和明暗主题实窗；本轮仍未自动运行测试、类型检查、构建或视觉脚本。
